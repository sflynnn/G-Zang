# G-Zang (瑜版帟妫?) 闁劎璁叉潻鎰樊閹靛鍞?

## 1. 閻滎垰顣ㄧ憴鍕灊

### 1.1 閻滎垰顣ㄧ猾璇茬??

#### 1.1.1 瀵偓閸欐垹骞嗘晶?(Development)
- **閻€劑鈧??*閿涙艾绱戦崣鎴ｇ殶鐠囨洏鈧礁宕熼崗鍐╃ゴ鐠??- **闁板秶鐤?**閿涙碍娓舵担搴ㄥ帳缂冾喛顩﹀Ч鍌︾礉韫囶偊鈧喎鎯庨崝?- **閺佺増宓?**閿涙碍绁寸拠鏇熸殶閹诡噯绱濋崗浣筋啅閺佺増宓佹稉銏犮亼
- **鐠佸潡妫?**閿涙矮绮庡鈧崣鎴濇礋闂冪喎鍞撮柈銊問闂??
#### 1.1.2 濞村鐦悳顖氼暔 (Testing)
- **閻€劑鈧??*閿涙岸娉﹂幋鎰ゴ鐠囨洏鈧胶閮寸紒鐔哥ゴ鐠囨洏鈧線鐛欓弨鑸电ゴ鐠??- **闁板秶鐤?**閿涙矮绗岄悽鐔堕獓閻滎垰顣ㄦ稉鈧懛?- **閺佺増宓?**閿涙氨鏁撴禍褎鏆熼幑顔垮姎閺佸繐澹囬張?- **鐠佸潡妫?**閿涙碍绁寸拠鏇炴礋闂冪喎鎷版禍褍鎼ч崶銏ゆЕ

#### 1.1.3 閻㈢喍楠囬悳顖氼暔 (Production)
- **閻€劑鈧??*閿涙碍顒滃蹇擃嚠婢舵牗婀囬崝?- **闁板秶鐤?**閿涙岸鐝崣顖滄暏闂嗗棛鍏㈤柊宥囩枂
- **閺佺増宓?**閿涙氨婀＄?圭偘绗熼崝鈩冩殶閹??- **鐠佸潡妫?**閿涙艾顦婚柈銊ф暏閹寸柉顔栭梻?
### 1.2 閺堝秴濮熼崳銊╁帳缂冾喛顩﹀Ч?
#### 1.2.1 鎼存梻鏁ら張宥呭閸ｃ劑鍘ょ純?```yaml
# 閺堚偓娴ｅ酣鍘ょ純?(瀵偓閸??濞村鐦?)
CPU: 2閺??閸愬懎鐡?: 4GB
鐎涙ê鍋?: 50GB SSD
缂冩垹绮?: 1Mbps

# 閹恒劏宕橀柊宥囩枂 (閻㈢喍楠?)
CPU: 4閺??閸愬懎鐡?: 8GB
鐎涙ê鍋?: 100GB SSD
缂冩垹绮?: 10Mbps
```

#### 1.2.2 閺佺増宓佹惔鎾存箛閸斺?虫珤闁板秶鐤?
```yaml
# MySQL閺堝秴濮熼崳?CPU: 4閺??閸愬懎鐡?: 16GB
鐎涙ê鍋?: 500GB SSD (NVMe閹恒劏宕?)
缂冩垹绮?: 10Mbps

# Redis閺堝秴濮熼崳?CPU: 2閺??閸愬懎鐡?: 8GB
鐎涙ê鍋?: 100GB SSD
缂冩垹绮?: 10Mbps
```

---

## 2. 閹垛偓閺堫垱鐖ら柈銊ц

### 2.1 閸氬海顏柈銊ц (Spring Boot)

#### 2.1.1 JAR閸栧懘鍎寸純?```bash
# 1. 閸掓稑缂撻柈銊ц閻╊喖缍?
sudo mkdir -p /opt/gzang/backend
cd /opt/gzang/backend

# 2. 娑撳﹣绱禞AR閸??scp gzang-server-1.0.0.jar user@server:/opt/gzang/backend/

# 3. 閸掓稑缂撻柊宥囩枂閺傚洣娆?
vim application-prod.yml
```

#### 2.1.2 Docker闁劎璁?
```dockerfile
# Dockerfile
FROM openjdk:17-jdk-alpine
WORKDIR /app
COPY target/gzang-server-1.0.0.jar app.jar
EXPOSE 8080
ENTRYPOINT ["java", "-jar", "app.jar", "--spring.profiles.active=prod"]
```

```bash
# 閺嬪嫬缂撻梹婊冨剼
docker build -t gzang/backend:1.0.0 .

# 鏉╂劘顢戠?圭懓娅?
docker run -d \
  --name gzang-backend \
  -p 8080:8080 \
  -v /opt/gzang/config:/app/config \
  -e SPRING_PROFILES_ACTIVE=prod \
  gzang/backend:1.0.0
```

### 2.2 閸撳秶顏柈銊ц (Vue3)

#### 2.2.1 闂堟瑦鈧浇绁┃鎰板劥缂??```bash
# 1. 閺嬪嫬缂撻悽鐔堕獓閻楀牊婀?
npm run build

# 2. 闁劎璁查崚鐧梘inx
sudo cp -r dist/* /usr/share/nginx/html/

# 3. 闁板秶鐤哊ginx
server {
    listen 80;
    server_name gzang.com;

    location / {
        root /usr/share/nginx/html;
        index index.html;
        try_files $uri $uri/ /index.html;
    }

    # API娴狅絿鎮?
    location /api/ {
        proxy_pass http://localhost:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
```

#### 2.2.2 CDN闁劎璁茬粵鏍殣
```javascript
// vite.config.js
export default {
  build: {
    rollupOptions: {
      output: {
        manualChunks: {
          vendor: ['vue', 'vue-router'],
          ui: ['naive-ui'],
          utils: ['axios', 'dayjs']
        }
      }
    }
  }
}
```

### 2.3 閺佺増宓佹惔鎾诲劥缂??
#### 2.3.1 MySQL闁劎璁?
```bash
# Docker Compose闁板秶鐤?
version: '3.8'
services:
  mysql:
    image: mysql:8.0
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
      MYSQL_DATABASE: gzang
      MYSQL_USER: gzang
      MYSQL_PASSWORD: ${MYSQL_PASSWORD}
    volumes:
      - mysql_data:/var/lib/mysql
      - ./db/schema.sql:/docker-entrypoint-initdb.d/schema.sql
    ports:
      - "3306:3306"
    command: --default-authentication-plugin=mysql_native_password

volumes:
  mysql_data:
```

#### 2.3.2 Redis闁劎璁?
```bash
# Redis闂嗗棛鍏㈤柊宥囩枂
version: '3.8'
services:
  redis-master:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    command: redis-server --appendonly yes

  redis-slave:
    image: redis:7-alpine
    ports:
      - "6380:6379"
    volumes:
      - redis_slave_data:/data
    command: redis-server --slaveof redis-master 6379 --appendonly yes

volumes:
  redis_data:
  redis_slave_data:
```

---

## 3. CI/CD濞翠焦鎸夌痪?
### 3.1 GitHub Actions闁板秶鐤?

#### 3.1.1 閸氬海顏珻I/CD
```yaml
# .github/workflows/backend.yml
name: Backend CI/CD

on:
  push:
    branches: [ main, develop ]
    paths: [ 'server/**' ]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Set up JDK 17
        uses: actions/setup-java@v3
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Cache Maven packages
        uses: actions/cache@v3
        with:
          path: ~/.m2
          key: ${{ runner.os }}-m2-${{ hashFiles('**/pom.xml') }}

      - name: Run tests
        run: mvn test

      - name: Build JAR
        run: mvn package -DskipTests

      - name: Build Docker image
        run: |
          docker build -t gzang/backend:${{ github.sha }} ./server
          docker tag gzang/backend:${{ github.sha }} gzang/backend:latest

      - name: Push Docker image
        run: |
          echo ${{ secrets.DOCKER_PASSWORD }} | docker login -u ${{ secrets.DOCKER_USERNAME }} --password-stdin
          docker push gzang/backend:${{ github.sha }}
          docker push gzang/backend:latest
```

#### 3.1.2 閸撳秶顏珻I/CD
```yaml
# .github/workflows/frontend.yml
name: Frontend CI/CD

on:
  push:
    branches: [ main, develop ]
    paths: [ 'apps/**', 'packages/**' ]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install

      - name: Run tests
        run: pnpm test

      - name: Build
        run: pnpm build

      - name: Deploy to CDN
        run: |
          # 娑撳﹣绱堕崚鐧圖N (缁?杞扮伐)
          aws s3 sync dist/ s3://gzang-cdn/ --delete
          aws cloudfront create-invalidation --distribution-id ${{ secrets.CDN_DISTRIBUTION_ID }} --paths "/*"
```

### 3.2 闁劎璁查懘姘拱

#### 3.2.1 閽冩繄璞㈤柈銊ц閼存碍婀?
```bash
#!/bin/bash
# blue-green-deploy.sh

# 闁板秶鐤?
APP_NAME="gzang-backend"
BLUE_PORT="8080"
GREEN_PORT="8081"
HEALTH_CHECK_URL="http://localhost"

# 濡偓閺屻儱缍嬮崜宥堢箥鐞涘瞼娈戦悧鍫熸拱
if docker ps | grep -q "${APP_NAME}-blue"; then
    CURRENT="blue"
    NEXT="green"
    CURRENT_PORT=$BLUE_PORT
    NEXT_PORT=$GREEN_PORT
else
    CURRENT="green"
    NEXT="blue"
    CURRENT_PORT=$GREEN_PORT
    NEXT_PORT=$BLUE_PORT
fi

echo "瑜版挸澧犳潻鎰攽閻楀牊婀?: $CURRENT (缁旑垰褰?: $CURRENT_PORT)"
echo "闁劎璁查弬鎵閺堫剙鍩?: $NEXT (缁旑垰褰?: $NEXT_PORT)"

# 閸嬫粍顒涙稉瀣╃娑擃亞澧楅張?(婵″倹鐏夌?涙ê婀?)
docker stop ${APP_NAME}-$NEXT 2>/dev/null
docker rm ${APP_NAME}-$NEXT 2>/dev/null

# 閸氼垰濮╅弬鎵閺??docker run -d \
  --name ${APP_NAME}-$NEXT \
  -p $NEXT_PORT:8080 \
  -e SPRING_PROFILES_ACTIVE=prod \
  gzang/backend:latest

# 缁涘绶熼崑銉ユ倣濡偓閺??echo "缁涘绶熸惔鏃傛暏閸氼垰濮?..."
for i in {1..30}; do
    if curl -f ${HEALTH_CHECK_URL}:${NEXT_PORT}/actuator/health > /dev/null 2>&1; then
        echo "鎼存梻鏁ら崥顖氬З閹存劕濮?"
        break
    fi
    sleep 10
done

# 閸掑洦宕插ù渚?鍣? (Nginx闁板秶鐤嗛弴瀛樻煀)
sed -i "s/proxy_pass http:\/\/localhost:$CURRENT_PORT;/proxy_pass http:\/\/localhost:$NEXT_PORT;/" /etc/nginx/conf.d/gzang.conf
nginx -s reload

# 閸嬫粍顒涢弮褏澧楅張?docker stop ${APP_NAME}-$CURRENT
docker rm ${APP_NAME}-$CURRENT

echo "闁劎璁茬?瑰本鍨氶敍?
```

---

## 4. 閻╂垶甯舵稉搴℃啞鐠??
### 4.1 鎼存梻鏁ら惄鎴炲付

#### 4.1.1 Spring Boot Actuator闁板秶鐤?
```yaml
# application-prod.yml
management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus
  endpoint:
    health:
      show-details: when-authorized
  metrics:
    export:
      prometheus:
        enabled: true
```

#### 4.1.2 Prometheus闁板秶鐤?
```yaml
# prometheus.yml
global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'gzang-backend'
    static_configs:
      - targets: ['localhost:8080']
    metrics_path: '/actuator/prometheus'

  - job_name: 'nginx'
    static_configs:
      - targets: ['localhost:9090']
```

### 4.2 閸╄櫣顢呯拋鐐煢閻╂垶甯?

#### 4.2.1 Grafana娴狀亣銆冮弶?- **缁崵绮洪幐鍥ㄧ垼**閿涙PU娴ｈ法鏁ら悳鍥モ偓浣稿敶鐎涙ü濞囬悽銊у芳閵嗕胶顥嗛惄妯瑰▏閻€劎宸奸妴浣虹秹缂佹粍绁﹂柌?- **鎼存梻鏁ら幐鍥ㄧ垼**閿涙艾鎼锋惔鏃?妞傞梻娣偓浣告偠閸氭劙鍣洪妴渚?鏁婄拠顖滃芳閵嗕焦妞跨捄鍐暏閹撮攱鏆?
- **娑撴艾濮熼幐鍥ㄧ垼**閿涙矮姘﹂弰鎾剁應閺佽埇鈧胶鏁ら幋閿嬫暈閸愬本鏆熼妴浣筋唶鐠愶箓顣堕悳?
### 4.3 閸涘﹨顒熼柊宥囩枂

#### 4.3.1 AlertManager闁板秶鐤?
```yaml
# alertmanager.yml
global:
  smtp_smtp:
    host: 'smtp.example.com'
    port: 587
    username: 'alert@gzang.com'
    password: '${SMTP_PASSWORD}'

route:
  group_by: ['alertname']
  group_wait: 10s
  group_interval: 10s
  repeat_interval: 1h
  receiver: 'email'

receivers:
  - name: 'email'
    email_configs:
      - to: 'ops@gzang.com'
        subject: 'G-Zang缁崵绮洪崨濠咁劅: {{ .GroupLabels.alertname }}'
```

#### 4.3.2 閸涘﹨顒熺憴鍕灟
```yaml
# alert_rules.yml
groups:
  - name: gzang
    rules:
      # 鎼存梻鏁ら崑銉ユ倣濡偓閺??      - alert: AppDown
        expr: up == 0
        for: 5m
        labels:
          severity: critical
        annotations:
          summary: "鎼存梻鏁? {{ $labels.job }} 娑撳秴褰查悽?

      # 閸濆秴绨查弮鍫曟？閸涘﹨顒?
      - alert: HighResponseTime
        expr: http_request_duration_seconds{quantile="0.95"} > 2
        for: 5m
        labels:
          severity: warning
        annotations:
          summary: "閸濆秴绨查弮鍫曟？鏉╁洭鐝?: {{ $value }}s"

      # 闁挎瑨顕ら悳鍥ф啞鐠??      - alert: HighErrorRate
        expr: rate(http_requests_total{status=~"5.."}[5m]) / rate(http_requests_total[5m]) > 0.05
        for: 5m
        labels:
          severity: warning
        annotations:
          summary: "闁挎瑨顕ら悳鍥箖妤?? {{ $value }}"
```

---

## 5. 婢跺洣鍞ゆ稉搴划婢??
### 5.1 閺佺増宓佹惔鎾愁槵娴犵晫鐡ラ悾?
#### 5.1.1 閼奉亜濮╂径鍥﹀敜閼存碍婀?
```bash
#!/bin/bash
# mysql_backup.sh

BACKUP_DIR="/opt/backups/mysql"
DATE=$(date +%Y%m%d_%H%M%S)
BACKUP_FILE="${BACKUP_DIR}/gzang_${DATE}.sql"

# 閸掓稑缂撴径鍥﹀敜閻╊喖缍?
mkdir -p $BACKUP_DIR

# 閹笛嗩攽婢跺洣鍞?
mysqldump -h localhost -u gzang -p${MYSQL_PASSWORD} gzang > $BACKUP_FILE

# 閸樺缂夋径鍥﹀敜閺傚洣娆?
gzip $BACKUP_FILE

# 閸掔娀娅?7婢垛晛澧犻惃鍕槵娴??find $BACKUP_DIR -name "*.sql.gz" -mtime +7 -delete

echo "婢跺洣鍞ょ?瑰本鍨?: ${BACKUP_FILE}.gz"
```

#### 5.1.2 Redis婢跺洣鍞ら柊宥囩枂
```redis.conf
# Redis閹镐椒绠欓崠鏍帳缂??save 900 1      # 900缁夋帒鍞撮懛鍐茬毌閺??娑撶尛ey閸欐ê瀵?
save 300 10     # 300缁夋帒鍞撮懛鍐茬毌閺??0娑撶尛ey閸欐ê瀵?
save 60 10000   # 60缁夋帒鍞撮懛鍐茬毌閺??0000娑撶尛ey閸欐ê瀵?

# AOF閹镐椒绠欓崠?appendonly yes
appendfilename "appendonly.aof"
appendfsync everysec
```

### 5.2 閺佺増宓侀幁銏狀槻濞翠胶鈻?

#### 5.2.1 MySQL閺佺増宓侀幁銏狀槻
```bash
# 閸嬫粍顒涙惔鏃傛暏閺堝秴濮?
docker stop gzang-backend

# 閹垹顦查弫鐗堝祦鎼??gunzip gzang_20240114_120000.sql.gz
mysql -h localhost -u gzang -p${MYSQL_PASSWORD} gzang < gzang_20240114_120000.sql

# 闁插秴鎯庢惔鏃傛暏閺堝秴濮?
docker start gzang-backend
```

#### 5.2.2 Redis閺佺増宓侀幁銏狀槻
```bash
# 閸嬫粍顒汻edis閺堝秴濮?
docker stop gzang-redis

# 閹垹顦查弫鐗堝祦
docker run --rm \
  -v gzang_redis_data:/data \
  -v $(pwd)/backup:/backup \
  redis:7-alpine \
  redis-server --appendonly yes --dir /backup

# 闁插秴鎯嶳edis閺堝秴濮?
docker start gzang-redis
```

---

## 6. 鐎瑰鍙忛柊宥囩枂

### 6.1 缂冩垹绮剁?瑰鍙?

#### 6.1.1 Nginx鐎瑰鍙忛柊宥囩枂
```nginx
# nginx.conf
server {
    listen 443 ssl http2;
    server_name gzang.com;

    # SSL闁板秶鐤?
    ssl_certificate /etc/ssl/certs/gzang.crt;
    ssl_certificate_key /etc/ssl/private/gzang.key;
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers ECDHE-RSA-AES128-GCM-SHA256:ECDHE-RSA-AES256-GCM-SHA384;

    # 鐎瑰鍙忔径?    add_header X-Frame-Options DENY;
    add_header X-Content-Type-Options nosniff;
    add_header X-XSS-Protection "1; mode=block";
    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains";

    # 鐠囬攱鐪伴梽鎰煑
    limit_req zone=api burst=10 nodelay;
    limit_req zone=static burst=20 nodelay;

    # CORS闁板秶鐤?
    location /api/ {
        if ($request_method = 'OPTIONS') {
            add_header Access-Control-Allow-Origin *;
            add_header Access-Control-Allow-Methods 'GET, POST, PUT, DELETE, OPTIONS';
            add_header Access-Control-Allow-Headers 'Authorization, Content-Type';
            return 204;
        }
    }
}
```

#### 6.1.2 闂冭尙浼?婢ф瑩鍘ょ純?```bash
# UFW闂冭尙浼?婢ф瑨顫夐崚?ufw default deny incoming
ufw default allow outgoing

# 閸忎浇顔廠SH
ufw allow ssh

# 閸忎浇顔廐TTP/HTTPS
ufw allow 80
ufw allow 443

# 閸忎浇顔忔惔鏃傛暏缁旑垰褰? (娴犲懎鍞寸純?
ufw allow from 10.0.0.0/8 to any port 8080

# 閸氼垳鏁ら梼鑼紑婢??ufw enable
```

### 6.2 鎼存梻鏁ょ?瑰鍙?

#### 6.2.1 Spring Security闁板秶鐤?
```java
@Configuration
public class SecurityConfig {

    @Bean
    public SecurityFilterChain filterChain(HttpSecurity http) throws Exception {
        http
            .csrf().disable()
            .cors().and()
            .sessionManagement().sessionCreationPolicy(SessionCreationPolicy.STATELESS)
            .and()
            .authorizeHttpRequests(authz -> authz
                .requestMatchers("/api/v1/auth/**").permitAll()
                .requestMatchers("/actuator/**").hasRole("ADMIN")
                .anyRequest().authenticated()
            )
            .addFilterBefore(jwtAuthenticationFilter(), UsernamePasswordAuthenticationFilter.class);

        return http.build();
    }
}
```

---

## 7. 閹嗗厴娴兼ê瀵?

### 7.1 閺佺増宓佹惔鎾茬喘閸??
#### 7.1.1 MySQL閹嗗厴闁板秶鐤?
```ini
# my.cnf
[mysqld]
innodb_buffer_pool_size = 1G
innodb_log_file_size = 256M
max_connections = 200
query_cache_size = 64M
tmp_table_size = 128M
max_heap_table_size = 128M

# 閹便垺鐓＄拠銏℃）韫??slow_query_log = 1
slow_query_log_file = /var/log/mysql/mysql-slow.log
long_query_time = 2
```

#### 7.1.2 Redis閹嗗厴娴兼ê瀵?
```redis.conf
# Redis閹嗗厴闁板秶鐤?
maxmemory 512mb
maxmemory-policy allkeys-lru

# 缁備胶鏁ら崡閬嶆珦閸涙垝鎶?
rename-command FLUSHDB ""
rename-command FLUSHALL ""
rename-command SHUTDOWN SHUTDOWN_REDIS
```

### 7.2 鎼存梻鏁ゆ导妯哄

#### 7.2.1 JVM鐠嬪啩绱?
```bash
# JVM閸氼垰濮╅崣鍌涙殶
java -server \
  -Xms2g -Xmx4g \
  -XX:+UseG1GC \
  -XX:MaxGCPauseMillis=200 \
  -XX:+PrintGCDetails \
  -XX:+PrintGCTimeStamps \
  -Xloggc:/opt/gzang/logs/gc.log \
  -jar app.jar
```

#### 7.2.2 閸撳秶顏幀褑鍏樻导妯哄
```javascript
// vite.config.js
export default {
  build: {
    rollupOptions: {
      output: {
        manualChunks: {
          vendor: ['vue', 'vue-router', 'pinia'],
          ui: ['naive-ui', 'vant'],
          chart: ['echarts']
        }
      }
    },
    chunkSizeWarningLimit: 1000
  },
  plugins: [
    // 閸樺缂?
    viteCompression({
      algorithm: 'gzip',
      threshold: 1024
    })
  ]
}
```

---

## 8. 閺佸懘娈伴幒鎺撶叀

### 8.1 鐢瓕顫嗛梻顕?顣界拠濠冩焽

#### 8.1.1 鎼存梻鏁ら崥顖氬З婢惰精瑙?
```bash
# 濡偓閺屻儳顏崣锝呭窗閻??netstat -tlnp | grep 8080

# 濡偓閺屻儲妫╄箛?tail -f /opt/gzang/logs/application.log

# 濡偓閺岊檺VM閸愬懎鐡?
jstat -gcutil <pid> 1000 10
```

#### 8.1.2 閺佺増宓佹惔鎾圭箾閹恒儵妫舵０?```bash
# 濞村鐦弫鐗堝祦鎼存捁绻涢幒?mysql -h localhost -u gzang -p -e "SELECT 1"

# 濡偓閺屻儴绻涢幒銉︾潨閻樿埖鈧??# 閸︺劌绨查悽銊よ厬鐠佸潡妫? /actuator/metrics 閺屻儳婀呮潻鐐村复濮圭姵瀵氶弽?```

#### 8.1.3 閸撳秶顏崝鐘烘祰缂傛挻鍙?
```bash
# 濡偓閺岊檳DN閻樿埖鈧??curl -I https://cdn.gzang.com/

# 濡偓閺岊檾ginx闁板秶鐤?
nginx -t

# 閺屻儳婀呯拋鍧楁６閺冦儱绻?
tail -f /var/log/nginx/access.log
```

### 8.2 缁毖勨偓銉︿划婢跺秵绁︾粙?
#### 8.2.1 閺堝秴濮熺?规洘婧?閹垹顦?
1. **缁斿宓嗛崫宥呯安**閿涙碍鏁归崚鏉挎啞鐠?锕?鎮?5閸掑棝鎸撻崘鍛惙鎼??2. **闂傤噣顣界拠濠冩焽**閿涙碍顥呴弻銉ョ安閻€劍妫╄箛妤?鎷扮化鑽ょ埠鐠у嫭绨?
3. **閺堝秴濮熼柌宥呮儙**閿涙艾鐨剧拠鏇㈠櫢閸氼垰绨查悽銊︽箛閸??4. **閸ョ偞绮撮柈銊ц**閿涙艾顩ч張澶婄箑鐟曚緤绱濋崶鐐寸泊閸掗绗傛稉鈧悧鍫熸拱
5. **閺佺増宓侀幁銏狀槻**閿涙矮绮犳径鍥﹀敜閹垹顦查弫鐗堝祦
6. **闂傤噣顣介弽鐟版礈閸掑棙鐎?**閿涙矮绨ㄩ崥搴″瀻閺嬫劕鑻熼崚璺虹暰妫板嫰妲婚幒顏呮煢

#### 8.2.2 閺佺増宓佹稉銏犮亼閹垹顦?
1. **鐠囧嫪鍙婅ぐ鍗炴惙**閿涙氨鈥樼?规碍鏆熼幑顔绘丢婢惰精瀵栭崶鏉戞嫲瑜板崬鎼风粙瀣
2. **閸嬫粍顒涢張宥呭**閿涙碍娈忛崑婊呮祲閸忚櫕婀囬崝鈥冲晸閸忋儲鎼锋担?3. **閺佺増宓侀幁銏狀槻**閿涙矮绮犳径鍥﹀敜閹垹顦查弫鐗堝祦
4. **閺佺増宓侀弽锟犵崣**閿涙岸鐛欑拠浣逛划婢跺秵鏆熼幑顔炬畱鐎瑰本鏆ｉ幀?5. **閺堝秴濮熼柌宥呮儙**閿涙碍浠径宥嗩劀鐢憡婀囬崝?6. **妫板嫰妲婚幒顏呮煢**閿涙艾濮炲鍝勵槵娴犵晫鐡ラ悾銉ユ嫲閻╂垶甯?

---

## 9. 缂佸瓨濮㈢拋鈥冲灊

### 9.1 閺冦儱鐖剁紒瀛樺Б娴犺濮?
- **濮ｅ繑妫?**閿涙碍顥呴弻銉ч兇缂佺喐妫╄箛妤嬬礉閻╂垶甯堕崗鎶芥暛閹稿洦鐖?
- **濮ｅ繐鎳?**閿涙碍娲块弬鎵兇缂佺喕藟娑撲緤绱濆〒鍛倞娑撳瓨妞傞弬鍥︽
- **濮ｅ繑婀?**閿涙碍鏆熼幑顔肩氨婢跺洣鍞ゆ宀冪槈閿涘本鈧嗗厴娴兼ê瀵插Λ鈧弻?- **濮ｅ繐顒滄惔?*閿涙艾鐣ㄩ崗銊ㄧ槑娴煎府绱濈?瑰綊鍣虹憴鍕灊

### 9.2 鐎规碍婀″Λ鈧弻銉︾閸??- [ ] 缁崵绮虹挧鍕爱娴ｈ法鏁ら幆鍛枌
- [ ] 鎼存梻鏁ら幀褑鍏橀幐鍥ㄧ垼
- [ ] 閺佺増宓佹惔鎾圭箥鐞涘瞼濮搁幀?- [ ] 婢跺洣鍞ら弬鍥︽鐎瑰本鏆ｉ幀?- [ ] 鐎瑰鍙忓⿻蹇旂閹殿偅寮?
- [ ] 閺冦儱绻旈弬鍥︽鏉烆喛娴?
- [ ] 鐠囦椒鍔熼崚鐗堟埂濡偓閺??
---

**閺傚洦銆傞悧鍫熸拱**閿??.0.0
**閺堚偓閸氬孩娲块弬?*閿??026-01-14
**缂佸瓨濮㈡禍鍝勬喅**閿涙俺绻嶇紒鏉戞礋闂??
