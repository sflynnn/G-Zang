# G-Zang (归藏) 数据库设计

## 1. 数据库概览
- **数据库类型**：MySQL 8.0+
- **字符集**：`utf8mb4`
- **存储引擎**：`InnoDB`

## 2. 核心表结构

### 2.1 用户与权限相关表

#### `t_user` (用户表)
| 字段名 | 类型 | 长度 | 默认值 | 是否为空 | 备注 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| `id` | BIGINT | 20 | | NOT NULL | 主键，用户ID |
| `username` | VARCHAR | 64 | | NOT NULL | 用户名（手机号/邮箱） |
| `password` | VARCHAR | 255 | | NOT NULL | 密码（加密存储） |
| `nickname` | VARCHAR | 64 | | NULL | 用户昵称 |
| `avatar` | VARCHAR | 255 | | NULL | 用户头像URL |
| `role_id` | BIGINT | 20 | | NOT NULL | 角色ID |
| `company_id` | BIGINT | 20 | | NULL | 所属公司ID（个人用户为NULL） |
| `status` | TINYINT | 1 | 1 | NOT NULL | 状态 (0:禁用, 1:正常) |
| `create_time` | DATETIME | | CURRENT_TIMESTAMP | NOT NULL | 创建时间 |
| `update_time` | DATETIME | | CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP | NOT NULL | 更新时间 |

#### `t_role` (角色表)
| 字段名 | 类型 | 长度 | 默认值 | 是否为空 | 备注 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| `id` | BIGINT | 20 | | NOT NULL | 主键，角色ID |
| `role_name` | VARCHAR | 64 | | NOT NULL | 角色名称（如：管理员、财务、普通用户） |
| `role_code` | VARCHAR | 64 | | NOT NULL | 角色编码 |
| `description` | VARCHAR | 255 | | NULL | 角色描述 |

#### `t_permission` (权限表)
| 字段名 | 类型 | 长度 | 默认值 | 是否为空 | 备注 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| `id` | BIGINT | 20 | | NOT NULL | 主键，权限ID |
| `permission_name` | VARCHAR | 64 | | NOT NULL | 权限名称（如：用户管理、记账录入） |
| `permission_code` | VARCHAR | 64 | | NOT NULL | 权限编码 |

#### `t_role_permission` (角色权限关联表)
| 字段名 | 类型 | 长度 | 默认值 | 是否为空 | 备注 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| `role_id` | BIGINT | 20 | | NOT NULL | 角色ID |
| `permission_id` | BIGINT | 20 | | NOT NULL | 权限ID |

### 2.2 公司与账本相关表

#### `t_company` (公司/组织表)
| 字段名 | 类型 | 长度 | 默认值 | 是否为空 | 备注 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| `id` | BIGINT | 20 | | NOT NULL | 主键，公司ID (tenant_id) |
| `company_name` | VARCHAR | 128 | | NOT NULL | 公司名称 |
| `admin_user_id` | BIGINT | 20 | | NOT NULL | 公司管理员用户ID |
| `create_time` | DATETIME | | CURRENT_TIMESTAMP | NOT NULL | 创建时间 |
| `update_time` | DATETIME | | CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP | NOT NULL | 更新时间 |

#### `t_account` (账户表)
| 字段名 | 类型 | 长度 | 默认值 | 是否为空 | 备注 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| `id` | BIGINT | 20 | | NOT NULL | 主键，账户ID |
| `user_id` | BIGINT | 20 | | NOT NULL | 所属用户ID |
| `company_id` | BIGINT | 20 | | NULL | 所属公司ID（个人账户为NULL） |
| `account_name` | VARCHAR | 64 | | NOT NULL | 账户名称（如：现金、支付宝、招商银行） |
| `account_type` | TINYINT | 1 | 1 | NOT NULL | 账户类型 (1:现金, 2:银行卡, 3:电子支付) |
| `balance` | DECIMAL | 18,2 | 0.00 | NOT NULL | 当前余额 |
| `create_time` | DATETIME | | CURRENT_TIMESTAMP | NOT NULL | 创建时间 |
| `update_time` | DATETIME | | CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP | NOT NULL | 更新时间 |

#### `t_category` (记账分类表)
| 字段名 | 类型 | 长度 | 默认值 | 是否为空 | 备注 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| `id` | BIGINT | 20 | | NOT NULL | 主键，分类ID |
| `user_id` | BIGINT | 20 | | NULL | 所属用户ID（公司分类为NULL） |
| `company_id` | BIGINT | 20 | | NULL | 所属公司ID（个人分类为NULL） |
| `category_name` | VARCHAR | 64 | | NOT NULL | 分类名称（如：餐饮、交通、配件采购） |
| `parent_id` | BIGINT | 20 | 0 | NOT NULL | 父分类ID（0为一级分类） |
| `type` | TINYINT | 1 | 1 | NOT NULL | 类型 (1:收入, 2:支出) |
| `is_system` | TINYINT | 1 | 0 | NOT NULL | 是否系统预设 (0:否, 1:是) |

### 2.3 交易记录相关表

#### `t_transaction` (交易记录表)
| 字段名 | 类型 | 长度 | 默认值 | 是否为空 | 备注 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| `id` | BIGINT | 20 | | NOT NULL | 主键，交易ID |
| `user_id` | BIGINT | 20 | | NOT NULL | 记录用户ID |
| `company_id` | BIGINT | 20 | | NULL | 所属公司ID（个人交易为NULL） |
| `amount` | DECIMAL | 18,2 | 0.00 | NOT NULL | 交易金额 |
| `type` | TINYINT | 1 | 1 | NOT NULL | 交易类型 (1:收入, 2:支出) |
| `category_id` | BIGINT | 20 | | NOT NULL | 分类ID |
| `account_id` | BIGINT | 20 | | NOT NULL | 账户ID |
| `transaction_time` | DATETIME | | CURRENT_TIMESTAMP | NOT NULL | 交易发生时间 |
| `remark` | VARCHAR | 500 | | NULL | 备注 |
| `related_business_id` | VARCHAR | 128 | | NULL | 关联业务ID（如维修单号） |
| `create_time` | DATETIME | | CURRENT_TIMESTAMP | NOT NULL | 创建时间 |
| `update_time` | DATETIME | | CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP | NOT NULL | 更新时间 |

### 2.4 系统管理相关表

#### `t_system_config` (系统配置表)
| 字段名 | 类型 | 长度 | 默认值 | 是否为空 | 备注 |
|--------|------|------|--------|----------|------|
| `id` | BIGINT | 20 | | NOT NULL | 主键，配置ID |
| `config_key` | VARCHAR | 128 | | NOT NULL | 配置键 |
| `config_value` | TEXT | | | NULL | 配置值 |
| `config_type` | VARCHAR | 32 | | NOT NULL | 配置类型 |
| `description` | VARCHAR | 255 | | NULL | 配置描述 |
| `is_system` | TINYINT | 1 | 0 | NOT NULL | 是否系统配置 |
| `create_time` | DATETIME | | CURRENT_TIMESTAMP | NOT NULL | 创建时间 |
| `update_time` | DATETIME | | CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP | NOT NULL | 更新时间 |

#### `t_operation_log` (操作日志表)
| 字段名 | 类型 | 长度 | 默认值 | 是否为空 | 备注 |
|--------|------|------|--------|----------|------|
| `id` | BIGINT | 20 | | NOT NULL | 主键，日志ID |
| `user_id` | BIGINT | 20 | | NULL | 操作用户ID |
| `username` | VARCHAR | 64 | | NULL | 操作用户名 |
| `operation_type` | VARCHAR | 32 | | NOT NULL | 操作类型 |
| `operation_desc` | VARCHAR | 255 | | NULL | 操作描述 |
| `request_method` | VARCHAR | 16 | | NULL | 请求方法 |
| `request_url` | VARCHAR | 500 | | NULL | 请求URL |
| `request_params` | TEXT | | | NULL | 请求参数 |
| `response_code` | INT | 11 | | NULL | 响应状态码 |
| `response_msg` | VARCHAR | 500 | | NULL | 响应消息 |
| `ip_address` | VARCHAR | 64 | | NULL | IP地址 |
| `user_agent` | VARCHAR | 500 | | NULL | 用户代理 |
| `operation_time` | DATETIME | | CURRENT_TIMESTAMP | NOT NULL | 操作时间 |
| `duration` | BIGINT | 20 | | NULL | 执行时长(毫秒) |

#### `t_file_upload` (文件上传表)
| 字段名 | 类型 | 长度 | 默认值 | 是否为空 | 备注 |
|--------|------|------|--------|----------|------|
| `id` | BIGINT | 20 | | NOT NULL | 主键，文件ID |
| `file_name` | VARCHAR | 255 | | NOT NULL | 文件原名 |
| `file_path` | VARCHAR | 500 | | NOT NULL | 文件路径 |
| `file_size` | BIGINT | 20 | | NOT NULL | 文件大小(字节) |
| `file_type` | VARCHAR | 32 | | NOT NULL | 文件类型 |
| `file_md5` | VARCHAR | 64 | | NULL | 文件MD5 |
| `mime_type` | VARCHAR | 128 | | NULL | MIME类型 |
| `upload_user_id` | BIGINT | 20 | | NULL | 上传用户ID |
| `upload_time` | DATETIME | | CURRENT_TIMESTAMP | NOT NULL | 上传时间 |

### 2.5 预算与成本管理表

#### `t_budget` (预算表)
| 字段名 | 类型 | 长度 | 默认值 | 是否为空 | 备注 |
|--------|------|------|--------|----------|------|
| `id` | BIGINT | 20 | | NOT NULL | 主键，预算ID |
| `user_id` | BIGINT | 20 | | NULL | 所属用户ID |
| `company_id` | BIGINT | 20 | | NULL | 所属公司ID |
| `budget_name` | VARCHAR | 128 | | NOT NULL | 预算名称 |
| `budget_type` | VARCHAR | 32 | | NOT NULL | 预算类型(annual/monthly/category) |
| `budget_year` | INT | 11 | | NOT NULL | 预算年份 |
| `budget_month` | INT | 11 | | NULL | 预算月份 |
| `category_id` | BIGINT | 20 | | NULL | 关联分类ID |
| `budget_amount` | DECIMAL | 18,2 | 0.00 | NOT NULL | 预算金额 |
| `used_amount` | DECIMAL | 18,2 | 0.00 | NOT NULL | 已使用金额 |
| `warning_percent` | DECIMAL | 5,2 | 80.00 | NOT NULL | 预警百分比 |
| `status` | TINYINT | 1 | 1 | NOT NULL | 状态(0禁用1启用) |
| `create_user_id` | BIGINT | 20 | | NOT NULL | 创建用户ID |
| `create_time` | DATETIME | | CURRENT_TIMESTAMP | NOT NULL | 创建时间 |
| `update_time` | DATETIME | | CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP | NOT NULL | 更新时间 |

#### `t_cost_center` (成本中心表)
| 字段名 | 类型 | 长度 | 默认值 | 是否为空 | 备注 |
|--------|------|------|--------|----------|------|
| `id` | BIGINT | 20 | | NOT NULL | 主键，成本中心ID |
| `company_id` | BIGINT | 20 | | NOT NULL | 所属公司ID |
| `cost_center_code` | VARCHAR | 64 | | NOT NULL | 成本中心编码 |
| `cost_center_name` | VARCHAR | 128 | | NOT NULL | 成本中心名称 |
| `parent_id` | BIGINT | 20 | 0 | NOT NULL | 父级成本中心ID |
| `manager_user_id` | BIGINT | 20 | | NULL | 负责人用户ID |
| `budget_amount` | DECIMAL | 18,2 | 0.00 | NOT NULL | 预算金额 |
| `description` | VARCHAR | 255 | | NULL | 描述 |
| `status` | TINYINT | 1 | 1 | NOT NULL | 状态(0禁用1启用) |
| `create_time` | DATETIME | | CURRENT_TIMESTAMP | NOT NULL | 创建时间 |
| `update_time` | DATETIME | | CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP | NOT NULL | 更新时间 |

### 2.6 业务关联表

#### `t_business_repair_order` (维修工单表)
| 字段名 | 类型 | 长度 | 默认值 | 是否为空 | 备注 |
|--------|------|------|--------|----------|------|
| `id` | BIGINT | 20 | | NOT NULL | 主键，工单ID |
| `company_id` | BIGINT | 20 | | NOT NULL | 所属公司ID |
| `order_no` | VARCHAR | 64 | | NOT NULL | 工单编号 |
| `customer_name` | VARCHAR | 128 | | NOT NULL | 客户姓名 |
| `customer_phone` | VARCHAR | 32 | | NULL | 客户电话 |
| `vehicle_info` | VARCHAR | 255 | | NULL | 车辆信息 |
| `repair_type` | VARCHAR | 64 | | NOT NULL | 维修类型 |
| `repair_status` | TINYINT | 1 | 1 | NOT NULL | 维修状态(1待修2进行中3完成) |
| `estimated_amount` | DECIMAL | 18,2 | 0.00 | NOT NULL | 预估金额 |
| `actual_amount` | DECIMAL | 18,2 | 0.00 | NOT NULL | 实际金额 |
| `repair_user_id` | BIGINT | 20 | | NULL | 维修人员ID |
| `create_user_id` | BIGINT | 20 | | NOT NULL | 创建用户ID |
| `create_time` | DATETIME | | CURRENT_TIMESTAMP | NOT NULL | 创建时间 |
| `update_time` | DATETIME | | CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP | NOT NULL | 更新时间 |

#### `t_business_repair_cost` (维修成本明细表)
| 字段名 | 类型 | 长度 | 默认值 | 是否为空 | 备注 |
|--------|------|------|--------|----------|------|
| `id` | BIGINT | 20 | | NOT NULL | 主键，成本明细ID |
| `repair_order_id` | BIGINT | 20 | | NOT NULL | 维修工单ID |
| `cost_type` | VARCHAR | 32 | | NOT NULL | 成本类型(parts/labor) |
| `cost_name` | VARCHAR | 128 | | NOT NULL | 成本名称 |
| `cost_amount` | DECIMAL | 18,2 | 0.00 | NOT NULL | 成本金额 |
| `cost_center_id` | BIGINT | 20 | | NULL | 成本中心ID |
| `remark` | VARCHAR | 255 | | NULL | 备注 |
| `create_user_id` | BIGINT | 20 | | NOT NULL | 创建用户ID |
| `create_time` | DATETIME | | CURRENT_TIMESTAMP | NOT NULL | 创建时间 |

### 2.7 移动端专用表

#### `t_user_device` (用户设备表)
| 字段名 | 类型 | 长度 | 默认值 | 是否为空 | 备注 |
|--------|------|------|--------|----------|------|
| `id` | BIGINT | 20 | | NOT NULL | 主键，设备ID |
| `user_id` | BIGINT | 20 | | NOT NULL | 用户ID |
| `device_token` | VARCHAR | 255 | | NOT NULL | 设备推送令牌 |
| `device_type` | VARCHAR | 32 | | NOT NULL | 设备类型(iOS/Android) |
| `device_model` | VARCHAR | 128 | | NULL | 设备型号 |
| `os_version` | VARCHAR | 32 | | NULL | 操作系统版本 |
| `app_version` | VARCHAR | 32 | | NULL | 应用版本 |
| `is_active` | TINYINT | 1 | 1 | NOT NULL | 是否活跃 |
| `last_login_time` | DATETIME | | NULL | 最后登录时间 |
| `create_time` | DATETIME | | CURRENT_TIMESTAMP | NOT NULL | 创建时间 |
| `update_time` | DATETIME | | CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP | NOT NULL | 更新时间 |

#### `t_offline_transaction` (离线交易记录表)
| 字段名 | 类型 | 长度 | 默认值 | 是否为空 | 备注 |
|--------|------|------|--------|----------|------|
| `id` | BIGINT | 20 | | NOT NULL | 主键，离线记录ID |
| `user_id` | BIGINT | 20 | | NOT NULL | 用户ID |
| `device_id` | VARCHAR | 64 | | NOT NULL | 设备唯一标识 |
| `transaction_data` | JSON | | | NOT NULL | 交易数据JSON |
| `sync_status` | TINYINT | 1 | 0 | NOT NULL | 同步状态(0未同步1已同步2同步失败) |
| `sync_time` | DATETIME | | NULL | 同步时间 |
| `sync_attempts` | INT | 11 | 0 | NOT NULL | 同步尝试次数 |
| `create_time` | DATETIME | | CURRENT_TIMESTAMP | NOT NULL | 创建时间 |
| `update_time` | DATETIME | | CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP | NOT NULL | 更新时间 |

#### `t_notification_record` (通知记录表)
| 字段名 | 类型 | 长度 | 默认值 | 是否为空 | 备注 |
|--------|------|------|--------|----------|------|
| `id` | BIGINT | 20 | | NOT NULL | 主键，通知ID |
| `user_id` | BIGINT | 20 | | NOT NULL | 用户ID |
| `notification_type` | VARCHAR | 32 | | NOT NULL | 通知类型 |
| `title` | VARCHAR | 128 | | NOT NULL | 通知标题 |
| `content` | TEXT | | | NOT NULL | 通知内容 |
| `is_read` | TINYINT | 1 | 0 | NOT NULL | 是否已读 |
| `read_time` | DATETIME | | NULL | 阅读时间 |
| `send_status` | TINYINT | 1 | 0 | NOT NULL | 发送状态(0待发送1已发送2发送失败) |
| `send_time` | DATETIME | | NULL | 发送时间 |
| `create_time` | DATETIME | | CURRENT_TIMESTAMP | NOT NULL | 创建时间 |

### 2.8 智能识别表

#### `t_voice_recognition_log` (语音识别日志表)
| 字段名 | 类型 | 长度 | 默认值 | 是否为空 | 备注 |
|--------|------|------|--------|----------|------|
| `id` | BIGINT | 20 | | NOT NULL | 主键，识别ID |
| `user_id` | BIGINT | 20 | | NOT NULL | 用户ID |
| `audio_file_id` | BIGINT | 20 | | NULL | 音频文件ID |
| `recognized_text` | TEXT | | | NULL | 识别出的文本 |
| `parsed_amount` | DECIMAL | 18,2 | | NULL | 解析出的金额 |
| `parsed_category` | VARCHAR | 64 | | NULL | 解析出的分类 |
| `parsed_type` | TINYINT | 1 | | NULL | 解析出的类型(1收入2支出) |
| `confidence` | DECIMAL | 5,2 | | NULL | 识别置信度 |
| `recognition_status` | TINYINT | 1 | 1 | NOT NULL | 识别状态(0失败1成功) |
| `create_time` | DATETIME | | CURRENT_TIMESTAMP | NOT NULL | 创建时间 |

#### `t_ocr_recognition_log` (OCR识别日志表)
| 字段名 | 类型 | 长度 | 默认值 | 是否为空 | 备注 |
|--------|------|------|--------|----------|------|
| `id` | BIGINT | 20 | | NOT NULL | 主键，识别ID |
| `user_id` | BIGINT | 20 | | NOT NULL | 用户ID |
| `image_file_id` | BIGINT | 20 | | NULL | 图片文件ID |
| `recognized_text` | TEXT | | | NULL | 识别出的文本 |
| `parsed_amount` | DECIMAL | 18,2 | | NULL | 解析出的金额 |
| `parsed_merchant` | VARCHAR | 128 | | NULL | 解析出的商家 |
| `parsed_items` | JSON | | | NULL | 解析出的商品明细 |
| `confidence` | DECIMAL | 5,2 | | NULL | 识别置信度 |
| `recognition_status` | TINYINT | 1 | 1 | NOT NULL | 识别状态(0失败1成功) |
| `create_time` | DATETIME | | CURRENT_TIMESTAMP | NOT NULL | 创建时间 |

## 3. 索引策略

### 3.1 基础索引
- 所有外键字段均建立索引
- 所有查询频繁的字段建立索引
- 复合索引用于多条件查询优化

### 3.2 核心表索引

#### 用户表索引
```sql
-- 用户名唯一索引
ALTER TABLE t_user ADD UNIQUE KEY uk_username (username);

-- 用户角色索引
ALTER TABLE t_user ADD KEY idx_role_id (role_id);

-- 用户公司索引
ALTER TABLE t_user ADD KEY idx_company_id (company_id);

-- 用户状态索引
ALTER TABLE t_user ADD KEY idx_status (status);
```

#### 交易记录表索引
```sql
-- 用户交易时间复合索引
ALTER TABLE t_transaction ADD KEY idx_user_transaction_time (user_id, transaction_time);

-- 公司交易时间复合索引
ALTER TABLE t_transaction ADD KEY idx_company_transaction_time (company_id, transaction_time);

-- 分类索引
ALTER TABLE t_transaction ADD KEY idx_category_id (category_id);

-- 账户索引
ALTER TABLE t_transaction ADD KEY idx_account_id (account_id);

-- 业务关联索引
ALTER TABLE t_transaction ADD KEY idx_related_business_id (related_business_id);
```

#### 操作日志表索引
```sql
-- 用户操作时间索引
ALTER TABLE t_operation_log ADD KEY idx_user_operation_time (user_id, operation_time);

-- 操作类型索引
ALTER TABLE t_operation_log ADD KEY idx_operation_type (operation_type);

-- IP地址索引
ALTER TABLE t_operation_log ADD KEY idx_ip_address (ip_address);
```

#### 预算表索引
```sql
-- 用户预算索引
ALTER TABLE t_budget ADD KEY idx_user_budget (user_id, budget_year, budget_month);

-- 公司预算索引
ALTER TABLE t_budget ADD KEY idx_company_budget (company_id, budget_year, budget_month);

-- 分类预算索引
ALTER TABLE t_budget ADD KEY idx_category_budget (category_id, budget_year);
```

### 3.3 性能优化索引
```sql
-- 设备推送索引
ALTER TABLE t_user_device ADD UNIQUE KEY uk_user_device_token (user_id, device_token);

-- 离线数据同步索引
ALTER TABLE t_offline_transaction ADD KEY idx_sync_status_user (sync_status, user_id);

-- 通知读取索引
ALTER TABLE t_notification_record ADD KEY idx_user_read_status (user_id, is_read, create_time);
```

## 4. 多租户实现

### 4.1 数据隔离策略
- **用户级隔离**：个人用户数据通过 `user_id` 隔离
- **公司级隔离**：企业用户数据通过 `company_id` 隔离
- **混合隔离**：某些表同时支持用户和公司隔离

### 4.2 租户上下文管理
- 后端通过 `ThreadLocal` 存储当前请求的 `user_id` 和 `company_id`
- MyBatis Plus 拦截器自动在 SQL 查询中添加租户条件
- JWT Token 中携带租户信息

### 4.3 隔离实现示例
```sql
-- 个人用户查询
SELECT * FROM t_transaction
WHERE user_id = #{currentUserId}
  AND company_id IS NULL

-- 企业用户查询
SELECT * FROM t_transaction
WHERE company_id = #{currentCompanyId}

-- 管理员查询所有
SELECT * FROM t_transaction
-- 无租户限制条件
```

## 5. 数据初始化

### 5.1 基础数据
```sql
-- 插入基础角色
INSERT INTO t_role (role_name, role_code, description) VALUES
('超级管理员', 'SUPER_ADMIN', '系统超级管理员，拥有所有权限'),
('管理员', 'ADMIN', '公司管理员，管理公司事务'),
('财务', 'FINANCE', '财务人员，负责记账和报表'),
('普通用户', 'USER', '普通用户，基本记账功能');

-- 插入基础权限
INSERT INTO t_permission (permission_name, permission_code) VALUES
('用户管理', 'USER_MANAGE'),
('角色管理', 'ROLE_MANAGE'),
('公司管理', 'COMPANY_MANAGE'),
('记账录入', 'TRANSACTION_ADD'),
('记账查看', 'TRANSACTION_VIEW'),
('记账编辑', 'TRANSACTION_EDIT'),
('记账删除', 'TRANSACTION_DELETE'),
('账户管理', 'ACCOUNT_MANAGE'),
('分类管理', 'CATEGORY_MANAGE'),
('报表查看', 'REPORT_VIEW'),
('预算管理', 'BUDGET_MANAGE'),
('成本中心管理', 'COST_CENTER_MANAGE'),
('系统配置', 'SYSTEM_CONFIG'),
('操作日志查看', 'LOG_VIEW');
```

### 5.2 系统配置初始化
```sql
-- 插入系统配置
INSERT INTO t_system_config (config_key, config_value, config_type, description, is_system) VALUES
('system.site_name', 'G-Zang财务管理系统', 'system', '系统名称', 1),
('system.max_login_attempts', '5', 'security', '最大登录尝试次数', 1),
('system.session_timeout', '7200', 'security', '会话超时时间(秒)', 1),
('file.upload.max_size', '10MB', 'file', '文件上传最大大小', 1),
('file.upload.allowed_types', 'jpg,jpeg,png,pdf,doc,docx,xlsx', 'file', '允许上传的文件类型', 1);
```

### 5.3 预设分类数据
```sql
-- 支出分类
INSERT INTO t_category (category_name, parent_id, type, is_system) VALUES
('餐饮', 0, 2, 1),
('交通', 0, 2, 1),
('购物', 0, 2, 1),
('娱乐', 0, 2, 1),
('医疗', 0, 2, 1),
('教育', 0, 2, 1),
('居家', 0, 2, 1),
('通讯', 0, 2, 1),
('其他支出', 0, 2, 1);

-- 收入分类
INSERT INTO t_category (category_name, parent_id, type, is_system) VALUES
('工资', 0, 1, 1),
('奖金', 0, 1, 1),
('投资', 0, 1, 1),
('兼职', 0, 1, 1),
('其他收入', 0, 1, 1);
```

## 6. 数据迁移与升级

### 6.1 版本管理
- 数据库版本通过 flyway 或 liquibase 管理
- 每个版本的变更脚本独立存放
- 支持回滚操作

### 6.2 数据迁移策略
- 新增表结构时使用 `IF NOT EXISTS`
- 新增字段使用 `ADD COLUMN IF NOT EXISTS`
- 数据迁移脚本支持幂等执行
- 备份数据后执行迁移操作

### 6.3 升级检查
```sql
-- 检查表是否存在
SELECT COUNT(*) FROM information_schema.TABLES
WHERE TABLE_SCHEMA = DATABASE() AND TABLE_NAME = 'table_name';

-- 检查字段是否存在
SELECT COUNT(*) FROM information_schema.COLUMNS
WHERE TABLE_SCHEMA = DATABASE() AND TABLE_NAME = 'table_name' AND COLUMN_NAME = 'column_name';
```
