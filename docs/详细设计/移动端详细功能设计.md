# G-Zang 移动端平台详细功能设计

## 1. 平台概述

### 1.1 定位与目标
移动端是G-Zang的核心记账入口，专注于提供便捷的移动记账体验。采用Vant UI组件库，适配iOS和Android设备。

### 1.2 核心功能
- **快速记账**：一键记账，语音记账，拍照识别
- **智能分析**：消费分析，预算提醒，趋势预测
- **移动优化**：离线记账，数据同步，触手可及

### 1.3 技术栈
- **框架**：Vue 3 + TypeScript
- **UI库**：Vant 4.x
- **路由**：Vue Router 4
- **状态管理**：Pinia
- **网络**：Axios + 离线缓存
- **原生能力**：相机、语音、地理位置

---

## 2. 应用架构

### 2.1 项目结构
```
mobile/
├── src/
│   ├── views/           # 页面组件
│   ├── components/      # 通用组件
│   ├── stores/          # 状态管理
│   ├── router/          # 路由配置
│   ├── utils/           # 工具函数
│   ├── api/             # API接口
│   ├── assets/          # 静态资源
│   └── composables/     # 组合式函数
├── public/
├── index.html
├── vite.config.ts
└── package.json
```

### 2.2 导航结构
移动端采用底部Tab导航，共有5个主要标签页：

```javascript
const tabs = [
  { name: 'home', title: '首页', icon: 'home-o', path: '/' },
  { name: 'accounting', title: '记账', icon: 'edit', path: '/accounting' },
  { name: 'bills', title: '账单', icon: 'records', path: '/bills' },
  { name: 'analysis', title: '统计', icon: 'bar-chart-o', path: '/analysis' },
  { name: 'profile', title: '我的', icon: 'user-o', path: '/profile' }
]
```

---

## 3. 首页 (Home)

### 3.1 页面概述
首页是应用的入口页面，提供资产概览、快捷操作和最近记录展示。

### 3.2 页面布局
```
┌─────────────────────────────────┐
│            顶部状态栏              │
├─────────────────────────────────┤
│  ┌─────────────────────────────┐ │
│  │        资产概览卡片           │ │
│  └─────────────────────────────┘ │
├─────────────────────────────────┤
│  ┌─────────────────────────────┐ │
│  │        快捷操作按钮           │ │
│  └─────────────────────────────┘ │
├─────────────────────────────────┤
│  ┌─────────────────────────────┐ │
│  │       今日收支概览           │ │
│  └─────────────────────────────┘ │
├─────────────────────────────────┤
│  ┌─────────────────────────────┐ │
│  │       最近交易记录           │ │
│  └─────────────────────────────┘ │
├─────────────────────────────────┤
│          底部Tab导航              │
└─────────────────────────────────┘
```

### 3.3 资产概览卡片
**组件设计：**
```vue
<template>
  <van-card class="asset-card" @click="goToAccounts">
    <template #header>
      <div class="asset-header">
        <span class="asset-title">总资产</span>
        <van-icon name="arrow" />
      </div>
    </template>

    <div class="asset-amount">
      ¥{{ totalAssets.toFixed(2) }}
    </div>

    <div class="asset-change">
      <span class="change-label">较昨日</span>
      <span :class="['change-value', changeAmount >= 0 ? 'positive' : 'negative']">
        {{ changeAmount >= 0 ? '+' : '' }}{{ changeAmount.toFixed(2) }}
      </span>
    </div>
  </van-card>
</template>

<style scoped>
.asset-card {
  margin: 16px;
  border-radius: 12px;
}

.asset-amount {
  font-size: 28px;
  font-weight: bold;
  color: #1989fa;
  margin: 12px 0;
}

.change-value.positive {
  color: #07c160;
}

.change-value.negative {
  color: #ff976a;
}
</style>
```

**功能特性：**
- **资产总览**：显示所有账户的总余额
- **变化趋势**：显示与前一天的对比
- **点击跳转**：跳转到账户详情页面
- **实时更新**：记账后自动刷新

### 3.4 快捷操作按钮
**设计布局：**
```vue
<template>
  <van-grid :column-num="4" class="quick-actions">
    <van-grid-item
      v-for="action in actions"
      :key="action.key"
      :icon="action.icon"
      :text="action.text"
      @click="handleAction(action.key)"
    />
  </van-grid>
</template>

<script setup>
const actions = [
  { key: 'income', icon: 'plus', text: '记收入' },
  { key: 'expense', icon: 'minus', text: '记支出' },
  { key: 'transfer', icon: 'exchange', text: '转账' },
  { key: 'scan', icon: 'scan', text: '扫码记账' },
  { key: 'voice', icon: 'volume-o', text: '语音记账' },
  { key: 'photo', icon: 'photograph', text: '拍照识别' },
  { key: 'template', icon: 'orders-o', text: '使用模板' },
  { key: 'more', icon: 'more-o', text: '更多' }
]

const handleAction = (key) => {
  switch (key) {
    case 'income':
      router.push('/accounting?type=income')
      break
    case 'expense':
      router.push('/accounting?type=expense')
      break
    case 'voice':
      startVoiceRecording()
      break
    case 'photo':
      openCamera()
      break
    // ... 其他操作
  }
}
</script>
```

### 3.5 今日收支概览
**组件设计：**
```vue
<template>
  <van-card class="daily-summary">
    <template #title>
      <span>今日收支</span>
    </template>

    <div class="summary-content">
      <div class="summary-item">
        <div class="amount income-amount">
          +¥{{ todayIncome.toFixed(2) }}
        </div>
        <div class="label">收入</div>
      </div>

      <van-divider direction="vertical" />

      <div class="summary-item">
        <div class="amount expense-amount">
          -¥{{ todayExpense.toFixed(2) }}
        </div>
        <div class="label">支出</div>
      </div>

      <van-divider direction="vertical" />

      <div class="summary-item">
        <div class="amount balance-amount">
          ¥{{ todayBalance.toFixed(2) }}
        </div>
        <div class="label">结余</div>
      </div>
    </div>
  </van-card>
</template>

<style scoped>
.summary-content {
  display: flex;
  justify-content: space-around;
  align-items: center;
  padding: 16px 0;
}

.summary-item {
  text-align: center;
  flex: 1;
}

.amount {
  font-size: 18px;
  font-weight: bold;
  margin-bottom: 4px;
}

.income-amount {
  color: #07c160;
}

.expense-amount {
  color: #ff976a;
}

.balance-amount {
  color: #1989fa;
}
</style>
```

### 3.6 最近交易记录
**组件设计：**
```vue
<template>
  <van-card class="recent-transactions">
    <template #title>
      <div class="card-header">
        <span>最近记录</span>
        <van-button size="mini" type="primary" plain @click="goToBills">
          查看全部
        </van-button>
      </div>
    </template>

    <van-cell-group>
      <van-cell
        v-for="transaction in recentTransactions"
        :key="transaction.id"
        :title="transaction.categoryName"
        :label="formatTime(transaction.transactionTime)"
        :value="formatAmount(transaction)"
        clickable
        @click="goToDetail(transaction)"
      >
        <template #icon>
          <van-icon :name="getCategoryIcon(transaction.categoryId)" />
        </template>
      </van-cell>
    </van-cell-group>
  </van-card>
</template>

<script setup>
const formatAmount = (transaction) => {
  const prefix = transaction.type === 1 ? '+' : '-'
  const color = transaction.type === 1 ? '#07c160' : '#ff976a'
  return `<span style="color: ${color}">${prefix}¥${transaction.amount.toFixed(2)}</span>`
}

const getCategoryIcon = (categoryId) => {
  // 根据分类ID返回对应图标
  const iconMap = {
    101: 'restaurant-o', // 餐饮
    102: 'location-o',   // 交通
    103: 'shop-o'        // 购物
  }
  return iconMap[categoryId] || 'circle'
}
</script>
```

### 3.7 后端API
```javascript
// 首页数据接口
GET /api/v1/mobile/dashboard

Response:
{
  "code": 0,
  "data": {
    "totalAssets": 12580.50,
    "changeAmount": 250.00,
    "todayIncome": 500.00,
    "todayExpense": 120.50,
    "todayBalance": 379.50,
    "recentTransactions": [
      {
        "id": 12345,
        "categoryName": "餐饮",
        "transactionTime": "2024-01-15 12:30:00",
        "amount": 25.50,
        "type": 2
      }
    ]
  }
}
```

---

## 4. 记账页面 (Accounting)

### 4.1 页面概述
记账页面提供多种记账方式，支持快速输入、智能识别等功能。

### 4.2 记账表单设计
**组件设计：**
```vue
<template>
  <van-form @submit="onSubmit">
    <!-- 记账类型切换 -->
    <van-radio-group v-model="form.type" direction="horizontal" class="type-selector">
      <van-radio name="1">收入</van-radio>
      <van-radio name="2">支出</van-radio>
      <van-radio name="3">转账</van-radio>
    </van-radio-group>

    <!-- 金额输入 -->
    <van-field
      v-model="form.amount"
      type="digit"
      label="金额"
      placeholder="请输入金额"
      :rules="[{ required: true, message: '请输入金额' }]"
      class="amount-field"
    >
      <template #left-icon>
        <span class="currency">¥</span>
      </template>
    </van-field>

    <!-- 分类选择 -->
    <van-field
      readonly
      clickable
      v-model="selectedCategory"
      label="分类"
      placeholder="请选择分类"
      @click="showCategoryPicker = true"
      :rules="[{ required: true, message: '请选择分类' }]"
    />

    <!-- 账户选择 -->
    <van-field
      readonly
      clickable
      v-model="selectedAccount"
      label="账户"
      placeholder="请选择账户"
      @click="showAccountPicker = true"
      :rules="[{ required: true, message: '请选择账户' }]"
    />

    <!-- 交易时间 -->
    <van-field
      readonly
      clickable
      v-model="form.transactionTime"
      label="时间"
      placeholder="选择时间"
      @click="showTimePicker = true"
    />

    <!-- 备注 -->
    <van-field
      v-model="form.remark"
      type="textarea"
      label="备注"
      placeholder="添加备注（可选）"
      :maxlength="200"
      show-word-limit
    />

    <!-- 常用金额快捷选择 -->
    <div class="quick-amounts">
      <van-button
        v-for="amount in quickAmounts"
        :key="amount"
        size="small"
        type="primary"
        plain
        @click="setAmount(amount)"
        class="quick-amount-btn"
      >
        ¥{{ amount }}
      </van-button>
    </div>

    <!-- 提交按钮 -->
    <div class="submit-btn">
      <van-button
        round
        block
        type="primary"
        native-type="submit"
        :loading="submitting"
        loading-text="提交中..."
      >
        确认记账
      </van-button>
    </div>
  </van-form>

  <!-- 分类选择弹窗 -->
  <van-popup v-model:show="showCategoryPicker" position="bottom">
    <van-picker
      :columns="categoryColumns"
      @confirm="onCategoryConfirm"
      @cancel="showCategoryPicker = false"
    />
  </van-popup>

  <!-- 账户选择弹窗 -->
  <van-popup v-model:show="showAccountPicker" position="bottom">
    <van-picker
      :columns="accountColumns"
      @confirm="onAccountConfirm"
      @cancel="showAccountPicker = false"
    />
  </van-popup>

  <!-- 时间选择弹窗 -->
  <van-popup v-model:show="showTimePicker" position="bottom">
    <van-datetime-picker
      v-model="currentTime"
      type="datetime"
      @confirm="onTimeConfirm"
      @cancel="showTimePicker = false"
    />
  </van-popup>
</template>
```

### 4.3 语音记账功能
**核心逻辑：**
```javascript
// composables/useVoiceRecording.js
import { ref, onMounted, onUnmounted } from 'vue'

export function useVoiceRecording() {
  const isRecording = ref(false)
  const mediaRecorder = ref(null)
  const audioChunks = ref([])

  const startRecording = async () => {
    try {
      const stream = await navigator.mediaDevices.getUserMedia({ audio: true })
      mediaRecorder.value = new MediaRecorder(stream)
      audioChunks.value = []

      mediaRecorder.value.ondataavailable = (event) => {
        audioChunks.value.push(event.data)
      }

      mediaRecorder.value.onstop = async () => {
        const audioBlob = new Blob(audioChunks.value, { type: 'audio/wav' })
        await uploadAudio(audioBlob)
      }

      mediaRecorder.value.start()
      isRecording.value = true
    } catch (error) {
      console.error('录音失败:', error)
    }
  }

  const stopRecording = () => {
    if (mediaRecorder.value && isRecording.value) {
      mediaRecorder.value.stop()
      isRecording.value = false
    }
  }

  const uploadAudio = async (audioBlob) => {
    const formData = new FormData()
    formData.append('audio', audioBlob)

    const response = await api.voiceRecognition(formData)
    // 处理识别结果
    processRecognitionResult(response.data)
  }

  return {
    isRecording,
    startRecording,
    stopRecording
  }
}
```

**语音识别流程：**
1. 用户点击语音记账按钮
2. 申请麦克风权限
3. 开始录音并显示录音动画
4. 用户说完后自动停止录音
5. 上传音频文件到后端
6. 后端进行语音识别
7. 返回识别结果并自动填充表单

### 4.4 拍照识别功能
**相机组件：**
```vue
<template>
  <div class="camera-container">
    <video ref="video" class="camera-preview" autoplay playsinline></video>
    <canvas ref="canvas" class="hidden"></canvas>

    <div class="camera-controls">
      <van-button
        type="primary"
        size="large"
        round
        @click="takePhoto"
        :loading="processing"
      >
        拍照识别
      </van-button>
    </div>
  </div>
</template>

<script setup>
import { ref, onMounted } from 'vue'

const video = ref(null)
const canvas = ref(null)
const processing = ref(false)

onMounted(async () => {
  try {
    const stream = await navigator.mediaDevices.getUserMedia({
      video: { facingMode: 'environment' }
    })
    video.value.srcObject = stream
  } catch (error) {
    console.error('相机初始化失败:', error)
  }
})

const takePhoto = async () => {
  processing.value = true

  const context = canvas.value.getContext('2d')
  canvas.value.width = video.value.videoWidth
  canvas.value.height = video.value.videoHeight
  context.drawImage(video.value, 0, 0)

  canvas.value.toBlob(async (blob) => {
    const formData = new FormData()
    formData.append('image', blob)

    try {
      const response = await api.ocrRecognition(formData)
      processOcrResult(response.data)
    } finally {
      processing.value = false
    }
  }, 'image/jpeg', 0.8)
}

const processOcrResult = (result) => {
  // 解析OCR结果，提取金额、商品信息等
  const extractedData = {
    amount: result.amount,
    merchant: result.merchant,
    category: guessCategory(result.merchant)
  }

  // 跳转到确认页面
  router.push({
    path: '/accounting/confirm',
    query: { ...extractedData, ocrData: JSON.stringify(result) }
  })
}
</script>
```

**OCR识别流程：**
1. 打开相机预览
2. 用户对准小票拍照
3. 压缩图片并上传
4. 后端OCR识别文字
5. 智能解析金额和商品信息
6. 跳转到确认页面

### 4.5 记账模板功能
**模板管理：**
```javascript
// 记账模板数据结构
const templates = [
  {
    id: 1,
    name: '早餐',
    categoryId: 101, // 餐饮
    accountId: 201,  // 微信
    amount: 15,
    remark: '早餐'
  },
  {
    id: 2,
    name: '地铁',
    categoryId: 102, // 交通
    accountId: 202,  // 交通卡
    amount: 3,
    remark: '地铁费'
  }
]
```

**模板使用：**
```vue
<template>
  <van-cell-group title="常用模板">
    <van-cell
      v-for="template in templates"
      :key="template.id"
      :title="template.name"
      :label="`¥${template.amount} - ${template.categoryName}`"
      clickable
      @click="useTemplate(template)"
    >
      <template #icon>
        <van-icon name="orders-o" />
      </template>
    </van-cell>
  </van-cell-group>
</template>
```

---

## 5. 账单页面 (Bills)

### 5.1 账单列表
**页面设计：**
```vue
<template>
  <div class="bills-page">
    <!-- 筛选器 -->
    <van-sticky>
      <van-dropdown-menu>
        <van-dropdown-item
          v-model="filter.type"
          :options="typeOptions"
          @change="onFilterChange"
        />
        <van-dropdown-item
          v-model="filter.categoryId"
          :options="categoryOptions"
          @change="onFilterChange"
        />
        <van-dropdown-item
          v-model="filter.dateRange"
          :options="dateOptions"
          @change="onFilterChange"
        />
      </van-dropdown-menu>
    </van-sticky>

    <!-- 账单列表 -->
    <van-list
      v-model:loading="loading"
      :finished="finished"
      finished-text="没有更多了"
      @load="onLoad"
    >
      <van-cell-group>
        <van-cell
          v-for="bill in bills"
          :key="bill.id"
          :title="bill.categoryName"
          :label="formatTime(bill.transactionTime)"
          :value="formatAmount(bill)"
          clickable
          @click="goToDetail(bill)"
        >
          <template #icon>
            <van-icon :name="getCategoryIcon(bill.categoryId)" />
          </template>
        </van-cell>
      </van-cell-group>
    </van-list>

    <!-- 底部操作栏 -->
    <van-action-bar>
      <van-action-bar-icon icon="delete" text="删除" @click="showDeleteDialog" />
      <van-action-bar-icon icon="edit" text="编辑" @click="showBatchEdit" />
      <van-action-bar-button type="primary" @click="goToAccounting">
        记一笔
      </van-action-bar-button>
    </van-action-bar>
  </div>
</template>
```

### 5.2 账单详情页面
**详情展示：**
```vue
<template>
  <div class="bill-detail">
    <van-nav-bar title="账单详情" left-text="返回" left-arrow @click-left="goBack" />

    <van-cell-group>
      <van-cell title="交易类型" :value="typeText" />
      <van-cell title="分类" :value="bill.categoryName" />
      <van-cell title="账户" :value="bill.accountName" />
      <van-cell title="金额" :value="formatAmount(bill)" />
      <van-cell title="时间" :value="formatTime(bill.transactionTime)" />
      <van-cell title="备注" :value="bill.remark || '无'" />
    </van-cell-group>

    <div class="action-buttons">
      <van-button block type="primary" @click="editBill">
        编辑
      </van-button>
      <van-button block type="danger" @click="deleteBill">
        删除
      </van-button>
    </div>
  </div>
</template>
```

---

## 6. 统计页面 (Analysis)

### 6.1 统计概览
**图表展示：**
```vue
<template>
  <div class="analysis-page">
    <!-- 时间选择 -->
    <van-tabs v-model:active="activeTab" @change="onTabChange">
      <van-tab title="周" name="week" />
      <van-tab title="月" name="month" />
      <van-tab title="年" name="year" />
    </van-tabs>

    <!-- 统计卡片 -->
    <van-card class="stat-card">
      <van-row>
        <van-col span="12">
          <div class="stat-item">
            <div class="value income">¥{{ stats.income }}</div>
            <div class="label">收入</div>
          </div>
        </van-col>
        <van-col span="12">
          <div class="stat-item">
            <div class="value expense">¥{{ stats.expense }}</div>
            <div class="label">支出</div>
          </div>
        </van-col>
      </van-row>
    </van-card>

    <!-- 图表区域 -->
    <van-card title="支出分类" class="chart-card">
      <div ref="pieChart" class="chart-container"></div>
    </van-card>

    <van-card title="收支趋势" class="chart-card">
      <div ref="lineChart" class="chart-container"></div>
    </van-card>
  </div>
</template>

<script setup>
import { ref, onMounted } from 'vue'
import * as echarts from 'echarts'

const pieChart = ref(null)
const lineChart = ref(null)

onMounted(() => {
  initCharts()
})

const initCharts = () => {
  // 饼图 - 支出分类
  const pieInstance = echarts.init(pieChart.value)
  pieInstance.setOption({
    series: [{
      type: 'pie',
      data: [
        { name: '餐饮', value: 1200 },
        { name: '交通', value: 800 },
        { name: '购物', value: 600 }
      ]
    }]
  })

  // 折线图 - 收支趋势
  const lineInstance = echarts.init(lineChart.value)
  lineInstance.setOption({
    xAxis: { type: 'category', data: ['1月', '2月', '3月', '4月'] },
    yAxis: { type: 'value' },
    series: [
      { name: '收入', type: 'line', data: [3000, 3500, 3200, 3800] },
      { name: '支出', type: 'line', data: [2500, 2800, 2600, 3000] }
    ]
  })
}
</script>
```

---

## 7. 我的页面 (Profile)

### 7.1 个人中心
**页面布局：**
```vue
<template>
  <div class="profile-page">
    <!-- 用户信息卡片 -->
    <van-card class="user-card">
      <template #header>
        <van-image
          round
          width="60px"
          height="60px"
          :src="user.avatar"
          fit="cover"
        />
        <div class="user-info">
          <div class="nickname">{{ user.nickname }}</div>
          <div class="username">{{ user.username }}</div>
        </div>
      </template>
    </van-card>

    <!-- 功能列表 -->
    <van-cell-group>
      <van-cell title="个人资料" is-link @click="goToProfile" />
      <van-cell title="账户管理" is-link @click="goToAccounts" />
      <van-cell title="分类设置" is-link @click="goToCategories" />
      <van-cell title="预算设置" is-link @click="goToBudgets" />
      <van-cell title="数据导出" is-link @click="exportData" />
      <van-cell title="设置" is-link @click="goToSettings" />
    </van-cell-group>

    <!-- 退出登录 -->
    <div class="logout-btn">
      <van-button block type="danger" @click="logout">
        退出登录
      </van-button>
    </div>
  </div>
</template>
```

### 7.2 设置页面
**设置项：**
```vue
<template>
  <van-cell-group title="通用设置">
    <van-cell title="主题模式" is-link @click="showThemePicker">
      <template #right-icon>
        <van-icon name="arrow" />
      </template>
    </van-cell>

    <van-cell title="语言设置" is-link @click="showLanguagePicker">
      <template #right-icon>
        <van-icon name="arrow" />
      </template>
    </van-cell>

    <van-cell title="货币单位" is-link @click="showCurrencyPicker">
      <template #right-icon>
        <van-icon name="arrow" />
      </template>
    </van-cell>
  </van-cell-group>

  <van-cell-group title="记账设置">
    <van-switch-cell title="默认分类记忆" v-model="settings.rememberCategory" />
    <van-switch-cell title="金额快速输入" v-model="settings.quickAmount" />
    <van-switch-cell title="语音记账" v-model="settings.voiceAccounting" />
  </van-cell-group>

  <van-cell-group title="通知设置">
    <van-switch-cell title="预算提醒" v-model="settings.budgetReminder" />
    <van-switch-cell title="定期报表" v-model="settings.regularReport" />
    <van-switch-cell title="记账提醒" v-model="settings.accountingReminder" />
  </van-cell-group>
</template>
```

---

## 8. 移动端特殊功能

### 8.1 离线记账
**离线存储：**
```javascript
// utils/offlineStorage.js
class OfflineStorage {
  constructor() {
    this.dbName = 'gzang-offline'
    this.version = 1
  }

  async saveTransaction(transaction) {
    const db = await this.openDB()
    const tx = db.transaction('transactions', 'readwrite')
    const store = tx.objectStore('transactions')
    await store.add({
      ...transaction,
      id: Date.now(), // 临时ID
      synced: false,
      createdAt: new Date()
    })
  }

  async syncTransactions() {
    const db = await this.openDB()
    const tx = db.transaction('transactions', 'readonly')
    const store = tx.objectStore('transactions')
    const unsynced = await store.index('synced').getAll(false)

    for (const transaction of unsynced) {
      try {
        await api.createTransaction(transaction)
        // 标记为已同步
        await this.markSynced(transaction.id)
      } catch (error) {
        console.error('同步失败:', error)
      }
    }
  }
}
```

### 8.2 手势操作
**滑动操作：**
```javascript
// composables/useSwipeActions.js
import { ref } from 'vue'

export function useSwipeActions() {
  const swipeActions = ref({
    left: [
      { name: 'delete', color: '#ff4444', text: '删除' }
    ],
    right: [
      { name: 'edit', color: '#1989fa', text: '编辑' }
    ]
  })

  const handleSwipeAction = (action, item) => {
    switch (action) {
      case 'delete':
        deleteItem(item)
        break
      case 'edit':
        editItem(item)
        break
    }
  }

  return {
    swipeActions,
    handleSwipeAction
  }
}
```

### 8.3 推送通知
**通知权限和处理：**
```javascript
// utils/notification.js
class NotificationManager {
  async requestPermission() {
    if ('Notification' in window) {
      const permission = await Notification.requestPermission()
      return permission === 'granted'
    }
    return false
  }

  async showBudgetAlert(message) {
    if (await this.requestPermission()) {
      new Notification('预算提醒', {
        body: message,
        icon: '/icon.png',
        tag: 'budget-alert'
      })
    }
  }

  async showAccountingReminder() {
    if (await this.requestPermission()) {
      new Notification('记账提醒', {
        body: '该记账了！',
        icon: '/icon.png',
        tag: 'accounting-reminder'
      })
    }
  }
}
```

---

## 9. 性能优化

### 9.1 首屏加载优化
- **代码分割**：按路由分割代码
- **资源压缩**：图片压缩，代码混淆
- **缓存策略**：Service Worker缓存静态资源

### 9.2 运行时性能
- **虚拟滚动**：长列表虚拟化
- **图片懒加载**：非可视区域图片延迟加载
- **防抖节流**：搜索和滚动事件优化

### 9.3 网络优化
- **离线缓存**：关键数据本地存储
- **增量同步**：只同步变更数据
- **网络监听**：网络状态变化处理

---

## 10. 用户体验设计

### 10.1 交互设计
- **触感反馈**：按钮点击震动反馈
- **加载状态**：优雅的加载动画
- **错误处理**：友好的错误提示
- **引导提示**：新功能引导

### 10.2 可访问性
- **大字体支持**：系统字体大小适配
- **高对比度**：深色模式支持
- **语音辅助**：重要操作语音提示

### 10.3 个性化
- **主题切换**：亮色/暗色主题
- **语言设置**：多语言支持
- **偏好记忆**：用户习惯保存

---

**文档版本**：1.0.0
**最后更新**：2026-01-14
**维护人员**：G-Zang开发团队
