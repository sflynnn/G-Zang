# G-Zang (归藏) 前端开发规范

> **文档版本**：1.0.0
> **最后更新**：2026-03-27
> **维护人员**：前端开发工程师
> **关联规则**：`pc-main.mdc`、`pc-personal.mdc`、`pc-business.mdc`、`mobile.mdc`、`admin.mdc`

## 1. 项目结构规范

### 1.1 Monorepo结构

```
G-Zang/
├── apps/                          # 应用目录
│   ├── admin/                     # 管理端 (Naive UI)
│   │   ├── src/
│   │   │   ├── api/              # API接口
│   │   │   ├── assets/           # 静态资源
│   │   │   │   ├── images/       # 图片资源
│   │   │   │   ├── icons/        # 图标资源
│   │   │   │   └── styles/       # 样式文件
│   │   │   ├── components/       # 组件目录
│   │   │   │   ├── common/       # 通用组件
│   │   │   │   ├── business/     # 业务组件
│   │   │   │   └── layout/       # 布局组件
│   │   │   ├── composables/      # 组合式函数
│   │   │   ├── router/           # 路由配置
│   │   │   ├── stores/           # 状态管理
│   │   │   ├── utils/            # 工具函数
│   │   │   ├── views/            # 页面组件
│   │   │   ├── types/            # TypeScript类型
│   │   │   ├── constants/        # 常量定义
│   │   │   └── locales/          # 国际化
│   │   ├── public/               # 公共资源
│   │   ├── index.html            # 入口HTML
│   │   ├── vite.config.ts        # Vite配置
│   │   └── package.json          # 项目配置
│   ├── pc-main/                  # PC主应用 (qiankun)
│   ├── pc-personal/              # 个人记账子应用
│   └── mobile/                   # 移动端应用 (Vant)
├── packages/                     # 共享包
│   └── shared/                   # 跨端共享模块
│       ├── src/
│       │   ├── api/              # API封装
│       │   ├── types/            # 全局类型
│       │   ├── utils/            # 工具函数
│       │   └── components/       # 共享组件
│       └── package.json
├── tools/                        # 构建工具
├── docs/                         # 项目文档
└── package.json                  # 根配置 (workspace)
```

### 1.2 文件命名规范

#### 1.2.1 组件文件命名
```typescript
// ✅ 正确命名
UserProfile.vue          // PascalCase + 描述性名称
UserList.vue
AccountCard.vue

// ❌ 错误命名
userProfile.vue          // camelCase (Vue组件应使用PascalCase)
user-profile.vue         // kebab-case (不推荐用于组件)
User.vue                 // 过于简单，缺乏描述性
```

#### 1.2.2 工具函数文件命名
```typescript
// ✅ 正确命名
dateUtils.ts            // camelCase + Utils后缀
formatters.ts
validators.ts

// ❌ 错误命名
DateUtils.ts            // PascalCase (工具文件应使用camelCase)
utils.ts               // 过于简单
```

#### 1.2.3 目录命名
```typescript
// ✅ 正确命名
components/             // 复数形式
utils/
types/
constants/

// ❌ 错误命名
component/              // 单数形式
util/
type/
constant/
```

## 2. Vue 3 Composition API 规范

### 2.1 组件结构

#### 2.1.1 推荐的组件结构
```vue
<template>
  <!-- 模板内容 -->
  <div class="user-profile">
    <header class="profile-header">
      <h1>{{ user.name }}</h1>
    </header>

    <main class="profile-content">
      <!-- 组件内容 -->
    </main>
  </div>
</template>

<script setup lang="ts">
// 1. 类型定义
interface Props {
  userId: number
  showActions?: boolean
}

interface Emits {
  update: [user: User]
  delete: [userId: number]
}

// 2. Props定义
const props = withDefaults(defineProps<Props>(), {
  showActions: true
})

// 3. Emits定义
const emit = defineEmits<Emits>()

// 4. 响应式数据
const user = ref<User | null>(null)
const loading = ref(false)
const error = ref<string | null>(null)

// 5. 计算属性
const displayName = computed(() => {
  return user.value?.nickname || user.value?.username || '未知用户'
})

const isAdmin = computed(() => {
  return user.value?.role === 'ADMIN'
})

// 6. 生命周期钩子
onMounted(() => {
  loadUser()
})

onUnmounted(() => {
  // 清理资源
})

// 7. 组合式函数
const { loadUser, updateUser } = useUser(props.userId)

// 8. 方法定义
const handleUpdate = async (newUserData: Partial<User>) => {
  try {
    loading.value = true
    await updateUser(newUserData)
    emit('update', { ...user.value!, ...newUserData })
    message.success('更新成功')
  } catch (err) {
    error.value = err.message
    message.error('更新失败')
  } finally {
    loading.value = false
  }
}

const handleDelete = () => {
  Modal.confirm({
    title: '确认删除',
    content: '此操作不可撤销，确定要删除吗？',
    onOk: async () => {
      try {
        await deleteUser(props.userId)
        emit('delete', props.userId)
        message.success('删除成功')
      } catch (err) {
        message.error('删除失败')
      }
    }
  })
}

// 9. 暴露组件接口 (如需要)
defineExpose({
  refresh: loadUser
})
</script>

<style scoped>
.user-profile {
  /* 组件样式 */
}

.profile-header {
  /* 头部样式 */
}

.profile-content {
  /* 内容样式 */
}
</style>
```

#### 2.1.2 组合式函数组织
```typescript
// composables/useUser.ts
import { ref, computed } from 'vue'
import { userApi } from '@/api/user'
import type { User } from '@/types/user'

export function useUser(userId: number) {
  // 状态
  const user = ref<User | null>(null)
  const loading = ref(false)

  // 计算属性
  const isLoaded = computed(() => user.value !== null)

  // 方法
  const loadUser = async () => {
    try {
      loading.value = true
      const response = await userApi.getInfo(userId)
      user.value = response.data
    } catch (error) {
      console.error('加载用户失败:', error)
      throw error
    } finally {
      loading.value = false
    }
  }

  const updateUser = async (userData: Partial<User>) => {
    const response = await userApi.update(userData)
    user.value = { ...user.value!, ...response.data }
  }

  const deleteUser = async () => {
    await userApi.delete(userId)
    user.value = null
  }

  return {
    // 状态
    user: readonly(user),
    loading: readonly(loading),
    isLoaded,

    // 方法
    loadUser,
    updateUser,
    deleteUser
  }
}
```

### 2.2 Props 和 Emits 规范

#### 2.2.1 Props定义
```typescript
// ✅ 推荐方式
interface Props {
  userId: number
  showActions?: boolean
  theme?: 'light' | 'dark'
}

const props = withDefaults(defineProps<Props>(), {
  showActions: true,
  theme: 'light'
})

// ❌ 不推荐方式
const props = defineProps({
  userId: Number,           // 缺乏类型安全
  showActions: Boolean,     // 缺乏默认值
  theme: String             // 缺乏枚举约束
})
```

#### 2.2.2 Emits定义
```typescript
// ✅ 推荐方式
interface Emits {
  update: [user: User]
  delete: [userId: number]
  error: [error: Error]
}

const emit = defineEmits<Emits>()

// 触发事件
emit('update', updatedUser)
emit('delete', userId)
emit('error', new Error('操作失败'))

// ❌ 不推荐方式
const emit = defineEmits(['update', 'delete', 'error'])
// 缺乏类型安全和参数约束
```

### 2.3 响应式数据管理

#### 2.3.1 基本响应式数据
```typescript
// ✅ 正确使用
const count = ref(0)
const user = ref<User | null>(null)
const items = ref<Array<Item>>([])

// ❌ 错误使用
let count = 0              // 非响应式
const count = ref<number>() // 没有默认值
```

#### 2.3.2 深层响应式对象
```typescript
// ✅ 对于深层对象，使用 reactive
const form = reactive({
  username: '',
  password: '',
  profile: {
    avatar: '',
    bio: ''
  }
})

// ✅ 对于数组，使用 ref
const users = ref<User[]>([])

// ❌ 避免在 reactive 中使用 ref
const state = reactive({
  count: ref(0)  // 不必要的嵌套
})
```

### 2.4 计算属性和监听器

#### 2.4.1 计算属性
```typescript
// ✅ 正确使用计算属性
const fullName = computed(() => {
  return `${user.value?.firstName || ''} ${user.value?.lastName || ''}`.trim()
})

const isValid = computed(() => {
  return username.value.length >= 3 && email.value.includes('@')
})

// ✅ 带 getter/setter 的计算属性
const fullName = computed({
  get: () => `${firstName.value} ${lastName.value}`,
  set: (newValue) => {
    const [first, last] = newValue.split(' ')
    firstName.value = first
    lastName.value = last
  }
})
```

#### 2.4.2 监听器
```typescript
// ✅ 监听单个响应式数据
watch(userId, (newId, oldId) => {
  if (newId !== oldId) {
    loadUser()
  }
}, { immediate: true })

// ✅ 监听多个响应式数据
watch([username, email], ([newUsername, newEmail]) => {
  validateForm()
})

// ✅ 深度监听对象
watch(user, (newUser) => {
  saveUserToLocalStorage(newUser)
}, { deep: true })

// ✅ 监听计算属性
watch(isValid, (valid) => {
  if (valid) {
    submitForm()
  }
})
```

## 3. TypeScript 规范

### 3.1 类型定义

#### 3.1.1 接口定义
```typescript
// ✅ 推荐的接口定义
interface User {
  readonly id: number          // 只读属性
  username: string
  nickname?: string           // 可选属性
  email: string
  role: UserRole              // 使用枚举类型
  profile: UserProfile        // 嵌套接口
  createdAt: Date
  updatedAt: Date
}

// ✅ 泛型接口
interface ApiResponse<T> {
  code: number
  message: string
  data: T
  timestamp: number
}

interface PaginatedResponse<T> extends ApiResponse<T> {
  total: number
  page: number
  size: number
}

// ✅ 联合类型和交叉类型
type UserStatus = 'active' | 'inactive' | 'suspended'
type AdminUser = User & { role: 'ADMIN' }
```

#### 3.1.2 枚举定义
```typescript
// ✅ 推荐的枚举定义
enum UserRole {
  ADMIN = 'ADMIN',
  FINANCE = 'FINANCE',
  USER = 'USER'
}

enum TransactionType {
  INCOME = 1,
  EXPENSE = 2
}

// ✅ 字符串枚举（更易调试）
enum ApiEndpoint {
  USERS = '/api/v1/users',
  TRANSACTIONS = '/api/v1/transactions',
  REPORTS = '/api/v1/reports'
}
```

#### 3.1.3 类型别名
```typescript
// ✅ 类型别名用于复杂类型
type UserForm = Omit<User, 'id' | 'createdAt' | 'updatedAt'>
type PartialUser = Partial<User>
type RequiredUser = Required<User>

// ✅ 函数类型别名
type Validator = (value: any) => boolean | string
type EventHandler<T = Event> = (event: T) => void

// ✅ 条件类型
type ArrayElement<T> = T extends (infer U)[] ? U : never
type UserId = ArrayElement<User['id']>  // number
```

### 3.2 类型安全实践

#### 3.2.1 泛型使用
```typescript
// ✅ 正确使用泛型
function createList<T>(items: T[]): List<T> {
  return new List(items)
}

const userList = createList<User>(users)
const stringList = createList<string>(strings)

// ✅ 泛型约束
function getProperty<T, K extends keyof T>(obj: T, key: K): T[K] {
  return obj[key]
}

const username = getProperty(user, 'username') // 类型安全
```

#### 3.2.2 类型断言
```typescript
// ✅ 安全类型断言
const user = response.data as User

// ✅ 双重断言（谨慎使用）
const value = '123' as any as number

// ✅ 类型守卫
function isUser(obj: any): obj is User {
  return obj && typeof obj.id === 'number' && typeof obj.username === 'string'
}

if (isUser(data)) {
  // TypeScript 知道 data 是 User 类型
  console.log(data.username)
}
```

## 4. 状态管理规范

### 4.1 Pinia Store 设计

#### 4.1.1 Store 结构
```typescript
// stores/useAuthStore.ts
import { defineStore } from 'pinia'
import { ref, computed } from 'vue'
import type { User } from '@/types/user'

export const useAuthStore = defineStore('auth', () => {
  // 状态
  const user = ref<User | null>(null)
  const token = ref<string>('')
  const refreshToken = ref<string>('')
  const loading = ref(false)

  // 计算属性
  const isAuthenticated = computed(() => !!token.value)
  const isAdmin = computed(() => user.value?.role === 'ADMIN')
  const displayName = computed(() => {
    return user.value?.nickname || user.value?.username || '未登录'
  })

  // 动作
  const login = async (credentials: LoginCredentials) => {
    try {
      loading.value = true
      const response = await authApi.login(credentials)

      user.value = response.data.user
      token.value = response.data.token
      refreshToken.value = response.data.refreshToken

      // 持久化存储
      localStorage.setItem('token', token.value)
      localStorage.setItem('refreshToken', refreshToken.value)

      return response
    } catch (error) {
      console.error('登录失败:', error)
      throw error
    } finally {
      loading.value = false
    }
  }

  const logout = () => {
    user.value = null
    token.value = ''
    refreshToken.value = ''

    localStorage.removeItem('token')
    localStorage.removeItem('refreshToken')
  }

  const refresh = async () => {
    try {
      const response = await authApi.refresh(refreshToken.value)

      token.value = response.data.token
      refreshToken.value = response.data.refreshToken

      localStorage.setItem('token', token.value)
      localStorage.setItem('refreshToken', refreshToken.value)
    } catch (error) {
      logout() // 刷新失败，清除登录状态
      throw error
    }
  }

  // 初始化（从 localStorage 恢复状态）
  const init = () => {
    const savedToken = localStorage.getItem('token')
    const savedRefreshToken = localStorage.getItem('refreshToken')

    if (savedToken && savedRefreshToken) {
      token.value = savedToken
      refreshToken.value = savedRefreshToken
      // 可以在这里验证 token 有效性
    }
  }

  // 暴露接口
  return {
    // 状态
    user: readonly(user),
    token: readonly(token),
    loading: readonly(loading),

    // 计算属性
    isAuthenticated,
    isAdmin,
    displayName,

    // 方法
    login,
    logout,
    refresh,
    init
  }
})
```

#### 4.1.2 Store 组织
```
stores/
├── modules/                      # 按功能模块组织
│   ├── auth.ts                  # 认证相关
│   ├── user.ts                  # 用户管理
│   ├── accounting.ts            # 记账业务
│   ├── company.ts               # 企业管理
│   └── report.ts                # 报表分析
├── plugins/                     # Store插件
│   ├── persist.ts               # 持久化插件
│   ├── logger.ts                # 日志插件
│   └── reset.ts                 # 重置插件
├── types/                       # Store类型定义
│   └── index.ts
└── index.ts                     # 导出配置
```

### 4.2 状态管理最佳实践

#### 4.2.1 状态设计原则
```typescript
// ✅ 状态扁平化，避免深层嵌套
const state = {
  // 好的设计
  user: { id: 1, name: 'John' },
  settings: { theme: 'light' },

  // 避免的嵌套
  app: {
    user: {
      profile: {
        personal: { name: 'John' }
      }
    }
  }
}

// ✅ 使用组合式 Store
export const useAppStore = defineStore('app', () => {
  const auth = useAuthStore()
  const user = useUserStore()

  const currentUser = computed(() => {
    if (auth.isAuthenticated) {
      return user.currentUser
    }
    return null
  })

  return {
    currentUser
  }
})
```

#### 4.2.2 异步操作处理
```typescript
// ✅ 正确处理异步操作
const action = async (params) => {
  try {
    loading.value = true
    const result = await apiCall(params)

    // 更新状态
    state.value = result.data

    return result
  } catch (error) {
    error.value = error.message
    throw error
  } finally {
    loading.value = false
  }
}

// ✅ 使用乐观更新
const optimisticUpdate = async (item) => {
  const previousState = { ...state.value }

  // 乐观更新
  state.value = { ...state.value, ...item }

  try {
    await apiCall(item)
  } catch (error) {
    // 回滚状态
    state.value = previousState
    throw error
  }
}
```

## 5. API 调用规范

### 5.1 API 封装设计

#### 5.1.1 基础 API 客户端
```typescript
// packages/shared/src/api/client.ts
import axios, { AxiosInstance, AxiosRequestConfig, AxiosResponse } from 'axios'
import type { ApiResponse } from '@/types/api'

class ApiClient {
  private instance: AxiosInstance

  constructor(baseURL: string) {
    this.instance = axios.create({
      baseURL,
      timeout: 10000,
      headers: {
        'Content-Type': 'application/json'
      }
    })

    this.setupInterceptors()
  }

  private setupInterceptors() {
    // 请求拦截器
    this.instance.interceptors.request.use(
      (config) => {
        // 添加认证头
        const token = localStorage.getItem('token')
        if (token) {
          config.headers.Authorization = `Bearer ${token}`
        }

        // 添加请求ID
        config.headers['X-Request-ID'] = generateRequestId()

        return config
      },
      (error) => Promise.reject(error)
    )

    // 响应拦截器
    this.instance.interceptors.response.use(
      (response) => {
        // 统一处理响应
        return this.handleResponse(response)
      },
      (error) => {
        // 统一处理错误
        return this.handleError(error)
      }
    )
  }

  private handleResponse(response: AxiosResponse): AxiosResponse {
    const { data } = response

    // 检查业务状态码
    if (data.code !== 0) {
      const error = new Error(data.message || '请求失败')
      ;(error as any).code = data.code
      throw error
    }

    return response
  }

  private handleError(error: any) {
    if (error.response?.status === 401) {
      // 处理未授权
      useAuthStore().logout()
      router.push('/login')
    } else if (error.response?.status >= 500) {
      // 处理服务器错误
      message.error('服务器错误，请稍后重试')
    } else {
      // 处理其他错误
      message.error(error.message || '请求失败')
    }

    throw error
  }

  // HTTP 方法
  get<T = any>(url: string, config?: AxiosRequestConfig): Promise<ApiResponse<T>> {
    return this.instance.get(url, config)
  }

  post<T = any>(url: string, data?: any, config?: AxiosRequestConfig): Promise<ApiResponse<T>> {
    return this.instance.post(url, data, config)
  }

  put<T = any>(url: string, data?: any, config?: AxiosRequestConfig): Promise<ApiResponse<T>> {
    return this.instance.put(url, data, config)
  }

  delete<T = any>(url: string, config?: AxiosRequestConfig): Promise<ApiResponse<T>> {
    return this.instance.delete(url, config)
  }

  patch<T = any>(url: string, data?: any, config?: AxiosRequestConfig): Promise<ApiResponse<T>> {
    return this.instance.patch(url, data, config)
  }
}

// 导出单例
export const apiClient = new ApiClient(import.meta.env.VITE_API_BASE_URL)
export default apiClient
```

#### 5.1.2 模块化 API 封装
```typescript
// packages/shared/src/api/modules/user.ts
import apiClient from '../client'
import type { User, CreateUserDTO, UpdateUserDTO } from '@/types/user'

export const userApi = {
  // 获取用户列表
  getUsers: (params?: {
    page?: number
    size?: number
    keyword?: string
  }) => apiClient.get<{
    total: number
    records: User[]
  }>('/users', { params }),

  // 获取用户信息
  getUser: (id: number) => apiClient.get<User>(`/users/${id}`),

  // 创建用户
  createUser: (data: CreateUserDTO) => apiClient.post<User>('/users', data),

  // 更新用户
  updateUser: (id: number, data: UpdateUserDTO) => apiClient.put<User>(`/users/${id}`, data),

  // 删除用户
  deleteUser: (id: number) => apiClient.delete(`/users/${id}`),

  // 获取当前用户信息
  getCurrentUser: () => apiClient.get<User>('/user/info'),

  // 更新当前用户信息
  updateCurrentUser: (data: UpdateUserDTO) => apiClient.put<User>('/user/profile', data),

  // 修改密码
  changePassword: (data: {
    oldPassword: string
    newPassword: string
  }) => apiClient.post('/user/change-password', data)
}

export type { User, CreateUserDTO, UpdateUserDTO }
export default userApi
```

### 5.2 API 调用最佳实践

#### 5.2.1 错误处理
```typescript
// ✅ 正确的错误处理
const handleApiCall = async () => {
  try {
    const response = await userApi.getUsers()
    users.value = response.data.records
  } catch (error) {
    if (error.code === 401) {
      message.error('请重新登录')
    } else if (error.code === 403) {
      message.error('权限不足')
    } else {
      message.error(error.message || '获取用户列表失败')
    }
  }
}

// ✅ 使用加载状态
const loadUsers = async () => {
  loading.value = true
  try {
    const response = await userApi.getUsers()
    users.value = response.data.records
  } catch (error) {
    console.error('加载用户失败:', error)
  } finally {
    loading.value = false
  }
}
```

#### 5.2.2 请求取消
```typescript
// ✅ 使用 AbortController 取消请求
const loadData = async () => {
  // 取消之前的请求
  if (abortController.value) {
    abortController.value.abort()
  }

  abortController.value = new AbortController()

  try {
    const response = await apiClient.get('/data', {
      signal: abortController.value.signal
    })
    data.value = response.data
  } catch (error) {
    if (error.name === 'AbortError') {
      console.log('请求被取消')
      return
    }
    throw error
  }
}

// ✅ 组件卸载时取消请求
onUnmounted(() => {
  if (abortController.value) {
    abortController.value.abort()
  }
})
```

## 6. 组件设计规范

### 6.1 组件分类

#### 6.1.1 展示组件 (Presentational Components)
```vue
<!-- components/UserCard.vue -->
<template>
  <div class="user-card" @click="handleClick">
    <img :src="user.avatar" :alt="user.name" class="avatar" />
    <div class="info">
      <h3>{{ user.name }}</h3>
      <p>{{ user.email }}</p>
    </div>
  </div>
</template>

<script setup lang="ts">
interface Props {
  user: User
}

defineProps<Props>()

const emit = defineEmits<{
  click: [user: User]
}>()

const handleClick = () => {
  emit('click', user)
}
</script>

<style scoped>
.user-card {
  display: flex;
  align-items: center;
  padding: 16px;
  border: 1px solid #e0e0e0;
  border-radius: 8px;
  cursor: pointer;
  transition: box-shadow 0.2s;
}

.user-card:hover {
  box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
}

.avatar {
  width: 48px;
  height: 48px;
  border-radius: 50%;
  margin-right: 12px;
}

.info h3 {
  margin: 0 0 4px 0;
  font-size: 16px;
  font-weight: 600;
}

.info p {
  margin: 0;
  color: #666;
  font-size: 14px;
}
</style>
```

#### 6.1.2 容器组件 (Container Components)
```vue
<!-- views/UserList.vue -->
<template>
  <div class="user-list">
    <div class="header">
      <h1>用户管理</h1>
      <n-button type="primary" @click="showCreateModal = true">
        新增用户
      </n-button>
    </div>

    <div class="filters">
      <n-input
        v-model:value="searchKeyword"
        placeholder="搜索用户"
        @input="handleSearch"
      />
    </div>

    <n-data-table
      :columns="columns"
      :data="users"
      :loading="loading"
      :pagination="pagination"
      @update:checked-row-keys="handleSelectChange"
    />

    <UserCreateModal
      v-model:show="showCreateModal"
      @success="handleCreateSuccess"
    />
  </div>
</template>

<script setup lang="ts">
import { ref, computed, onMounted } from 'vue'
import { useUserStore } from '@/stores/user'
import UserCreateModal from '@/components/UserCreateModal.vue'

const userStore = useUserStore()

// 响应式数据
const searchKeyword = ref('')
const showCreateModal = ref(false)
const loading = computed(() => userStore.loading)

// 计算属性
const users = computed(() => userStore.filteredUsers(searchKeyword.value))

// 表格配置
const columns = [
  {
    title: '用户名',
    key: 'username'
  },
  {
    title: '邮箱',
    key: 'email'
  },
  {
    title: '状态',
    key: 'status',
    render: (row: User) => row.status === 1 ? '正常' : '禁用'
  },
  {
    title: '操作',
    key: 'actions',
    render: (row: User) => h('div', [
      h('button', { onClick: () => editUser(row) }, '编辑'),
      h('button', { onClick: () => deleteUser(row) }, '删除')
    ])
  }
]

const pagination = computed(() => ({
  pageSize: userStore.pageSize,
  total: userStore.total,
  showSizePicker: true,
  pageSizes: [10, 20, 50, 100],
  onChange: (page: number) => {
    userStore.setPage(page)
    loadUsers()
  },
  onUpdatePageSize: (pageSize: number) => {
    userStore.setPageSize(pageSize)
    loadUsers()
  }
}))

// 方法
const loadUsers = () => {
  userStore.loadUsers()
}

const handleSearch = () => {
  userStore.setSearchKeyword(searchKeyword.value)
}

const handleCreateSuccess = () => {
  showCreateModal.value = false
  loadUsers()
}

const editUser = (user: User) => {
  // 编辑用户逻辑
}

const deleteUser = async (user: User) => {
  // 删除用户逻辑
}

onMounted(() => {
  loadUsers()
})
</script>

<style scoped>
.user-list {
  padding: 24px;
}

.header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 24px;
}

.filters {
  margin-bottom: 16px;
}
</style>
```

### 6.2 组件通信

#### 6.2.1 Props 传递数据
```vue
<!-- 父组件 -->
<template>
  <UserList
    :users="userList"
    :loading="isLoading"
    @user-select="handleUserSelect"
    @user-delete="handleUserDelete"
  />
</template>

<script setup lang="ts">
const userList = ref<User[]>([])
const isLoading = ref(false)

const handleUserSelect = (user: User) => {
  console.log('选中用户:', user)
}

const handleUserDelete = (user: User) => {
  console.log('删除用户:', user)
}
</script>
```

#### 6.2.2 Provide/Inject 依赖注入
```typescript
// 父组件提供数据
provide('theme', 'dark')
provide('userPermissions', userPermissions)

// 子组件注入数据
const theme = inject('theme', 'light')
const permissions = inject('userPermissions', [])
```

#### 6.2.3 组合式函数共享状态
```typescript
// composables/useTheme.ts
import { ref, readonly } from 'vue'

const theme = ref<'light' | 'dark'>('light')

export function useTheme() {
  const setTheme = (newTheme: 'light' | 'dark') => {
    theme.value = newTheme
    document.documentElement.setAttribute('data-theme', newTheme)
  }

  const toggleTheme = () => {
    setTheme(theme.value === 'light' ? 'dark' : 'light')
  }

  return {
    theme: readonly(theme),
    setTheme,
    toggleTheme
  }
}
```

## 7. 样式规范

### 7.1 CSS 组织结构

#### 7.1.1 组件样式
```vue
<style scoped>
/* 组件根样式 */
.user-card {
  /* 基础样式 */
}

/* 元素样式 */
.user-card__header {
  /* 头部样式 */
}

.user-card__content {
  /* 内容样式 */
}

.user-card__footer {
  /* 底部样式 */
}

/* 修饰符样式 */
.user-card--active {
  /* 激活状态 */
}

.user-card--disabled {
  /* 禁用状态 */
}

/* 响应式样式 */
@media (max-width: 768px) {
  .user-card {
    /* 移动端样式 */
  }
}
</style>
```

#### 7.1.2 BEM 命名规范
```css
/* 块 (Block) */
.block {}

/* 元素 (Element) */
.block__element {}

/* 修饰符 (Modifier) */
.block--modifier {}
.block__element--modifier {}

/* 示例 */
.user-card {}                    /* 块 */
.user-card__avatar {}           /* 元素 */
.user-card__name {}             /* 元素 */
.user-card--selected {}         /* 修饰符 */
.user-card__avatar--large {}    /* 元素修饰符 */
```

### 7.2 设计系统

#### 7.2.1 颜色系统
```scss
// colors.scss
:root {
  // 主色调
  --primary-color: #1890ff;
  --primary-hover: #40a9ff;
  --primary-active: #096dd9;

  // 成功色
  --success-color: #52c41a;
  --success-light: #f6ffed;

  // 警告色
  --warning-color: #faad14;
  --warning-light: #fffbe6;

  // 错误色
  --error-color: #ff4d4f;
  --error-light: #fff2f0;

  // 中性色
  --text-primary: #262626;
  --text-secondary: #595959;
  --text-disabled: #bfbfbf;

  --border-color: #d9d9d9;
  --background-color: #fafafa;
}
```

#### 7.2.2 间距系统
```scss
// spacing.scss
:root {
  --spacing-xs: 4px;
  --spacing-sm: 8px;
  --spacing-md: 16px;
  --spacing-lg: 24px;
  --spacing-xl: 32px;
  --spacing-xxl: 48px;
}

// 使用示例
.my-component {
  padding: var(--spacing-md);
  margin-bottom: var(--spacing-lg);
}
```

## 8. 代码质量规范

### 8.1 ESLint 配置

#### 8.1.1 ESLint 配置
```javascript
// .eslintrc.js
module.exports = {
  root: true,
  env: {
    browser: true,
    es2022: true,
    node: true
  },
  extends: [
    'eslint:recommended',
    '@vue/eslint-config-typescript',
    '@vue/eslint-config-prettier'
  ],
  parserOptions: {
    ecmaVersion: 'latest',
    sourceType: 'module'
  },
  rules: {
    // Vue 相关规则
    'vue/multi-word-component-names': 'off',
    'vue/component-definition-name-casing': ['error', 'PascalCase'],
    'vue/component-name-in-template-casing': ['error', 'PascalCase'],

    // TypeScript 相关规则
    '@typescript-eslint/no-unused-vars': ['error', { argsIgnorePattern: '^_' }],
    '@typescript-eslint/explicit-function-return-type': 'off',
    '@typescript-eslint/no-explicit-any': 'warn',

    // 通用规则
    'no-console': process.env.NODE_ENV === 'production' ? 'error' : 'off',
    'no-debugger': process.env.NODE_ENV === 'production' ? 'error' : 'off',
    'prefer-const': 'error',
    'no-var': 'error'
  },
  overrides: [
    {
      files: ['*.vue'],
      rules: {
        'vue/script-setup-uses-vars': 'error'
      }
    }
  ]
}
```

### 8.2 Prettier 配置

#### 8.2.1 Prettier 配置
```javascript
// .prettierrc.js
module.exports = {
  // 基础配置
  printWidth: 100,
  tabWidth: 2,
  useTabs: false,
  semi: false,
  singleQuote: true,
  quoteProps: 'as-needed',
  trailingComma: 'none',

  // Vue 相关
  vueIndentScriptAndStyle: false,

  // 其他配置
  bracketSpacing: true,
  bracketSameLine: false,
  arrowParens: 'avoid',
  endOfLine: 'lf'
}
```

### 8.3 提交规范

#### 8.3.1 Commit 消息格式
```
<type>(<scope>): <subject>

<body>

<footer>
```

#### 8.3.2 Type 类型
- `feat`: 新功能
- `fix`: 修复bug
- `docs`: 文档更新
- `style`: 代码格式调整
- `refactor`: 代码重构
- `test`: 测试相关
- `chore`: 构建工具或辅助工具的变动

#### 8.3.3 示例
```
feat(auth): 添加用户登录功能

- 实现用户名密码登录
- 支持记住密码功能
- 添加登录失败错误提示

Closes #123
```

### 8.3.3 示例

```
feat(auth): 添加用户登录功能

- 实现用户名密码登录
- 支持记住密码功能
- 添加登录失败错误提示

Closes #123
```

---

## 9. 无障碍规范 (Accessibility)

> 参考关联规范：`ui-design.mdc`、`.cursor/rules/ui-design.mdc`

### 9.1 WCAG 2.1 AA 标准

| 标准 | 要求 | 实现方式 |
|------|------|---------|
| 对比度 | 正文文本 ≥ 4.5:1，大文本 ≥ 3:1 | Tailwind `text-gray-500` 以上 |
| 焦点可见 | 所有可交互元素有可见焦点环 | Tailwind `focus:ring-2 focus:ring-primary` |
| 键盘导航 | 所有功能可通过键盘操作 | Tab 顺序、Enter/Space 激活 |
| ARIA 标签 | 图标按钮、非标准控件标注 | `aria-label`、`aria-expanded` |
| 屏幕阅读器 | 动态内容变化通知 | `aria-live="polite"` |

### 9.2 实现示例

```vue
<!-- ✅ 语义化 HTML + ARIA -->
<button
  class="gzang-btn-primary"
  aria-label="删除交易记录"
  aria-describedby="delete-hint"
  @click="handleDelete"
>
  <icon-trash />
</button>
<span id="delete-hint" class="sr-only">
  此操作将永久删除记录
</span>

<!-- ✅ 动态内容通知 -->
<div aria-live="polite" aria-atomic="true">
  <span v-if="notification">{{ notification }}</span>
</div>

<!-- ✅ 表单关联标签 -->
<label for="amount-input" class="gzang-label">金额</label>
<input
  id="amount-input"
  v-model="amount"
  type="number"
  step="0.01"
  aria-describedby="amount-hint amount-error"
  aria-invalid="hasError"
/>
<span id="amount-hint" class="gzang-hint">输入交易金额</span>
<span v-if="hasError" id="amount-error" class="gzang-error" role="alert">
  金额不能为空
</span>

<!-- ✅ 键盘导航焦点管理 -->
<div
  role="dialog"
  aria-modal="true"
  aria-labelledby="dialog-title"
  @keydown.escape="closeDialog"
>
  <h2 id="dialog-title">确认删除</h2>
  <button autofocus @click="confirm">确认</button>
  <button @click="cancel">取消</button>
</div>
```

### 9.3 屏幕阅读器支持

| 场景 | 实现 |
|------|------|
| 加载状态 | `aria-busy="true"` + `aria-label="加载中"` |
| 空状态 | `aria-empty="true"` + 描述文案 |
| 表格 | `role="grid"` + `scope="col/row"` |
| 折叠面板 | `aria-expanded` + `aria-controls` |

---

## 10. PWA 规范

> Progressive Web App — 让 Web 应用拥有原生体验

### 10.1 Web Manifest

```json
// public/manifest.json
{
  "name": "G-Zang 财务管家",
  "short_name": "G-Zang",
  "description": "智能财务管理系统",
  "start_url": "/",
  "display": "standalone",
  "background_color": "#F8F9FA",
  "theme_color": "#0F4C5C",
  "icons": [
    { "src": "/icons/icon-192.png", "sizes": "192x192", "type": "image/png" },
    { "src": "/icons/icon-512.png", "sizes": "512x512", "type": "image/png" }
  ]
}
```

### 10.2 Service Worker

```typescript
// public/sw.js — 缓存策略
const CACHE_NAME = 'gzang-v1';
const STATIC_ASSETS = [
  '/',
  '/index.html',
  '/manifest.json',
];

// 安装：预缓存静态资源
self.addEventListener('install', (event) => {
  event.waitUntil(
    caches.open(CACHE_NAME).then((cache) => cache.addAll(STATIC_ASSETS))
  );
});

// 激活：清理旧缓存
self.addEventListener('activate', (event) => {
  event.waitUntil(
    caches.keys().then((keys) =>
      Promise.all(keys.filter((k) => k !== CACHE_NAME).map((k) => caches.delete(k)))
    )
  );
});

// 拦截：Cache-First (静态) + Network-First (API)
self.addEventListener('fetch', (event) => {
  const { request } = event;
  const url = new URL(request.url);

  // API 请求：Network-First
  if (url.pathname.startsWith('/api/')) {
    event.respondWith(
      fetch(request)
        .then((res) => res)
        .catch(() => caches.match(request))
    );
    return;
  }

  // 静态资源：Cache-First
  event.respondWith(
    caches.match(request).then((cached) => cached || fetch(request))
  );
});
```

### 10.3 离线支持

```typescript
// hooks/useOffline.ts — 离线状态检测
import { ref, onMounted } from 'vue';

export function useOffline() {
  const isOffline = ref(!navigator.onLine);

  const updateOnlineStatus = () => {
    isOffline.value = !navigator.onLine;
  };

  onMounted(() => {
    window.addEventListener('online', updateOnlineStatus);
    window.addEventListener('offline', updateOnlineStatus);
  });

  return { isOffline };
}

// 使用
const { isOffline } = useOffline();
// <div v-if="isOffline" class="offline-banner">当前离线，部分功能不可用</div>
```

---

## 11. 性能优化

### 11.1 Core Web Vitals 目标

| 指标 | 目标 | 含义 |
|------|------|------|
| LCP (最大内容绘制) | < 2.5s | 页面主要内容加载速度 |
| FID (首次输入延迟) | < 100ms | 页面响应速度 |
| CLS (累积布局偏移) | < 0.1 | 页面稳定性（无意外位移） |

### 11.2 Bundle 优化

| 策略 | 实现 |
|------|------|
| Tree Shaking | Vite 默认支持，ES Module 导入 |
| 代码分割 | `import()` 动态导入，按路由拆分 |
| 预取/预加载 | `<link rel="prefetch/preload">` |
| 压缩 | Gzip + Brotli，Vite `build.target` |
| CDN | 静态资源部署至 CDN（阿里云 OSS） |

```typescript
// ✅ 按路由代码分割
const TransactionList = () => import('@/views/accounting/TransactionList.vue');
const Report = () => import(/* webpackPrefetch: true */ '@/views/report/Report.vue');

// ✅ 组件级别延迟加载
const VoiceInput = defineAsyncComponent(() => import('@/components/VoiceInput.vue'));
```

### 11.3 图片优化

| 策略 | 实现 |
|------|------|
| 响应式图片 | `<img srcset="img-2x.webp 2x, img.webp 1x">` |
| 懒加载 | `loading="lazy"` + Intersection Observer |
| 格式选择 | WebP（现代）> JPEG（照片）/ PNG（图标） |
| 图标 | SVG Inline 或 Iconify，避免图片图标 |

### 11.4 渲染优化

| 策略 | 实现 |
|------|------|
| 虚拟列表 | 大数据列表使用 `vue-virtual-scroller` |
| v-memo | 固定内容避免重复渲染 |
| shallowRef | 大数据结构使用 shallowRef |
| Keep Alive | 路由缓存 `<KeepAlive>` |
| 防抖/节流 | 搜索输入、滚动事件、resize |

---

**文档版本**：1.0.0
**最后更新**：2026-03-27
**维护人员**：前端开发工程师
**关联规则**：`pc-main.mdc`、`pc-personal.mdc`、`pc-business.mdc`、`mobile.mdc`、`admin.mdc`、`ui-design.mdc`
