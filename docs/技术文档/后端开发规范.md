# G-Zang (归藏) 后端开发规范

> **文档版本**：1.0.0
> **最后更新**：2026-03-27
> **维护人员**：后端架构师
> **关联规则**：`backend.mdc`、`security.mdc`、`database.mdc`

## 1. 项目结构规范

### 1.1 标准 Maven 项目结构

```
server/
├── src/
│   ├── main/
│   │   ├── java/com/gzang/app/
│   │   │   ├── config/              # 配置类
│   │   │   │   ├── SecurityConfig.java
│   │   │   │   ├── MyBatisPlusConfig.java
│   │   │   │   ├── JwtAuthenticationFilter.java
│   │   │   │   ├── TenantInterceptor.java
│   │   │   │   ├── SwaggerConfig.java
│   │   │   │   └── WebConfig.java
│   │   │   ├── controller/          # 控制器层
│   │   │   │   ├── AuthController.java
│   │   │   │   ├── UserController.java
│   │   │   │   ├── AccountingController.java
│   │   │   │   └── ReportController.java
│   │   │   ├── service/             # 业务逻辑层
│   │   │   │   ├── impl/            # 实现类
│   │   │   │   └── interfaces/      # 接口定义
│   │   │   ├── mapper/              # 数据访问层
│   │   │   ├── entity/              # 实体类
│   │   │   │   ├── BaseEntity.java
│   │   │   │   ├── User.java
│   │   │   │   └── Transaction.java
│   │   │   ├── dto/                 # 数据传输对象
│   │   │   ├── vo/                  # 视图对象
│   │   │   ├── exception/           # 异常处理
│   │   │   ├── util/                # 工具类
│   │   │   ├── constant/            # 常量定义
│   │   │   └── GZangApplication.java
│   │   └── resources/
│   │       ├── application.yml      # 主配置文件
│   │       ├── application-dev.yml  # 开发环境配置
│   │       ├── application-test.yml # 测试环境配置
│   │       ├── application-prod.yml # 生产环境配置
│   │       ├── db/
│   │       │   └── schema.sql       # 数据库初始化脚本
│   │       ├── mapper/              # MyBatis XML映射文件
│   │       └── static/              # 静态资源
│   └── test/
│       ├── java/com/gzang/app/      # 测试类
│       └── resources/               # 测试资源
├── target/                          # 编译输出目录
├── pom.xml                          # Maven配置
└── README.md                        # 项目说明
```

### 1.2 包命名规范

#### 1.2.1 基础包结构
```java
com.gzang.app                    // 根包
├── config                       // 配置类包
├── controller                   // 控制器包
├── service                      // 业务逻辑包
│   └── impl                     // 实现类包
├── mapper                       // 数据访问包
├── entity                       // 实体类包
├── dto                          // 数据传输对象包
├── vo                           // 视图对象包
├── exception                    // 异常类包
├── util                         // 工具类包
├── constant                     // 常量类包
└── annotation                   // 自定义注解包
```

#### 1.2.2 包命名规则
- **包名全小写**：使用小写字母和数字
- **层次分明**：按功能模块划分子包
- **避免冲突**：使用公司域名倒置作为根包名
- **语义明确**：包名应能反映其功能职责

```java
// ✅ 正确示例
com.gzang.app.controller
com.gzang.app.service.impl
com.gzang.app.mapper.user

// ❌ 错误示例
com.gzang.App.Controller    // 大写字母
com.gzang.app.ctl           // 缩写不清晰
```

### 1.3 类命名规范

#### 1.3.1 类命名规则
- **PascalCase**：首字母大写，驼峰命名
- **语义明确**：类名应准确反映其职责
- **后缀规范**：不同类型类使用标准后缀

```java
// 实体类
public class User {}              // 用户实体
public class Transaction {}       // 交易实体
public class Account {}           // 账户实体

// 控制器类
public class UserController {}    // 用户控制器
public class AuthController {}    // 认证控制器

// 服务类
public interface UserService {}   // 用户服务接口
public class UserServiceImpl {}   // 用户服务实现

// 配置类
@Configuration
public class SecurityConfig {}    // 安全配置

// 异常类
public class BusinessException {} // 业务异常

// 工具类
public class JwtUtil {}           // JWT工具类
public class DateUtil {}          // 日期工具类
```

#### 1.3.2 方法命名规则
- **camelCase**：首字母小写，驼峰命名
- **动词开头**：方法名以动词开头，表明操作类型
- **语义明确**：准确描述方法功能

```java
// 查询方法
public User getUserById(Long id) {}           // 根据ID获取用户
public List<User> findUsersByStatus(Integer status) {} // 根据状态查找用户

// 修改方法
public void updateUser(User user) {}          // 更新用户
public void changePassword(Long userId, String newPassword) {} // 修改密码

// 删除方法
public void deleteUser(Long id) {}            // 删除用户
public void removeUserRole(Long userId, Long roleId) {} // 移除用户角色

// 业务方法
public void createTransaction(Transaction transaction) {} // 创建交易
public BigDecimal calculateBalance(Long accountId) {}    // 计算余额
```

#### 1.3.3 变量命名规则
- **camelCase**：局部变量和成员变量
- **语义明确**：变量名应准确反映其含义
- **合理长度**：避免过长或过短的变量名

```java
// 成员变量
private Long userId;
private String username;
private List<Transaction> transactions;

// 局部变量
User currentUser = getCurrentUser();
BigDecimal totalAmount = calculateTotal();
List<User> activeUsers = findActiveUsers();

// 常量
public static final String JWT_SECRET = "gzang-secret-key";
public static final int MAX_LOGIN_ATTEMPTS = 5;
```

## 2. 代码编写规范

### 2.1 注解使用规范

#### 2.1.1 Lombok 注解
```java
// 实体类使用
@Data                           // 生成 getter/setter/equals/hashCode/toString
@NoArgsConstructor              // 生成无参构造
@AllArgsConstructor             // 生成全参构造
@Builder                        // 生成建造者模式
@TableName("t_user")            // MyBatis Plus 表名映射
public class User extends BaseEntity {

    @TableId(type = IdType.AUTO)
    private Long id;

    @TableField("username")
    private String username;

    @TableField("password")
    private String password;
}
```

#### 2.1.2 Spring 注解
```java
// 控制器注解
@RestController
@RequestMapping("/api/v1/users")
public class UserController {

    @Autowired
    private UserService userService;

    @GetMapping("/{id}")
    public Result<UserVO> getUser(@PathVariable Long id) {
        return Result.success(userService.getUserById(id));
    }

    @PostMapping
    public Result<UserVO> createUser(@Valid @RequestBody CreateUserDTO dto) {
        return Result.success(userService.createUser(dto));
    }
}
```

#### 2.1.3 验证注解
```java
public class CreateUserDTO {

    @NotBlank(message = "用户名不能为空")
    @Size(min = 3, max = 20, message = "用户名长度必须在3-20之间")
    private String username;

    @NotBlank(message = "密码不能为空")
    @Pattern(regexp = "^(?=.*[a-z])(?=.*[A-Z])(?=.*\\d).{8,}$",
             message = "密码必须包含大小写字母和数字，且长度至少8位")
    private String password;

    @NotBlank(message = "邮箱不能为空")
    @Email(message = "邮箱格式不正确")
    private String email;
}
```

### 2.2 异常处理规范

#### 2.2.1 统一异常处理
```java
@RestControllerAdvice
@Slf4j
public class GlobalExceptionHandler {

    /**
     * 处理业务异常
     */
    @ExceptionHandler(BusinessException.class)
    public Result<Void> handleBusinessException(BusinessException e) {
        log.warn("业务异常: {}", e.getMessage());
        return Result.error(e.getCode(), e.getMessage());
    }

    /**
     * 处理参数验证异常
     */
    @ExceptionHandler(MethodArgumentNotValidException.class)
    public Result<Void> handleValidationException(MethodArgumentNotValidException e) {
        String message = e.getBindingResult().getFieldErrors().stream()
            .map(error -> error.getField() + ": " + error.getDefaultMessage())
            .collect(Collectors.joining(", "));
        log.warn("参数验证失败: {}", message);
        return Result.error(400, "参数错误: " + message);
    }

    /**
     * 处理系统异常
     */
    @ExceptionHandler(Exception.class)
    public Result<Void> handleSystemException(Exception e) {
        log.error("系统异常", e);
        return Result.error(500, "系统内部错误");
    }
}
```

#### 2.2.2 自定义异常
```java
/**
 * 业务异常
 */
public class BusinessException extends RuntimeException {

    private Integer code;

    public BusinessException(String message) {
        super(message);
        this.code = 500;
    }

    public BusinessException(Integer code, String message) {
        super(message);
        this.code = code;
    }

    // getters and setters
}

/**
 * 数据不存在异常
 */
public class DataNotFoundException extends BusinessException {

    public DataNotFoundException(String resource) {
        super(404, resource + "不存在");
    }
}

/**
 * 权限不足异常
 */
public class PermissionDeniedException extends BusinessException {

    public PermissionDeniedException() {
        super(403, "权限不足");
    }
}
```

### 2.3 日志规范

#### 2.3.1 日志级别使用
```java
@Slf4j
@Service
public class UserServiceImpl implements UserService {

    public UserVO getUserById(Long id) {
        log.debug("开始获取用户，ID: {}", id);

        User user = userMapper.selectById(id);
        if (user == null) {
            log.warn("用户不存在，ID: {}", id);
            throw new DataNotFoundException("用户");
        }

        log.debug("成功获取用户，ID: {}, 用户名: {}", id, user.getUsername());
        return convertToVO(user);
    }

    public void updateUser(User user) {
        log.info("开始更新用户，ID: {}, 用户名: {}", user.getId(), user.getUsername());

        try {
            userMapper.updateById(user);
            log.info("用户更新成功，ID: {}", user.getId());
        } catch (Exception e) {
            log.error("用户更新失败，ID: {}, 错误: {}", user.getId(), e.getMessage(), e);
            throw e;
        }
    }
}
```

#### 2.3.2 日志配置
```yaml
# application.yml
logging:
  level:
    root: INFO
    com.gzang.app: DEBUG
    org.springframework.security: DEBUG
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n"
    file: "%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n"
  file:
    name: logs/gzang.log
  logback:
    rollingpolicy:
      max-file-size: 100MB
      max-history: 30
```

## 3. 数据访问层规范

### 3.1 MyBatis Plus 使用规范

#### 3.1.1 Mapper 接口定义
```java
/**
 * 用户Mapper接口
 */
@Mapper
public interface UserMapper extends BaseMapper<User> {

    /**
     * 根据用户名查询用户
     */
    User selectByUsername(@Param("username") String username);

    /**
     * 查询用户列表（分页）
     */
    IPage<User> selectUserPage(IPage<User> page, @Param("query") UserQuery query);

    /**
     * 批量更新用户状态
     */
    int updateStatusBatch(@Param("ids") List<Long> ids, @Param("status") Integer status);

    /**
     * 统计用户数量（按状态分组）
     */
    List<Map<String, Object>> countByStatus();
}
```

#### 3.1.2 XML 映射文件
```xml
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.gzang.app.mapper.UserMapper">

    <!-- 根据用户名查询用户 -->
    <select id="selectByUsername" resultType="com.gzang.app.entity.User">
        SELECT * FROM t_user WHERE username = #{username}
    </select>

    <!-- 分页查询用户 -->
    <select id="selectUserPage" resultType="com.gzang.app.entity.User">
        SELECT * FROM t_user
        <where>
            <if test="query.username != null and query.username != ''">
                AND username LIKE CONCAT('%', #{query.username}, '%')
            </if>
            <if test="query.status != null">
                AND status = #{query.status}
            </if>
            <if test="query.roleId != null">
                AND role_id = #{query.roleId}
            </if>
        </where>
        ORDER BY create_time DESC
    </select>

    <!-- 批量更新状态 -->
    <update id="updateStatusBatch">
        UPDATE t_user SET status = #{status}, update_time = NOW()
        WHERE id IN
        <foreach item="id" collection="ids" open="(" separator="," close=")">
            #{id}
        </foreach>
    </update>

    <!-- 统计用户状态 -->
    <select id="countByStatus" resultType="map">
        SELECT status, COUNT(*) as count FROM t_user GROUP BY status
    </select>

</mapper>
```

#### 3.1.3 条件构造器使用
```java
@Service
public class UserServiceImpl implements UserService {

    @Autowired
    private UserMapper userMapper;

    public IPage<User> getUserPage(UserQuery query, int page, int size) {
        Page<User> pageParam = new Page<>(page, size);

        return userMapper.selectPage(pageParam, new LambdaQueryWrapper<User>()
            .like(StringUtils.hasText(query.getUsername()), User::getUsername, query.getUsername())
            .eq(query.getStatus() != null, User::getStatus, query.getStatus())
            .eq(query.getRoleId() != null, User::getRoleId, query.getRoleId())
            .orderByDesc(User::getCreateTime)
        );
    }

    public boolean updateUserStatus(Long userId, Integer status) {
        return userMapper.update(null, new LambdaUpdateWrapper<User>()
            .set(User::getStatus, status)
            .set(User::getUpdateTime, LocalDateTime.now())
            .eq(User::getId, userId)
        ) > 0;
    }
}
```

### 3.2 数据传输对象规范

#### 3.2.1 DTO 定义
```java
/**
 * 创建用户DTO
 */
@Data
@NoArgsConstructor
@AllArgsConstructor
@Builder
public class CreateUserDTO {

    @NotBlank(message = "用户名不能为空")
    @Size(min = 3, max = 20, message = "用户名长度必须在3-20之间")
    private String username;

    @NotBlank(message = "密码不能为空")
    @Pattern(regexp = "^(?=.*[a-z])(?=.*[A-Z])(?=.*\\d).{8,}$",
             message = "密码必须包含大小写字母和数字，且长度至少8位")
    private String password;

    @NotBlank(message = "邮箱不能为空")
    @Email(message = "邮箱格式不正确")
    private String email;

    @Size(max = 20, message = "昵称长度不能超过20")
    private String nickname;
}

/**
 * 更新用户DTO
 */
@Data
@NoArgsConstructor
@AllArgsConstructor
@Builder
public class UpdateUserDTO {

    @Size(max = 20, message = "昵称长度不能超过20")
    private String nickname;

    @Size(max = 100, message = "头像URL长度不能超过100")
    private String avatar;
}
```

#### 3.2.2 VO 定义
```java
/**
 * 用户视图对象
 */
@Data
@NoArgsConstructor
@AllArgsConstructor
@Builder
public class UserVO {

    private Long id;
    private String username;
    private String nickname;
    private String avatar;
    private Integer roleId;
    private String roleName;
    private Long companyId;
    private String companyName;
    private Integer status;
    private LocalDateTime createTime;

    /**
     * 从实体转换VO
     */
    public static UserVO fromEntity(User user) {
        return UserVO.builder()
            .id(user.getId())
            .username(user.getUsername())
            .nickname(user.getNickname())
            .avatar(user.getAvatar())
            .roleId(user.getRoleId())
            .status(user.getStatus())
            .createTime(user.getCreateTime())
            .build();
    }

    /**
     * 从实体转换VO（包含关联数据）
     */
    public static UserVO fromEntityWithRelations(User user, Role role, Company company) {
        UserVO vo = fromEntity(user);
        if (role != null) {
            vo.setRoleName(role.getRoleName());
        }
        if (company != null) {
            vo.setCompanyId(company.getId());
            vo.setCompanyName(company.getCompanyName());
        }
        return vo;
    }
}
```

## 4. 业务逻辑层规范

### 4.1 服务接口设计

#### 4.1.1 接口定义规范
```java
/**
 * 用户服务接口
 */
public interface UserService {

    /**
     * 根据ID获取用户
     * @param id 用户ID
     * @return 用户视图对象
     * @throws DataNotFoundException 用户不存在时抛出
     */
    UserVO getUserById(Long id);

    /**
     * 根据用户名获取用户
     * @param username 用户名
     * @return 用户对象
     */
    User getUserByUsername(String username);

    /**
     * 分页查询用户
     * @param query 查询条件
     * @param page 页码
     * @param size 每页数量
     * @return 分页结果
     */
    IPage<UserVO> getUserPage(UserQuery query, int page, int size);

    /**
     * 创建用户
     * @param dto 创建用户DTO
     * @return 创建的用户VO
     */
    UserVO createUser(CreateUserDTO dto);

    /**
     * 更新用户
     * @param id 用户ID
     * @param dto 更新用户DTO
     * @return 更新后的用户VO
     * @throws DataNotFoundException 用户不存在时抛出
     */
    UserVO updateUser(Long id, UpdateUserDTO dto);

    /**
     * 删除用户
     * @param id 用户ID
     * @throws DataNotFoundException 用户不存在时抛出
     */
    void deleteUser(Long id);

    /**
     * 更新用户状态
     * @param id 用户ID
     * @param status 状态值
     * @return 是否成功
     */
    boolean updateUserStatus(Long id, Integer status);
}
```

#### 4.1.2 实现类规范
```java
/**
 * 用户服务实现类
 */
@Slf4j
@Service
@Transactional(readOnly = true)
public class UserServiceImpl implements UserService {

    @Autowired
    private UserMapper userMapper;

    @Autowired
    private RoleMapper roleMapper;

    @Autowired
    private CompanyMapper companyMapper;

    @Autowired
    private PasswordEncoder passwordEncoder;

    @Override
    public UserVO getUserById(Long id) {
        log.debug("获取用户详情，ID: {}", id);

        User user = userMapper.selectById(id);
        if (user == null) {
            throw new DataNotFoundException("用户");
        }

        // 获取关联数据
        Role role = roleMapper.selectById(user.getRoleId());
        Company company = null;
        if (user.getCompanyId() != null) {
            company = companyMapper.selectById(user.getCompanyId());
        }

        return UserVO.fromEntityWithRelations(user, role, company);
    }

    @Override
    public User getUserByUsername(String username) {
        return userMapper.selectByUsername(username);
    }

    @Override
    public IPage<UserVO> getUserPage(UserQuery query, int page, int size) {
        Page<User> pageParam = new Page<>(page, size);
        IPage<User> userPage = userMapper.selectUserPage(pageParam, query);

        // 转换为VO
        return userPage.convert(user -> {
            Role role = roleMapper.selectById(user.getRoleId());
            Company company = user.getCompanyId() != null ?
                companyMapper.selectById(user.getCompanyId()) : null;
            return UserVO.fromEntityWithRelations(user, role, company);
        });
    }

    @Override
    @Transactional
    public UserVO createUser(CreateUserDTO dto) {
        log.info("创建用户: {}", dto.getUsername());

        // 检查用户名是否已存在
        if (userMapper.selectByUsername(dto.getUsername()) != null) {
            throw new BusinessException(400, "用户名已存在");
        }

        // 创建用户实体
        User user = User.builder()
            .username(dto.getUsername())
            .password(passwordEncoder.encode(dto.getPassword()))
            .nickname(dto.getNickname())
            .email(dto.getEmail())
            .roleId(3) // 默认普通用户角色
            .status(1) // 默认正常状态
            .build();

        userMapper.insert(user);
        log.info("用户创建成功，ID: {}", user.getId());

        return UserVO.fromEntity(user);
    }

    @Override
    @Transactional
    public UserVO updateUser(Long id, UpdateUserDTO dto) {
        log.info("更新用户，ID: {}", id);

        User user = userMapper.selectById(id);
        if (user == null) {
            throw new DataNotFoundException("用户");
        }

        // 更新字段
        if (StringUtils.hasText(dto.getNickname())) {
            user.setNickname(dto.getNickname());
        }
        if (StringUtils.hasText(dto.getAvatar())) {
            user.setAvatar(dto.getAvatar());
        }

        userMapper.updateById(user);

        return UserVO.fromEntity(user);
    }

    @Override
    @Transactional
    public void deleteUser(Long id) {
        log.info("删除用户，ID: {}", id);

        if (userMapper.selectById(id) == null) {
            throw new DataNotFoundException("用户");
        }

        userMapper.deleteById(id);
    }

    @Override
    @Transactional
    public boolean updateUserStatus(Long id, Integer status) {
        log.info("更新用户状态，ID: {}, 状态: {}", id, status);

        return userMapper.updateStatusBatch(Arrays.asList(id), status) > 0;
    }
}
```

### 4.2 事务管理

#### 4.2.1 事务使用规范
```java
@Service
public class AccountingServiceImpl implements AccountingService {

    @Autowired
    private TransactionMapper transactionMapper;

    @Autowired
    private AccountMapper accountMapper;

    /**
     * 创建交易（需要事务保证数据一致性）
     */
    @Override
    @Transactional
    public TransactionVO createTransaction(CreateTransactionDTO dto) {
        // 1. 验证账户存在
        Account account = accountMapper.selectById(dto.getAccountId());
        if (account == null) {
            throw new DataNotFoundException("账户");
        }

        // 2. 验证分类存在
        Category category = categoryMapper.selectById(dto.getCategoryId());
        if (category == null) {
            throw new DataNotFoundException("分类");
        }

        // 3. 创建交易记录
        Transaction transaction = Transaction.builder()
            .amount(dto.getAmount())
            .type(dto.getType())
            .categoryId(dto.getCategoryId())
            .accountId(dto.getAccountId())
            .transactionTime(dto.getTransactionTime())
            .remark(dto.getRemark())
            .build();

        transactionMapper.insert(transaction);

        // 4. 更新账户余额
        BigDecimal newBalance = calculateNewBalance(account.getBalance(), dto);
        accountMapper.updateBalance(dto.getAccountId(), newBalance);

        return TransactionVO.fromEntity(transaction);
    }

    /**
     * 只读操作使用 readOnly 事务
     */
    @Override
    @Transactional(readOnly = true)
    public IPage<TransactionVO> getTransactionPage(TransactionQuery query, int page, int size) {
        Page<Transaction> pageParam = new Page<>(page, size);
        return transactionMapper.selectTransactionPage(pageParam, query)
            .convert(TransactionVO::fromEntity);
    }

    /**
     * 计算新余额
     */
    private BigDecimal calculateNewBalance(BigDecimal currentBalance, CreateTransactionDTO dto) {
        if (dto.getType() == 1) { // 收入
            return currentBalance.add(dto.getAmount());
        } else { // 支出
            return currentBalance.subtract(dto.getAmount());
        }
    }
}
```

#### 4.2.2 事务传播行为
```java
@Service
public class ComplexService {

    @Autowired
    private UserService userService;

    @Autowired
    private AccountService accountService;

    /**
     * 复杂业务操作（使用REQUIRED传播）
     * 如果当前存在事务，则加入该事务；如果不存在，则创建新事务
     */
    @Override
    @Transactional
    public void createUserWithAccount(CreateUserWithAccountDTO dto) {
        // 创建用户
        UserVO user = userService.createUser(dto.getUser());

        // 创建账户（加入同一事务）
        accountService.createAccount(user.getId(), dto.getInitialBalance());
    }

    /**
     * 独立操作（使用REQUIRES_NEW传播）
     * 总是创建新事务，如果当前存在事务，则挂起当前事务
     */
    @Override
    @Transactional
    public void processBatchOperations(List<BatchOperation> operations) {
        for (BatchOperation op : operations) {
            try {
                // 每个操作独立事务，保证部分失败不影响其他
                processSingleOperation(op);
            } catch (Exception e) {
                log.error("批处理操作失败: {}", op.getId(), e);
                // 记录失败，继续处理其他操作
            }
        }
    }

    @Transactional(propagation = Propagation.REQUIRES_NEW)
    private void processSingleOperation(BatchOperation op) {
        // 具体操作逻辑
    }
}
```

## 5. 控制器层规范

### 5.1 RESTful API 设计

#### 5.1.1 控制器规范
```java
/**
 * 用户管理控制器
 */
@Slf4j
@RestController
@RequestMapping("/api/v1/users")
@Validated
public class UserController {

    @Autowired
    private UserService userService;

    /**
     * 获取用户列表（分页）
     */
    @GetMapping
    public Result<IPage<UserVO>> getUsers(
            @RequestParam(defaultValue = "1") @Min(1) int page,
            @RequestParam(defaultValue = "10") @Min(1) @Max(100) int size,
            UserQuery query) {

        log.debug("查询用户列表，页码: {}, 每页数量: {}", page, size);
        IPage<UserVO> result = userService.getUserPage(query, page, size);
        return Result.success(result);
    }

    /**
     * 获取用户详情
     */
    @GetMapping("/{id}")
    public Result<UserVO> getUser(@PathVariable @Min(1) Long id) {
        log.debug("获取用户详情，ID: {}", id);
        UserVO user = userService.getUserById(id);
        return Result.success(user);
    }

    /**
     * 创建用户
     */
    @PostMapping
    public Result<UserVO> createUser(@Valid @RequestBody CreateUserDTO dto) {
        log.info("创建用户: {}", dto.getUsername());
        UserVO user = userService.createUser(dto);
        return Result.success(user);
    }

    /**
     * 更新用户
     */
    @PutMapping("/{id}")
    public Result<UserVO> updateUser(
            @PathVariable @Min(1) Long id,
            @Valid @RequestBody UpdateUserDTO dto) {

        log.info("更新用户，ID: {}", id);
        UserVO user = userService.updateUser(id, dto);
        return Result.success(user);
    }

    /**
     * 删除用户
     */
    @DeleteMapping("/{id}")
    public Result<Void> deleteUser(@PathVariable @Min(1) Long id) {
        log.info("删除用户，ID: {}", id);
        userService.deleteUser(id);
        return Result.success();
    }

    /**
     * 更新用户状态
     */
    @PatchMapping("/{id}/status")
    public Result<Void> updateUserStatus(
            @PathVariable @Min(1) Long id,
            @RequestParam @Min(0) @Max(1) Integer status) {

        log.info("更新用户状态，ID: {}, 状态: {}", id, status);
        boolean success = userService.updateUserStatus(id, status);
        return success ? Result.success() : Result.error("更新失败");
    }
}
```

#### 5.1.2 参数验证
```java
/**
 * 查询参数对象
 */
@Data
@NoArgsConstructor
@AllArgsConstructor
@Builder
public class UserQuery {

    @Size(max = 20, message = "用户名长度不能超过20")
    private String username;

    @Min(value = 0, message = "状态值不能小于0")
    @Max(value = 1, message = "状态值不能大于1")
    private Integer status;

    @Min(value = 1, message = "角色ID不能小于1")
    private Long roleId;

    @DateTimeFormat(pattern = "yyyy-MM-dd HH:mm:ss")
    private LocalDateTime createTimeStart;

    @DateTimeFormat(pattern = "yyyy-MM-dd HH:mm:ss")
    private LocalDateTime createTimeEnd;
}

/**
 * 分组验证示例
 */
public class AdvancedUserQuery {

    @Size(max = 20)
    private String username;

    @NotNull(groups = AdminQuery.class)
    @Min(value = 1, groups = AdminQuery.class)
    private Long roleId;

    // 分组接口
    public interface AdminQuery {}
    public interface NormalQuery {}
}
```

### 5.2 统一响应格式

#### 5.2.1 响应对象定义
```java
/**
 * 统一API响应对象
 */
@Data
@NoArgsConstructor
@AllArgsConstructor
@Builder
public class Result<T> {

    /**
     * 状态码：0-成功，其他-失败
     */
    private Integer code;

    /**
     * 响应消息
     */
    private String message;

    /**
     * 响应数据
     */
    private T data;

    /**
     * 响应时间戳
     */
    private Long timestamp;

    /**
     * 成功响应
     */
    public static <T> Result<T> success(T data) {
        return Result.<T>builder()
            .code(0)
            .message("操作成功")
            .data(data)
            .timestamp(System.currentTimeMillis())
            .build();
    }

    /**
     * 成功响应（无数据）
     */
    public static <T> Result<T> success() {
        return success(null);
    }

    /**
     * 错误响应
     */
    public static <T> Result<T> error(String message) {
        return error(500, message);
    }

    /**
     * 错误响应（指定状态码）
     */
    public static <T> Result<T> error(Integer code, String message) {
        return Result.<T>builder()
            .code(code)
            .message(message)
            .timestamp(System.currentTimeMillis())
            .build();
    }
}
```

#### 5.2.2 分页响应
```java
/**
 * 分页响应对象
 */
@Data
@NoArgsConstructor
@AllArgsConstructor
@Builder
public class PageResult<T> {

    /**
     * 数据列表
     */
    private List<T> records;

    /**
     * 总记录数
     */
    private Long total;

    /**
     * 总页数
     */
    private Long pages;

    /**
     * 当前页码
     */
    private Long current;

    /**
     * 每页大小
     */
    private Long size;

    /**
     * 从MyBatis Plus分页结果转换
     */
    public static <T> PageResult<T> fromPage(IPage<T> page) {
        return PageResult.<T>builder()
            .records(page.getRecords())
            .total(page.getTotal())
            .pages(page.getPages())
            .current(page.getCurrent())
            .size(page.getSize())
            .build();
    }
}
```

## 6. 配置管理规范

### 6.1 配置文件层次

#### 6.1.1 主配置文件
```yaml
# application.yml
server:
  port: 8080
  servlet:
    context-path: /api

spring:
  application:
    name: gzang-server
  profiles:
    active: dev

  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://${MYSQL_HOST:localhost}:${MYSQL_PORT:3306}/${MYSQL_DATABASE:gzang}?useSSL=false&serverTimezone=Asia/Shanghai
    username: ${MYSQL_USERNAME:gzang}
    password: ${MYSQL_PASSWORD:gzang123}

  redis:
    host: ${REDIS_HOST:localhost}
    port: ${REDIS_PORT:6379}
    password: ${REDIS_PASSWORD:}
    database: ${REDIS_DATABASE:0}

mybatis-plus:
  mapper-locations: classpath:/mapper/*.xml
  type-aliases-package: com.gzang.app.entity
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdoutImpl

logging:
  level:
    root: INFO
    com.gzang.app: DEBUG
```

#### 6.1.2 环境配置
```yaml
# application-dev.yml (开发环境)
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/gzang_dev?useSSL=false&serverTimezone=Asia/Shanghai
    username: gzang_dev
    password: gzang_dev123

logging:
  level:
    com.gzang.app: DEBUG
    org.springframework: INFO

# application-test.yml (测试环境)
spring:
  datasource:
    url: jdbc:mysql://test-db:3306/gzang_test?useSSL=false&serverTimezone=Asia/Shanghai
    username: gzang_test
    password: gzang_test123

# application-prod.yml (生产环境)
spring:
  datasource:
    url: jdbc:mysql://prod-db:3306/gzang_prod?useSSL=false&serverTimezone=Asia/Shanghai
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus
  metrics:
    export:
      prometheus:
        enabled: true

logging:
  level:
    root: WARN
    com.gzang.app: INFO
```

### 6.2 配置类规范

#### 6.2.1 配置属性类
```java
/**
 * JWT配置属性
 */
@Data
@ConfigurationProperties(prefix = "gzang.jwt")
public class JwtProperties {

    /**
     * JWT密钥
     */
    private String secret = "gzang-default-secret-key";

    /**
     * 访问令牌过期时间（秒）
     */
    private Long accessTokenExpire = 7200L; // 2小时

    /**
     * 刷新令牌过期时间（秒）
     */
    private Long refreshTokenExpire = 604800L; // 7天

    /**
     * 令牌发行者
     */
    private String issuer = "gzang-app";
}

/**
 * 文件上传配置属性
 */
@Data
@ConfigurationProperties(prefix = "gzang.upload")
public class UploadProperties {

    /**
     * 上传根目录
     */
    private String baseDir = "/opt/gzang/upload";

    /**
     * 最大文件大小
     */
    private String maxFileSize = "10MB";

    /**
     * 允许的文件类型
     */
    private List<String> allowedTypes = Arrays.asList("jpg", "jpeg", "png", "pdf");

    /**
     * URL前缀
     */
    private String urlPrefix = "/api/v1/files";
}
```

#### 6.2.2 配置类
```java
/**
 * JWT配置
 */
@Configuration
@EnableConfigurationProperties(JwtProperties.class)
public class JwtConfig {

    @Autowired
    private JwtProperties jwtProperties;

    @Bean
    public JwtUtil jwtUtil() {
        return new JwtUtil(jwtProperties);
    }
}

/**
 * 上传配置
 */
@Configuration
@EnableConfigurationProperties(UploadProperties.class)
public class UploadConfig {

    @Autowired
    private UploadProperties uploadProperties;

    @Bean
    public MultipartConfigElement multipartConfigElement() {
        MultipartConfigFactory factory = new MultipartConfigFactory();
        factory.setMaxFileSize(DataSize.parse(uploadProperties.getMaxFileSize()));
        factory.setMaxRequestSize(DataSize.parse(uploadProperties.getMaxFileSize()));
        return factory.createMultipartConfig();
    }

    @Bean
    public FileUploadService fileUploadService() {
        return new LocalFileUploadService(uploadProperties);
    }
}
```

## 7. 测试规范

### 7.1 单元测试

#### 7.1.1 Service层测试
```java
@SpringBootTest
class UserServiceTest {

    @Autowired
    private UserService userService;

    @Autowired
    private UserMapper userMapper;

    @Test
    @DisplayName("创建用户 - 成功")
    void createUser_Success() {
        // Given
        CreateUserDTO dto = CreateUserDTO.builder()
            .username("testuser")
            .password("TestPass123")
            .email("test@example.com")
            .nickname("测试用户")
            .build();

        // When
        UserVO result = userService.createUser(dto);

        // Then
        assertThat(result).isNotNull();
        assertThat(result.getUsername()).isEqualTo("testuser");
        assertThat(result.getEmail()).isEqualTo("test@example.com");

        // 验证数据库
        User saved = userMapper.selectByUsername("testuser");
        assertThat(saved).isNotNull();
        assertThat(saved.getStatus()).isEqualTo(1);
    }

    @Test
    @DisplayName("创建用户 - 用户名已存在")
    void createUser_UsernameExists() {
        // Given
        CreateUserDTO dto = CreateUserDTO.builder()
            .username("existinguser")
            .password("TestPass123")
            .email("test@example.com")
            .build();

        // 预先创建用户
        userService.createUser(dto);

        // When & Then
        assertThatThrownBy(() -> userService.createUser(dto))
            .isInstanceOf(BusinessException.class)
            .hasMessageContaining("用户名已存在");
    }
}
```

#### 7.1.2 Controller层测试
```java
@SpringBootTest
@AutoConfigureMockMvc
class UserControllerTest {

    @Autowired
    private MockMvc mockMvc;

    @Autowired
    private ObjectMapper objectMapper;

    @Test
    @DisplayName("获取用户详情 - 成功")
    void getUser_Success() throws Exception {
        // Given
        Long userId = 1L;

        // When
        MvcResult result = mockMvc.perform(
                get("/api/v1/users/{id}", userId)
                    .contentType(MediaType.APPLICATION_JSON)
            )
            .andExpect(status().isOk())
            .andReturn();

        // Then
        String responseBody = result.getResponse().getContentAsString();
        Result<UserVO> response = objectMapper.readValue(responseBody,
            new TypeReference<Result<UserVO>>() {});

        assertThat(response.getCode()).isEqualTo(0);
        assertThat(response.getData()).isNotNull();
        assertThat(response.getData().getId()).isEqualTo(userId);
    }

    @Test
    @DisplayName("创建用户 - 参数验证失败")
    void createUser_ValidationFailed() throws Exception {
        // Given
        CreateUserDTO invalidDto = CreateUserDTO.builder()
            .username("") // 空用户名
            .password("123") // 密码太短
            .email("invalid-email") // 邮箱格式错误
            .build();

        // When & Then
        mockMvc.perform(
                post("/api/v1/users")
                    .contentType(MediaType.APPLICATION_JSON)
                    .content(objectMapper.writeValueAsString(invalidDto))
            )
            .andExpect(status().isOk())
            .andExpect(jsonPath("$.code").value(400))
            .andExpect(jsonPath("$.message").value(containsString("参数错误")));
    }
}
```

### 7.2 集成测试

#### 7.2.1 API集成测试
```java
@SpringBootTest(webEnvironment = SpringBootTest.WebEnvironment.RANDOM_PORT)
class UserApiIntegrationTest {

    @Autowired
    private TestRestTemplate restTemplate;

    @Autowired
    private UserMapper userMapper;

    @Test
    @DisplayName("用户注册和登录集成测试")
    void userRegistrationAndLogin() {
        // 1. 注册用户
        CreateUserDTO registerDto = CreateUserDTO.builder()
            .username("integration_test_user")
            .password("TestPass123")
            .email("integration@example.com")
            .build();

        ResponseEntity<Result> registerResponse = restTemplate.postForEntity(
            "/api/v1/auth/register", registerDto, Result.class);

        assertThat(registerResponse.getStatusCode()).isEqualTo(HttpStatus.OK);
        assertThat(registerResponse.getBody().getCode()).isEqualTo(0);

        // 2. 登录
        LoginDTO loginDto = LoginDTO.builder()
            .username("integration_test_user")
            .password("TestPass123")
            .build();

        ResponseEntity<Result> loginResponse = restTemplate.postForEntity(
            "/api/v1/auth/login", loginDto, Result.class);

        assertThat(loginResponse.getStatusCode()).isEqualTo(HttpStatus.OK);
        assertThat(loginResponse.getBody().getCode()).isEqualTo(0);

        // 3. 验证登录响应包含token
        @SuppressWarnings("unchecked")
        Map<String, Object> data = (Map<String, Object>) loginResponse.getBody().getData();
        assertThat(data).containsKey("token");
        assertThat(data.get("token")).isNotNull();

        // 4. 清理测试数据
        userMapper.deleteByUsername("integration_test_user");
    }
}
```

---

## 8. 安全规范

> 参考关联规则：`security.mdc`、`.cursor/rules/security.mdc`

### 8.1 认证与授权

#### 8.1.1 JWT Token 安全

| 配置项 | 值 | 说明 |
|--------|-----|------|
| 算法 | HS256（对称）/ RS256（非对称） | 生产环境推荐 RS256 |
| Access Token 有效期 | ≤ 2 小时 | 必须短于 Refresh Token |
| Refresh Token 有效期 | ≤ 7 天 | 建议存 HttpOnly Cookie |
| Token 存储 | 不存 localStorage | 推荐存内存 + HttpOnly Cookie |

```java
// ✅ JWT 工具类配置示例
@Component
public class JwtUtil {
    @Value("${jwt.secret}")
    private String secret;

    @Value("${jwt.expiration:7200000}") // 默认 2 小时
    private Long expiration;

    public String generateToken(LoginUser user) {
        return Jwts.builder()
            .setSubject(user.getUsername())
            .claim("userId", user.getId())
            .claim("roles", user.getRoles())
            .setIssuedAt(new Date())
            .setExpiration(new Date(System.currentTimeMillis() + expiration))
            .signWith(Keys.hmacShaKeyFor(secret.getBytes()), SignatureAlgorithm.HS256)
            .compact();
    }
}
```

#### 8.1.2 密码安全

| 要求 | 实现 |
|------|------|
| 密码加密 | BCrypt（strength ≥ 11） |
| 禁止明文 | 任何场景下禁止打印/日志记录明文密码 |
| 登录失败锁定 | Redis 计数器：5 次失败锁定 30 分钟 |

```java
// ✅ 密码加密
@Bean
public PasswordEncoder passwordEncoder() {
    return new BCryptPasswordEncoder(11);
}

// ❌ 禁止
String md5 = DigestUtils.md5Hex(password); // 不安全
```

#### 8.1.3 权限检查

```java
// ✅ 使用注解声明式权限控制
@RequirePermission(code = "TRANSACTION_ADD", logical = Logical.AND)
@PostMapping("/transaction")
public Result<?> createTransaction(@RequestBody @Valid CreateTransactionDTO dto) {
    return Result.success(transactionService.create(dto));
}

// ✅ 注解定义
@Target({ElementType.METHOD})
@Retention(RetentionPolicy.RUNTIME)
public @interface RequirePermission {
    String code();
    Logical logical() default Logical.AND;
    enum Logical { AND, OR }
}
```

### 8.2 数据安全

#### 8.2.1 敏感数据加密

```java
// ✅ AES-GCM 加密工具类（用于身份证、银行卡等敏感字段）
@Component
public class AesEncryptUtil {
    private static final String ALGORITHM = "AES/GCM/NoPadding";
    private static final int GCM_TAG_LENGTH = 128;
    private static final int GCM_IV_LENGTH = 12;

    public String encrypt(String plaintext, String secretKey) {
        byte[] iv = new byte[GCM_IV_LENGTH];
        new SecureRandom().nextBytes(iv);
        Cipher cipher = Cipher.getInstance(ALGORITHM);
        SecretKeySpec keySpec = new SecretKeySpec(secretKey.getBytes(), "AES");
        GCMParameterSpec gcmSpec = new GCMParameterSpec(GCM_TAG_LENGTH, iv);
        cipher.init(Cipher.ENCRYPT_MODE, keySpec, gcmSpec);
        byte[] ciphertext = cipher.doFinal(plaintext.getBytes());
        // IV + 密文 拼接存储
        return Base64.getEncoder().encodeToString(
            ByteBuffer.allocate(iv.length + ciphertext.length)
                .put(iv).put(ciphertext).array()
        );
    }
}
```

#### 8.2.2 数据脱敏

```java
// ✅ JSON 序列化时自动脱敏
@JsonSerialize(using = DesensitizeSerializer.class)
private String phone; // 输出: 138****5678

@JsonSerialize(using = DesensitizeSerializer.class)
private String idCard; // 输出: 110***********1234

@JsonSerialize(using = DesensitizeSerializer.class)
private String bankCard; // 输出: **** **** **** 1234
```

### 8.3 输入安全

| 风险 | 防护措施 | 实现 |
|------|---------|------|
| SQL 注入 | 参数化查询 | MyBatis Plus Lambda QueryWrapper |
| XSS | 输出编码 + HTML 净化 | OWASP Encoder + jsoup |
| 文件上传 | 扩展名 + MIME + 魔数验证 | 白名单 + 文件头检查 |
| 参数校验 | Hibernate Validator | `@Valid` + `@Validated` |

```java
// ✅ 参数校验示例
@PostMapping("/transaction")
public Result<?> create(@Valid @RequestBody CreateTransactionDTO dto,
                        BindingResult bindingResult) {
    if (bindingResult.hasErrors()) {
        return Result.error(400, bindingResult.getFieldError().getDefaultMessage());
    }
    return Result.success(transactionService.create(dto));
}

// ✅ DTO 字段校验
public class CreateTransactionDTO {
    @NotNull(message = "金额不能为空")
    @DecimalMin(value = "0.01", message = "金额最小为 0.01")
    @Digits(integer = 15, fraction = 4, message = "金额格式错误")
    private BigDecimal amount;

    @NotNull(message = "交易类型不能为空")
    @Min(value = 1) @Max(value = 3)
    private Integer type;
}
```

### 8.4 API 安全

| 配置项 | 规范 |
|--------|------|
| CORS | 禁止 `allowedOrigins("*")`，必须指定白名单 |
| Rate Limiting | Redis 令牌桶算法：每 IP 1000 次/分钟 |
| HTTPS | 生产环境强制 TLS 1.3 |
| 安全响应头 | Nginx 配置 X-Frame-Options、CSP、X-Content-Type-Options |

```nginx
# Nginx 安全响应头
add_header X-Content-Type-Options "nosniff" always;
add_header X-Frame-Options "DENY" always;
add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
```

---

## 9. 监控与可观测性

> 参考关联规则：`devops.mdc`、`.cursor/rules/devops.mdc`

### 9.1 健康检查

```java
// ✅ Spring Boot Actuator 健康检查端点
@Component
public class CustomHealthIndicator implements HealthIndicator {
    @Override
    public Health health() {
        // 检查数据库连接
        if (!isDatabaseHealthy()) {
            return Health.down().withDetail("database", "unavailable").build();
        }
        // 检查 Redis 连接
        if (!isRedisHealthy()) {
            return Health.down().withDetail("redis", "unavailable").build();
        }
        return Health.up().build();
    }
}
```

### 9.2 指标收集 (Micrometer + Prometheus)

```java
// ✅ 自定义业务指标
@Component
public class TransactionMetrics {
    private final MeterRegistry meterRegistry;

    public TransactionMetrics(MeterRegistry meterRegistry) {
        this.meterRegistry = meterRegistry;
    }

    public void recordTransaction(Long userId, BigDecimal amount, int type) {
        Counter.builder("gzang.transaction.total")
            .tag("type", type == 1 ? "income" : "expense")
            .register(meterRegistry)
            .increment();

        DistributionSummary.builder("gzang.transaction.amount")
            .tag("type", type == 1 ? "income" : "expense")
            .register(meterRegistry)
            .record(amount.doubleValue());
    }
}
```

### 9.3 日志规范

| 级别 | 使用场景 | 示例 |
|------|---------|------|
| ERROR | 异常影响业务，必须关注 | `log.error("Transaction failed: {}", e.getMessage(), e)` |
| WARN | 潜在问题，需关注 | `log.warn("Rate limit exceeded for user: {}", userId)` |
| INFO | 关键业务节点 | `log.info("User {} logged in successfully", username)` |
| DEBUG | 开发调试，生产关闭 | `log.debug("SQL: {}", sql)` |

> **规范**：禁止在日志中记录密码、Token、身份证号、银行卡号等敏感信息。

---

## 10. 弹性设计

### 10.1 缓存策略

| 缓存级别 | 工具 | 适用场景 | TTL |
|---------|------|---------|-----|
| 本地缓存 | Caffeine | 热点数据、配置数据 | 5-30 分钟 |
| 分布式缓存 | Redis | 跨 JVM 共享、会话数据 | 1-24 小时 |
| 多级组合 | Caffeine + Redis | 热点+共享组合 | 本地 5min + Redis 1h |

```java
// ✅ 多级缓存配置
@Bean
public CacheManager cacheManager(RedisConnectionFactory factory,
                                  CacheManagerBuilder.StatsCachingStatsReader stats) {
    CaffeineCacheManager caffeine = new CaffeineCacheManager("user", "permission");
    caffeine.setCaffeine(Caffeine.newBuilder()
        .maximumSize(10_000)
        .expireAfterWrite(Duration.ofMinutes(5))
        .recordStats());

    RedisCacheManager redis = RedisCacheManager.builder(factory)
        .cacheDefaults(RedisCacheConfiguration.defaultCacheConfig()
            .entryTtl(Duration.ofHours(1)))
        .build();

    return new CompositeCacheManager(caffeine, redis);
}
```

### 10.2 异步处理

| 场景 | 实现方式 | 说明 |
|------|---------|------|
| 异步日志 | `@Async` + 线程池 | 不阻塞主线程 |
| 消息队列 | RabbitMQ / Kafka | 解耦、削峰 |
| 批量操作 | 分批 + 线程池 | 避免 OOM |

```java
// ✅ 异步任务线程池配置
@Bean("taskExecutor")
public Executor taskExecutor() {
    ThreadPoolTaskExecutor executor = new ThreadPoolTaskExecutor();
    executor.setCorePoolSize(10);
    executor.setMaxPoolSize(50);
    executor.setQueueCapacity(200);
    executor.setThreadNamePrefix("gzang-async-");
    executor.setRejectedExecutionHandler(new ThreadPoolExecutor.CallerRunsPolicy());
    executor.initialize();
    return executor;
}

// ✅ 使用 @Async
@Async("taskExecutor")
public void sendNotification(Long userId, String message) {
    // 异步发送通知，不阻塞主流程
}
```

### 10.3 熔断降级

```java
// ✅ Resilience4j 熔断器配置
@Configuration
public class Resilience4jConfig {
    @Bean
    public CircuitBreakerRegistry circuitBreakerRegistry() {
        CircuitBreakerConfig config = CircuitBreakerConfig.custom()
            .failureRateThreshold(50)          // 50% 失败率触发熔断
            .waitDurationInOpenState(Duration.ofSeconds(30))  // 熔断持续 30 秒
            .slidingWindowType(SlidingWindowType.COUNT_BASED)
            .slidingWindowSize(10)             // 统计最近 10 次调用
            .minimumNumberOfCalls(5)            // 最少 5 次调用才计算失败率
            .build();
        return CircuitBreakerRegistry.of(config);
    }
}

// ✅ 熔断器降级处理
@CircuitBreaker(name = "externalApi", fallbackMethod = "fallback")
public String callExternalApi(Long id) {
    return restTemplate.getForObject("/api/external/" + id, String.class);
}

public String fallback(Long id, Exception e) {
    log.warn("External API call failed, returning cached data: {}", e.getMessage());
    return cacheService.get("external_api_" + id); // 返回缓存或默认值
}
```

---

**文档版本**：1.0.0
**最后更新**：2026-03-27
**维护人员**：后端架构师
**关联规则**：`backend.mdc`、`security.mdc`、`database.mdc`
