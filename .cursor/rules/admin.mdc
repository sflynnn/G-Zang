---
alwaysApply: false
filePathPatterns:
  - "apps/pc-admin/**"
  - "**/admin/**"
framework: "Vue 3 + TypeScript"
description: "G-Zang 管理端开发规范"
---

# G-Zang 管理端 (Admin) MDC 规则

## 0. 设计哲学遵循

### "智简·归藏" 管理端实现
管理端遵循G-Zang的"Trust/Professionalism" (信任/专业性)理念，作为企业级后台管理界面：
- **Container隐喻**：数据表格和管理面板作为系统配置的"存储容器"
- **Flow隐喻**：清晰的管理流程和数据处理流
- **企业专业**：深色侧边栏、专业配色、有序的数据展示

## 1. 应用概述

### 1.1 应用定位
管理端是G-Zang财务管理系统的后台管理界面，主要面向系统管理员、公司管理员等角色，提供用户管理、权限控制、系统配置、数据统计等功能。

### 1.2 核心功能
- **用户管理**：用户创建、编辑、禁用、角色分配
- **权限管理**：角色定义、权限分配、访问控制
- **公司管理**：企业信息维护、管理员设置
- **系统配置**：参数设置、功能开关
- **数据统计**：用户数据、交易统计、系统监控
- **操作日志**：管理员操作审计、安全监控

## 2. 技术栈规范

### 2.1 基础技术栈
```json
{
  "框架": "Vue 3.0+ (Composition API)",
  "构建工具": "Vite 4.x",
  "样式框架": "Tailwind CSS (优先级最高 - 所有样式均使用Tailwind)",
  "UI组件库": "Naive UI (与Tailwind CSS结合使用)",
  "图标库": "Iconify / Lucide-vue",
  "状态管理": "Pinia",
  "路由": "Vue Router 4",
  "HTTP客户端": "Axios (通过packages/shared)",
  "类型系统": "TypeScript 5.x",
  "图表库": "ECharts (必须使用定义的颜色调色板)",
  "表格组件": "Naive UI DataTable + Tailwind样式",
  "表单组件": "Naive UI Form + Tailwind样式",
  "字体系统": {
    "UI界面": "Inter",
    "中文文本": "PingFang SC",
    "数字/统计": "DIN Alternate / Roboto Mono (必须使用font-mono)"
  },
  "代码规范": "ESLint + Prettier"
}
```

### 2.2 设计系统实现 (管理端)

#### 2.2.1 管理端颜色调色板
```javascript
// tailwind.config.js - 管理端配置
module.exports = {
  theme: {
    extend: {
      colors: {
        primary: '#0F4C5C',      // 归藏青 - 侧边栏背景、标题文本
        secondary: '#FB8B24',    // 琉璃金 - CTA按钮、重要操作
        success: '#06D6A0',      // 利润绿 - 成功状态、统计图表
        danger: '#EF476F',       // 支出红 - 错误状态、警告信息
        background: '#F8F9FA',   // 浅灰背景 - 主内容区背景
        surface: '#FFFFFF',      // 白色 - 卡片、表格背景
      }
    }
  }
}
```

#### 2.2.2 管理端布局模式
- **侧边栏**：固定深色设计 `bg-primary`，可折叠
- **主内容区**：白色背景，网格布局数据表格
- **表格样式**：条纹行 `even:bg-slate-50`，货币列右对齐
- **导航样式**：专业深色主题，清晰的层级结构

### 2.2 管理端特性技术栈
```json
{
  "表格组件": "Naive UI DataTable (高级筛选、分页、排序)",
  "表单组件": "Naive UI Form (动态表单、验证)",
  "图表可视化": "ECharts (统计图表、仪表板)",
  "富文本编辑器": "TinyMCE 或 Quill (系统公告)",
  "文件上传": "Naive UI Upload (批量上传、进度显示)",
  "权限指令": "Vue自定义指令 (v-permission, v-role)"
}
```

## 3. 项目结构规范

### 3.1 目录结构
```
apps/pc-admin/
├── src/
│   ├── api/                    # API接口层 (通过packages/shared统一管理)
│   ├── assets/                 # 静态资源文件
│   │   ├── icons/             # 图标资源
│   │   └── images/            # 图片资源
│   ├── components/            # Vue组件目录
│   │   ├── common/            # 通用基础组件
│   │   │   ├── DataTable/     # 通用数据表格组件
│   │   │   ├── FormModal/     # 通用表单弹窗组件
│   │   │   ├── SearchForm/    # 通用搜索表单组件
│   │   │   └── StatisticCard/ # 统计卡片组件
│   │   ├── business/          # 业务功能组件
│   │   │   ├── UserManager/   # 用户管理业务组件
│   │   │   ├── RoleManager/   # 角色管理业务组件
│   │   │   ├── PermissionManager/ # 权限管理业务组件
│   │   │   └── SystemConfig/  # 系统配置业务组件
│   │   └── layout/            # 页面布局组件
│   │       ├── AdminLayout/   # 管理端主布局组件
│   │       ├── Sidebar/       # 侧边栏导航组件
│   │       └── Header/        # 顶部导航栏组件
│   ├── composables/           # Vue 3组合式函数
│   │   ├── useUserManager.ts  # 用户管理业务逻辑
│   │   ├── useRoleManager.ts  # 角色管理业务逻辑
│   │   ├── useDataTable.ts    # 数据表格通用逻辑
│   │   └── useChart.ts        # 图表组件逻辑
│   ├── router/                # 路由配置目录
│   │   ├── routes/            # 路由定义文件
│   │   │   ├── user.ts        # 用户管理相关路由
│   │   │   ├── system.ts      # 系统管理相关路由
│   │   │   └── dashboard.ts   # 仪表板相关路由
│   │   └── guards.ts          # 路由守卫和权限验证
│   ├── stores/                # Pinia状态管理
│   │   ├── modules/           # 按业务模块组织
│   │   │   ├── user.ts        # 用户状态管理
│   │   │   ├── role.ts        # 角色状态管理
│   │   │   ├── permission.ts  # 权限状态管理
│   │   │   └── system.ts      # 系统状态管理
│   │   └── index.ts           # 状态管理导出配置
│   ├── types/                 # TypeScript类型定义
│   │   ├── user.ts            # 用户相关类型接口
│   │   ├── role.ts            # 角色相关类型接口
│   │   └── system.ts          # 系统相关类型接口
│   ├── utils/                 # 工具函数库
│   │   ├── permission.ts      # 权限验证工具函数
│   │   ├── validation.ts      # 数据验证工具函数
│   │   └── export.ts          # 数据导出工具函数
│   ├── views/                 # 页面视图组件
│   │   ├── dashboard/         # 仪表板页面目录
│   │   │   ├── index.vue      # 主仪表板页面
│   │   │   └── components/    # 仪表板专用组件
│   │   ├── user/              # 用户管理页面目录
│   │   │   ├── index.vue      # 用户列表页面
│   │   │   ├── create.vue     # 用户创建页面
│   │   │   ├── edit.vue       # 用户编辑页面
│   │   │   └── detail.vue     # 用户详情页面
│   │   ├── role/              # 角色管理页面
│   │   ├── permission/        # 权限管理页面
│   │   ├── system/            # 系统管理页面
│   │   └── profile/           # 用户个人资料页面
│   ├── constants/             # 常量和配置定义
│   │   ├── routes.ts          # 路由常量配置
│   │   ├── permissions.ts     # 权限常量定义
│   │   └── config.ts          # 系统配置常量
│   └── locales/               # 国际化语言包
│       ├── zh-CN/             # 中文简体语言包
│       └── en-US/             # 英文语言包
├── public/                    # 公共静态资源
├── tests/                     # 单元测试和集成测试
└── types/                     # 全局TypeScript类型定义
```

## 4. 组件设计规范

### 4.1 通用组件规范

#### 4.1.1 数据表格组件
- 使用 Naive UI 的 n-data-table 组件，配置分页、排序、筛选等功能
- 支持列宽调整、固定列、自定义渲染等高级特性
- 统一的表格样式和交互行为，确保用户体验一致性

#### 4.1.2 表单弹窗组件
- 使用 Naive UI 的 n-modal 和 n-form 组件，封装通用的表单弹窗功能
- 支持动态表单配置、字段验证、表单提交等特性
- 统一的弹窗样式和操作流程

#### 4.1.3 搜索表单组件
- 提供统一的搜索表单组件，支持多条件组合查询
- 支持高级搜索面板和快捷搜索
- 集成表单重置和搜索历史功能

### 4.2 业务组件规范

#### 4.2.1 用户管理组件
- 集成搜索、列表展示、增删改查等功能的完整用户管理页面组件
- 支持批量操作、数据导出、用户状态管理等高级功能
- 集成权限控制，确保操作安全性

#### 4.2.2 角色管理组件
- 提供角色创建、编辑、权限分配等功能
- 支持角色继承和权限模板配置
- 提供角色用户关联管理界面

#### 4.2.3 系统配置组件
- 提供系统参数配置、功能开关等管理功能
- 支持配置版本管理和回滚操作
- 提供配置生效状态监控

## 5. 权限控制规范

### 5.1 路由权限控制
- 基于用户角色和权限配置，实现路由级别的访问控制
- 使用路由守卫进行权限验证，无权限时重定向到403页面
- 支持动态路由，根据用户权限动态添加路由配置

### 5.2 组件权限控制
- 提供权限检查工具函数，支持细粒度的权限验证
- 实现Vue自定义指令 `v-permission` 和 `v-role`，控制组件显示
- 支持按钮级别、菜单级别的权限控制

### 5.3 权限配置管理
- 定义系统权限常量，按功能模块组织权限标识
- 支持角色继承和权限组合，实现灵活的权限管理
- 提供权限缓存机制，提升系统性能

## 6. 数据可视化规范

### 6.1 仪表板设计
- 集成统计卡片、趋势图表、数据表格的管理端仪表板页面
- 支持实时数据更新和自定义时间范围筛选
- 提供多种图表类型：柱状图、折线图、饼图、仪表盘等
- 支持数据导出和仪表板布局自定义

### 6.2 图表组件规范
- 使用ECharts作为统一图表库，确保图表样式和交互一致性
- 定义标准的图表配色方案和主题样式
- 支持图表响应式布局和移动端适配

## 7. 代码生成规范

### 7.1 管理页面生成规范
生成管理端页面时，请明确指定以下信息：

#### 7.1.1 基础信息
- **页面类型**：列表页、表单页、详情页、仪表板
- **数据模型**：实体名称、字段定义、关联关系
- **路由配置**：页面路径、路由名称、路由参数

#### 7.1.2 功能特性
- **数据操作**：增删改查、批量操作、数据导入导出
- **查询筛选**：基础查询、高级筛选、快捷筛选
- **权限控制**：页面访问权限、操作权限、字段级权限
- **其他特性**：分页、排序、数据缓存、离线支持

#### 7.1.3 设计系统要求
- **布局模式**：固定侧边栏 + 主内容网格布局
- **配色方案**：深色侧边栏(primary)，白色主内容区
- **组件样式**：数据表格使用条纹行，统计数字使用等宽字体
- **响应式设计**：使用Tailwind前缀确保桌面端优先适配

#### 7.1.4 界面配置
- **布局方式**：单列布局、多列布局、标签页布局
- **组件选用**：推荐使用的通用组件和业务组件
- **样式要求**：特殊的样式需求和主题适配

### 7.2 组件复用原则
管理端应充分利用以下通用组件，提高开发效率和维护性：
- **DataTable**：统一的数据表格组件，支持分页、排序、筛选、导出
- **FormModal**：统一的表单弹窗组件，支持动态表单和验证
- **SearchForm**：统一的搜索表单组件，支持多条件组合查询
- **StatisticCard**：统一的统计卡片组件，支持数据可视化展示

## 8. 国际化与主题规范

### 8.1 国际化规范

#### 8.1.1 语言包组织
- 按功能模块组织的多语言文件，支持热重载和按需加载
- 支持中文简体、中文繁体、英文等主要语言
- 提供语言包校验机制，确保翻译完整性

#### 8.1.2 管理端专用语言包
- 按功能模块组织的管理端专用文本，包括菜单、按钮、提示信息等
- 支持多语言切换，提供语言切换组件
- 集成用户语言偏好设置和自动语言检测

#### 8.1.3 日期时间本地化
- 统一的日期时间格式化处理
- 支持时区设置和本地化显示
- 提供日期选择器和时间选择器的国际化支持

### 8.2 主题系统规范

#### 8.2.1 主题变量定义
- 定义管理端专用的CSS变量系统，支持主题切换和组件样式统一
- 提供亮色主题和深色主题的完整变量集合
- 支持主题扩展和自定义主题开发

#### 8.2.2 深色主题适配
- 提供深色主题下的完整变量覆盖，确保在深色模式下有良好的视觉效果
- 支持系统主题自动跟随和手动主题切换
- 优化深色模式下的对比度和可读性

#### 8.2.3 全局主题配置

##### 8.2.3.1 应用级配置
- 在App.vue中配置Naive UI的全局主题和语言设置
- 支持主题和语言的运行时切换
- 提供主题配置的持久化存储

#### 8.3 组件主题适配

##### 8.3.1 数据表格主题适配
- 为数据表格组件应用管理端主题变量，确保样式统一
- 支持表格的亮色和深色主题切换
- 优化表格的交互状态样式（悬停、选中等）

##### 8.3.2 表单组件主题适配
- 为表单组件提供统一的主题样式
- 支持表单验证状态的视觉反馈
- 优化表单组件在不同主题下的显示效果

##### 8.3.3 导航组件主题适配
- 为侧边栏和顶部导航提供主题适配
- 支持导航组件的展开收起状态样式
- 优化导航菜单的激活状态显示

**文档版本**：1.1.0
**最后更新**：2026-01-18
**维护人员**：前端开发团队