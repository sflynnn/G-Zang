---
alwaysApply: false
filePathPatterns:
  - "apps/mobile/**"
framework: "uni-app + Vue 3 + TypeScript"
description: "G-Zang 移动端开发规范"
---

# G-Zang 移动端 (Mobile) MDC 规则

## 0. 设计哲学遵循

### "智简·归藏" 移动端实现

移动端遵循 G-Zang 的"Simplicity/Fun"（简洁/有趣）理念，强化个人场景体验：

- **Container 隐喻**：移动卡片作为便携的财富存储容器
- **Flow 隐喻**：触手可及的快速记账流程
- **移动优先**：单手操作、快速录入、离线优先

---

## 1. 应用概述

### 1.1 应用定位

移动端是 G-Zang 财务管理系统的移动应用，基于 uni-app 框架开发。专注于移动场景下的轻量化记账体验，支持离线记账、语音输入、拍照识别、智能同步等功能，为用户提供随时随地的财务管理服务。

### 1.2 核心功能

- **快速记账**：一键记账，支持语音、拍照、手动输入
- **离线同步**：网络恢复时自动同步离线数据
- **智能识别**：语音记账、图片 OCR、短信导入
- **数据查看**：账本查看、统计图表、预算提醒
- **设备绑定**：支持多设备数据同步
- **生物识别**：指纹/面部解锁，安全快捷

---

## 2. 技术栈规范

### 2.1 基础技术栈

基于 uni-app 框架，使用 Vue 3 + TypeScript 技术栈，提供跨平台移动应用开发能力。

### 2.2 移动端特性技术栈

集成语音识别、图片 OCR、生物识别等移动端特色功能，提供丰富的用户交互体验。

### 2.3 样式规范

- **单位**：使用 rpx 单位实现响应式设计
- **条件编译**：针对 iOS/Android 进行平台差异化适配
- **设计系统**：严格遵循 G-Zang 品牌色（primary: #0F4C5C, secondary: #FB8B24, success: #06D6A0, danger: #EF476F）
- **数字字体**：财务数字使用等宽字体确保对齐

---

## 3. 项目结构规范

### 3.1 uni-app 项目结构

```
apps/mobile/
├── src/
│   ├── api/                    # API接口 (引用 packages/shared)
│   ├── components/            # 组件目录
│   │   ├── common/            # 通用组件
│   │   │   ├── AmountKeyboard/  # 金额键盘组件
│   │   │   ├── VoiceRecorder/  # 语音录制组件
│   │   │   ├── CameraCapture/   # 相机拍摄组件
│   │   │   ├── OfflineIndicator/ # 离线状态指示器
│   │   │   ├── SyncStatus/      # 同步状态组件
│   │   │   └── LoadingSpinner/  # 加载动画
│   │   └── business/          # 业务组件
│   │       ├── QuickRecord/     # 快速记账组件
│   │       ├── TransactionList/ # 交易列表组件
│   │       ├── AccountSelector/ # 账户选择组件
│   │       ├── CategoryGrid/   # 分类网格组件
│   │       └── BudgetCard/     # 预算卡片组件
│   ├── composables/           # 组合式函数
│   │   ├── useVoiceRecord.ts   # 语音录制 hooks
│   │   ├── useOCR.ts          # OCR 识别 hooks
│   │   ├── useOfflineSync.ts  # 离线同步 hooks
│   │   ├── useBiometric.ts    # 生物识别 hooks
│   │   ├── useDevice.ts      # 设备信息 hooks
│   │   └── usePush.ts        # 推送通知 hooks
│   ├── pages/                # 页面目录
│   │   ├── index/            # 首页
│   │   │   ├── index.vue     # 首页页面
│   │   │   └── components/  # 首页组件
│   │   ├── accounting/       # 记账页面
│   │   │   ├── index.vue     # 记账首页
│   │   │   ├── quick.vue    # 快速记账
│   │   │   ├── voice.vue    # 语音记账
│   │   │   ├── camera.vue   # 拍照记账
│   │   │   └── manual.vue   # 手动记账
│   │   ├── transactions/     # 交易管理页面
│   │   │   ├── index.vue    # 交易列表
│   │   │   ├── detail.vue   # 交易详情
│   │   │   └── edit.vue     # 交易编辑
│   │   ├── analysis/        # 财务分析页面
│   │   │   ├── index.vue   # 分析首页
│   │   │   ├── chart.vue   # 图表分析
│   │   │   └── report.vue  # 财务报告
│   │   ├── accounts/        # 账户管理页面
│   │   ├── categories/       # 分类管理页面
│   │   ├── budget/          # 预算管理页面
│   │   ├── profile/         # 个人资料页面
│   │   └── settings/        # 设置页面
│   ├── static/              # 静态资源
│   │   ├── icons/          # 图标资源
│   │   ├── images/         # 图片资源
│   │   └── audio/          # 音频资源
│   ├── stores/              # 状态管理
│   │   ├── modules/        # 按模块组织
│   │   │   ├── user.ts    # 用户状态
│   │   │   ├── transaction.ts # 交易状态
│   │   │   ├── account.ts  # 账户状态
│   │   │   ├── category.ts # 分类状态
│   │   │   └── app.ts     # 应用状态
│   │   └── index.ts       # 导出配置
│   ├── types/               # TypeScript 类型定义
│   │   ├── transaction.ts  # 交易类型
│   │   ├── account.ts      # 账户类型
│   │   └── device.ts      # 设备类型
│   ├── utils/               # 工具函数
│   │   ├── finance.ts     # 财务计算工具
│   │   ├── format.ts      # 格式化工具
│   │   ├── storage.ts      # 本地存储工具
│   │   ├── device.ts      # 设备工具
│   │   └── permission.ts  # 权限工具
│   ├── App.vue              # 应用入口
│   ├── main.ts             # 主入口文件
│   ├── manifest.json        # 应用配置
│   ├── pages.json          # 页面路由配置
│   └── uni.scss            # 全局样式
├── unpackage/              # 编译输出目录
├── dist/                   # 生产构建目录
├── tests/                  # 测试文件
└── types/                 # 全局类型定义
```

---

## 4. 核心功能组件规范

### 4.1 快速记账组件

提供一键记账、语音输入、拍照识别、手动输入等多种记账方式，支持支出、收入、转账三种类型。

### 4.2 语音记账组件

支持语音录制和智能识别，自动解析交易金额、分类、商家等信息，提供便捷的语音记账体验。

### 4.3 离线同步组件

实现网络恢复时的自动数据同步，支持失败重试和同步状态显示，确保离线操作数据的完整性。

---

## 5. 移动端适配规范

### 5.1 页面路由配置

使用 pages.json 进行路由配置，支持页面参数传递和导航守卫。

### 5.2 移动端样式规范

使用 rpx 单位和条件编译，确保在不同设备上的一致性显示效果。

---

## 6. 代码生成规范

### 6.1 移动端页面生成

生成移动端页面时，请明确指定：

- **页面类型**：首页、列表页、表单页、详情页、设置页
- **功能特性**：下拉刷新、上拉加载、分享、生物识别
- **数据交互**：离线支持、实时同步、缓存策略
- **UI 组件**：Vant、Tailwind CSS 样式、自定义组件

#### 6.1.1 设计系统要求

- **Magic Button**：底部导航栏中央的一键记账入口，琉璃金配色（#FB8B24），圆形浮动设计
- **卡片样式**：白色背景、rounded-2xl 圆角、柔和阴影
- **配色方案**：严格使用 G-Zang 定义的颜色调色板
- **字体规范**：数字使用等宽字体确保对齐

### 6.2 uni-app 组件规范

遵循 uni-app 组件开发规范，确保跨平台兼容性和性能优化。结合 Tailwind CSS 实现统一的视觉设计。

---

## 7. 国际化与主题规范

### 7.1 移动端国际化要求

支持多语言切换，适配移动端用户的语言偏好。

### 7.2 移动端主题定制

提供浅色、深色主题切换，支持系统主题自动跟随。

---

**文档版本**：1.1.0
**最后更新**：2026-01-18
**维护人员**：前端开发团队
