---
alwaysApply: false
filePathPatterns:
  - "!apps/pc-admin/**"
  - "!apps/pc-main/**"
  - "!apps/pc-personal/**"
  - "!apps/pc-business/**"
  - "!apps/mobile/**"
  - "!server/**"
---

# G-Zang (归藏) Cursor MDC 通用规范

## 1. 总体原则

- **严格遵循设计文档**：所有代码生成必须严格遵守文档中定义的架构、数据模型和接口规范。
- **TypeScript 优先**：前端代码必须使用 TypeScript，确保类型安全。
- **代码可读性与维护性**：生成的代码应结构清晰、命名规范、注释得当。
- **性能与安全**：在代码生成时，需考虑性能优化和安全防护。
- **多租户架构**：系统采用多租户设计，用户级和企业级数据严格隔离。

## 2. 前端开发规范 (Vue3 + TypeScript)

### 2.1 技术栈选型
- **框架**：Vue 3.0+ (Composition API)
- **构建工具**：Vite 4.x
- **样式框架**：Tailwind CSS (PC端优先级最高，所有样式均使用Tailwind实现)；SCSS仅用于Tailwind无法覆盖的高级场景
- **状态管理**：Pinia
- **路由**：Vue Router 4
- **HTTP 客户端**：Axios
- **类型系统**：TypeScript 5.x
- **代码规范**：ESLint + Prettier
- **微前端**：qiankun 3.x
- **UI 组件库**：
    - PC 端 (Admin/PC-Main/PC-Personal/PC-Business)：Naive UI
    - 移动端 (Mobile)：Vant 4.x
- **国际化**：Vue I18n 9.x
- **主题系统**：CSS变量 + 动态主题切换
- **测试框架**：Jest + Vue Test Utils

### 2.2 项目结构 (Monorepo)
```
G-Zang/
├── apps/                          # 应用目录
│   ├── pc-admin/                  # 管理端 (Naive UI)
│   │   ├── src/
│   │   │   ├── api/              # API接口
│   │   │   ├── assets/           # 静态资源
│   │   │   ├── components/       # 组件目录
│   │   │   │   ├── common/       # 通用组件
│   │   │   │   ├── business/     # 业务组件
│   │   │   │   └── layout/       # 布局组件
│   │   │   ├── composables/      # 组合式函数
│   │   │   ├── router/           # 路由配置
│   │   │   ├── stores/           # 状态管理
│   │   │   ├── utils/            # 工具函数
│   │   │   ├── views/            # 页面组件
│   │   │   ├── types/            # TypeScript类型
│   │   │   ├── constants/        # 常量定义
│   │   │   ├── locales/          # 国际化
│   │   │   │   ├── zh-CN/        # 中文语言包
│   │   │   │   │   ├── common.ts # 通用文本
│   │   │   │   │   ├── menu.ts   # 菜单文本
│   │   │   │   │   └── message.ts # 消息文本
│   │   │   │   └── en-US/        # 英文语言包
│   │   │   │       ├── common.ts
│   │   │   │       ├── menu.ts
│   │   │   │       └── message.ts
│   │   │   ├── themes/           # 主题配置
│   │   │   │   ├── light.ts      # 浅色主题
│   │   │   │   ├── dark.ts       # 深色主题
│   │   │   │   └── index.ts      # 主题导出
│   │   │   └── index.ts          # 国际化配置
│   │   ├── public/
│   │   ├── index.html
│   │   ├── vite.config.ts
│   │   └── package.json
│   ├── pc-main/                  # PC主应用 (qiankun)
│   ├── pc-personal/              # 个人记账子应用
│   └── mobile/                   # 移动端应用 (Vant)
├── packages/                     # 共享包
│   └── shared/                   # 跨端共享模块
│       ├── src/
│       │   ├── api/              # API封装
│       │   ├── types/            # 全局类型
│       │   ├── utils/            # 工具函数
│       │   └── components/       # 共享组件
│       └── package.json
├── tools/                        # 构建工具
├── docs/                         # 项目文档
└── package.json                  # 根配置 (pnpm workspace)
```

### 2.3 命名规范

#### 2.3.1 组件命名
- **组件文件**：PascalCase (e.g., `UserList.vue`, `AccountCard.vue`)
- **组件引用**：camelCase (e.g., `userList`, `accountCard`)

#### 2.3.2 文件命名
- **页面组件**：kebab-case (e.g., `user-list.vue`, `account-detail.vue`)
- **工具文件**：camelCase + 后缀 (e.g., `dateUtils.ts`, `formatters.ts`)
- **类型文件**：PascalCase + `.type.ts` (e.g., `User.type.ts`)

#### 2.3.3 变量命名
- **常量**：UPPER_SNAKE_CASE (e.g., `API_BASE_URL`)
- **变量/函数**：camelCase (e.g., `userList`, `getUserById()`)
- **类型**：PascalCase (e.g., `User`, `ApiResponse<T>`)

### 2.4 Vue 组件规范

#### 2.4.1 组件结构
使用 Vue 3 Composition API + `<script setup>` 语法糖，组件应包含模板、脚本和样式三部分。

#### 2.4.2 Composition API 最佳实践
- **Props定义**：使用 `defineProps<Props>()` 替代 `props: Props[]`
- **事件定义**：使用 `defineEmits<Emits>()` 替代 `emits: string[]`
- **响应式数据**：优先使用 `ref()`，复杂对象使用 `reactive()`
- **组合式函数**：按功能拆分，提高代码复用性

### 2.5 状态管理 (Pinia)

#### 2.5.1 Store 设计模式
使用 Pinia 的 Composition API 模式，每个 Store 应包含状态、计算属性和动作。

#### 2.5.2 Store 组织结构
按业务模块组织Store文件，每个模块独立管理相关状态。

### 2.6 API 封装规范

#### 2.6.1 统一 API 客户端
使用 Axios 作为 HTTP 客户端，封装统一的请求/响应拦截器和错误处理。

#### 2.6.2 模块化 API 封装
按业务模块组织API接口，提供统一的请求封装和错误处理。

### 2.7 微前端规范 (qiankun)

#### 2.7.1 主应用配置
使用 qiankun 框架实现微前端架构，主应用负责子应用的注册、加载和生命周期管理。

#### 2.7.2 子应用配置
子应用需要实现qiankun要求的生命周期钩子函数，支持独立运行和集成运行。

### 2.8 国际化与主题规范

#### 2.8.1 国际化 (i18n) 规范

##### 国际化配置
使用 Vue I18n 实现国际化支持，配置语言包和语言切换逻辑。

##### 语言包结构
按功能模块组织语言包，支持嵌套结构和动态加载。

##### 组件中使用国际化
在组件中使用 `$t()` 函数进行文本翻译，支持动态参数和嵌套键。

##### 语言切换功能
提供语言切换组件，支持用户手动切换和系统语言检测。

#### 2.8.2 主题系统规范

##### Tailwind CSS 变量主题系统
使用 Tailwind CSS 配置和 CSS 变量定义完整的主题系统，包括：
- 颜色系统 (品牌色、功能色、中性色) - 在 `tailwind.config.js` 中精确扩展十六进制颜色代码
- 字体系统 (字体族、字号、字重)
- 间距系统 (spacing scale)
- 圆角系统 (border-radius scale)
- 阴影系统 (box-shadow scale)
- 动画系统 (transition timing)

**G-Zang 品牌色（所有应用必须严格使用）**：
- `primary: '#0F4C5C'` — 归藏青，用于标题、侧边栏、主文本
- `secondary: '#FB8B24'` — 琉璃金，用于 CTA、高亮、VIP 图标
- `success: '#06D6A0'` — 利润绿，用于成功状态、统计图表
- `danger: '#EF476F'` — 支出红，用于错误状态、警告信息
- `background: '#F8F9FA'` — 浅灰背景
- `surface: '#FFFFFF'` — 卡片背景

支持浅色、深色和自动主题切换，所有组件样式必须基于 Tailwind 类或 CSS 变量构建。

##### 主题管理器 (Naive UI 配置)
使用组合式函数管理主题状态，支持主题切换和系统主题监听。

##### 主题切换组件 (Naive UI)
提供主题切换下拉菜单，支持用户手动选择主题模式。

#### 2.8.3 应用集成 (Naive UI配置)

##### 应用初始化配置
在应用启动时配置主题、国际化和其他全局设置。

##### 根组件配置 (推荐方式)
使用 NConfigProvider 包装根组件，实现主题和语言的全局配置。

## 3. 后端开发规范 (Java + Spring Boot)

### 3.1 技术栈选型
- **语言**：Java 17+
- **框架**：Spring Boot 3.1.x
- **ORM**：MyBatis Plus 3.5.x
- **安全框架**：Spring Security 6.x + JWT
- **数据库**：MySQL 8.0
- **缓存**：Redis 7.x
- **API文档**：SpringDoc OpenAPI 2.x
- **连接池**：Druid 1.2.x
- **工具库**：Lombok, Apache Commons, Guava
- **测试框架**：JUnit 5 + Mockito

### 3.2 项目结构
采用分层架构设计，包括Controller、Service、Mapper、Entity等层次。

### 3.3 命名规范

#### 3.3.1 类命名
- **控制器类**：`XxxController` (e.g., `UserController`)
- **服务接口**：`XxxService` (e.g., `UserService`)
- **服务实现**：`XxxServiceImpl` (e.g., `UserServiceImpl`)
- **实体类**：`Xxx` (e.g., `User`, `Transaction`)
- **DTO类**：`XxxDTO` (e.g., `CreateUserDTO`)
- **VO类**：`XxxVO` (e.g., `UserVO`)
- **异常类**：`XxxException` (e.g., `BusinessException`)
- **工具类**：`XxxUtil` (e.g., `JwtUtil`)

#### 3.3.2 方法命名
- **查询方法**：`getXxx()`, `findXxx()`, `selectXxx()`
- **修改方法**：`updateXxx()`, `saveXxx()`, `modifyXxx()`
- **删除方法**：`deleteXxx()`, `removeXxx()`
- **业务方法**：动词开头，描述具体业务 (e.g., `createTransaction()`)

### 3.4 实体类设计

#### 3.4.1 基础实体类
定义包含基础字段的实体基类，如ID、创建时间、更新时间等。

#### 3.4.2 业务实体类
业务实体类继承基础实体类，包含具体的业务字段和关联关系。

### 3.5 多租户实现

#### 3.5.1 租户上下文
使用ThreadLocal实现租户上下文，确保每个请求的租户信息正确传递。

#### 3.5.2 MyBatis拦截器
通过MyBatis拦截器在SQL执行时自动注入租户条件，实现数据隔离。

### 3.6 API 接口规范

#### 3.6.1 RESTful 设计
遵循RESTful设计原则，提供标准化的HTTP接口。

#### 3.6.2 统一响应对象
定义统一的API响应格式，包含状态码、消息和数据。

### 3.7 异常处理规范

#### 3.7.1 全局异常处理器
使用@ControllerAdvice实现全局异常处理，统一处理各类异常。

#### 3.7.2 自定义异常
根据业务需求定义特定的异常类型，便于异常分类处理。

## 4. 数据库设计规范

### 4.1 数据库选型
- **数据库**：MySQL 8.0+
- **字符集**：`utf8mb4`
- **存储引擎**：`InnoDB`
- **架构**：主从复制 (读写分离)

### 4.2 命名规范

#### 4.2.1 表命名
- **前缀**：`t_` (表示table)
- **命名规则**：小写字母 + 下划线
- **示例**：`t_user`, `t_transaction`, `t_company`

#### 4.2.2 字段命名
- **规则**：小写字母 + 下划线
- **示例**：`user_name`, `create_time`, `company_id`

#### 4.2.3 索引命名
- **主键索引**：`PRIMARY`
- **唯一索引**：`uk_表名_字段名`
- **普通索引**：`idx_表名_字段名`
- **复合索引**：`idx_表名_字段1_字段2`

### 4.3 表结构规范

#### 4.3.1 核心表结构
设计标准化的表结构，包含必要的审计字段和状态字段。

#### 4.3.2 多租户字段
- **user_id**：用户级数据隔离
- **company_id**：企业级数据隔离
- **NULL值处理**：个人数据 `company_id` 为NULL，企业数据 `user_id` 可为NULL

### 4.4 索引优化策略

#### 4.4.1 核心业务索引
为主要查询场景创建合适的索引，提高查询性能。

#### 4.4.2 复合索引优化
针对多条件查询场景，设计合理的复合索引。

## 5. 安全规范

### 5.1 身份认证
- **JWT Token**：无状态认证，包含用户身份和权限信息
- **Token过期**：访问令牌2小时，刷新令牌7天
- **密码加密**：BCrypt强哈希算法
- **登录限制**：密码重试次数限制

### 5.2 数据安全
- **敏感数据加密**：手机号、身份证等使用AES加密存储
- **数据脱敏**：接口返回数据自动脱敏处理
- **传输加密**：HTTPS强制加密传输
- **SQL注入防护**：使用预编译语句和参数绑定

### 5.3 访问控制
- **RBAC模型**：基于角色的访问控制
- **权限粒度**：支持到具体操作级别的权限控制
- **多租户隔离**：用户级和企业级数据严格隔离

### 5.4 安全监控
- **操作日志**：记录所有敏感操作
- **异常检测**：监控异常登录和操作行为
- **审计报告**：定期生成安全审计报告

## 6. 部署运维规范

### 6.1 容器化部署
- **Docker镜像**：多阶段构建，优化镜像大小
- **容器编排**：Kubernetes集群部署
- **配置管理**：环境变量和配置中心

### 6.2 高可用架构
- **负载均衡**：Nginx + Keepalived
- **数据库**：主从复制 + 读写分离
- **缓存**：Redis集群
- **监控告警**：Prometheus + Grafana

### 6.3 CI/CD流程
- **代码检查**：ESLint + SonarQube
- **自动化测试**：单元测试 + 集成测试
- **构建部署**：GitHub Actions自动部署
- **回滚策略**：快速回滚机制

## 7. Cursor 使用建议

### 7.1 代码生成规范
- **明确指令**：生成代码时必须明确指出模块、功能和技术栈
- **遵循规范**：严格按照本文档定义的规范生成代码
- **类型安全**：前端代码必须使用TypeScript，后端代码使用强类型
- **注释完整**：生成的代码要有适当的注释和文档

### 7.2 开发流程建议
- **逐步迭代**：先生成核心骨架，再逐步完善业务逻辑
- **测试驱动**：重要功能生成时考虑测试用例
- **性能优化**：生成代码时考虑性能优化点
- **安全考虑**：涉及敏感数据的功能要考虑安全防护

### 7.3 规范引用
在对话中引用本文档规范，例如：
- "请根据前端开发规范生成Vue3组合式API组件"
- "请根据后端开发规范生成Spring Boot Controller"
- "请根据数据库设计规范设计用户表结构"