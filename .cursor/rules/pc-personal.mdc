---
alwaysApply: false
filePathPatterns:
  - "apps/pc-personal/**"
framework: "Vue 3 + TypeScript"
description: "G-Zang PC个人记账子应用开发规范"
---

# G-Zang PC个人记账子应用 (PC-Personal) MDC 规则

## 0. 设计哲学遵循

### "智简·归藏" 个人端实现
个人记账子应用遵循G-Zang的"Simplicity/Fun" (简洁/有趣)理念：
- **Container隐喻**：资产卡片和账本作为个人财富的"存储容器"
- **Flow隐喻**：流畅的记账流程和数据可视化
- **个人场景 (Mobile First)**：干净布局、空旷空间、友好的用户体验

## 1. 应用概述

### 1.1 应用定位
PC个人记账子应用是G-Zang财务管理系统的个人用户核心功能模块，基于qiankun微前端架构运行。专注于个人财务管理，提供便捷的记账体验、账本管理、财务分析等功能。

### 1.2 核心功能
- **快速记账**：支持收入、支出记录，多账户管理
- **账本管理**：个人账本的创建、编辑、分类管理
- **财务分析**：收支统计、趋势分析、预算控制
- **智能识别**：语音记账、图片OCR识别
- **数据导出**：财务数据导出和备份
- **多设备同步**：支持离线记账和数据同步

## 2. 技术栈规范

### 2.1 基础技术栈
```json
{
  "框架": "Vue 3.0+ (Composition API)",
  "构建工具": "Vite 4.x",
  "样式框架": "Tailwind CSS (优先级最高 - 所有样式均使用Tailwind)",
  "UI组件库": "Naive UI (与Tailwind CSS结合使用)",
  "图标库": "Iconify / Lucide-vue",
  "状态管理": "Pinia",
  "路由": "Vue Router 4",
  "HTTP客户端": "Axios (通过packages/shared)",
  "类型系统": "TypeScript 5.x",
  "图表库": "ECharts (必须使用定义的颜色调色板)",
  "日期处理": "Day.js",
  "数字处理": "Decimal.js",
  "字体系统": {
    "UI界面": "Inter",
    "中文文本": "PingFang SC",
    "数字/货币": "DIN Alternate / Roboto Mono (必须使用font-mono)"
  },
  "代码规范": "ESLint + Prettier"
}
```

### 2.2 设计系统实现 (个人端)

#### 2.2.1 颜色调色板严格使用
```javascript
// tailwind.config.js - 个人端配置
module.exports = {
  theme: {
    extend: {
      colors: {
        primary: '#0F4C5C',      // 归藏青 - 标题、重要文本
        secondary: '#FB8B24',    // 琉璃金 - CTA按钮、趣味元素
        success: '#06D6A0',      // 利润绿 - 收入显示、积极反馈
        danger: '#EF476F',       // 支出红 - 支出显示、警告
        background: '#F8F9FA',   // 浅灰背景 - 页面背景
        surface: '#FFFFFF',      // 白色 - 卡片、表单背景
      }
    }
  }
}
```

#### 2.2.2 个人端布局模式 (Mobile First)
- **顶部资产卡片**：大尺寸卡片，渐变背景 `bg-gradient-to-r from-primary to-[#186a7d]`
- **主体内容**：单列垂直滚动布局，空旷友好的间距
- **响应式设计**：使用Tailwind前缀 `sm:`, `md:`, `lg:` 实现移动优先
- **导航样式**：底部标签栏，中间突出Magic Button (移动端特色)

### 2.2 个人记账特性技术栈
```json
{
  "语音识别": "Web Speech API",
  "图片OCR": "第三方OCR服务",
  "离线存储": "IndexedDB + Service Worker",
  "数据同步": "WebSocket + 后台同步",
  "文件导出": "SheetJS (Excel导出)",
  "财务计算": "Decimal.js (精确计算)",
  "日期选择器": "Naive UI DatePicker",
  "快捷键支持": "自定义键盘事件"
}
```

## 3. 项目结构规范

### 3.1 目录结构
```
apps/pc-personal/
├── src/
│   ├── api/                    # API接口 (引用packages/shared)
│   ├── assets/                 # 静态资源
│   │   ├── icons/             # 图标资源
│   │   ├── images/            # 图片资源
│   │   └── audio/             # 音频资源
│   ├── components/            # 组件目录
│   │   ├── common/            # 通用组件
│   │   │   ├── AmountInput/   # 金额输入组件
│   │   │   ├── CategorySelect/ # 分类选择组件
│   │   │   ├── AccountSelect/ # 账户选择组件
│   │   │   ├── DatePicker/    # 日期选择组件
│   │   │   └── QuickRecord/   # 快速记账组件
│   │   ├── business/          # 业务组件
│   │   │   ├── TransactionForm/ # 交易表单
│   │   │   ├── TransactionList/ # 交易列表
│   │   │   ├── AccountManager/ # 账户管理
│   │   │   ├── CategoryManager/ # 分类管理
│   │   │   └── BudgetManager/ # 预算管理
│   │   └── layout/            # 布局组件
│   │       ├── PersonalLayout/ # 个人端主布局
│   │       └── Sidebar/       # 侧边栏
│   ├── composables/           # 组合式函数
│   │   ├── useTransaction.ts  # 交易管理hooks
│   │   ├── useAccount.ts      # 账户管理hooks
│   │   ├── useCategory.ts     # 分类管理hooks
│   │   ├── useSpeech.ts       # 语音识别hooks
│   │   ├── useOCR.ts          # OCR识别hooks
│   │   ├── useOffline.ts      # 离线数据hooks
│   │   └── useSync.ts         # 数据同步hooks
│   ├── router/                # 路由配置
│   │   ├── index.ts           # 路由配置
│   │   ├── routes.ts          # 路由定义
│   │   └── guards.ts          # 路由守卫
│   ├── stores/                # 状态管理
│   │   ├── modules/           # 按模块组织
│   │   │   ├── transaction.ts # 交易状态
│   │   │   ├── account.ts     # 账户状态
│   │   │   ├── category.ts    # 分类状态
│   │   │   ├── budget.ts      # 预算状态
│   │   │   └── analysis.ts    # 分析状态
│   │   └── index.ts           # 导出配置
│   ├── types/                 # TypeScript类型定义
│   │   ├── transaction.ts     # 交易类型
│   │   ├── account.ts         # 账户类型
│   │   ├── category.ts        # 分类类型
│   │   └── analysis.ts        # 分析类型
│   ├── utils/                 # 工具函数
│   │   ├── finance.ts         # 财务计算工具
│   │   ├── format.ts          # 格式化工具
│   │   ├── export.ts          # 数据导出工具
│   │   ├── storage.ts         # 本地存储工具
│   │   └── sync.ts            # 同步工具
│   ├── views/                 # 页面组件
│   │   ├── dashboard/         # 仪表板页面
│   │   │   ├── index.vue      # 主仪表板
│   │   │   └── components/    # 仪表板组件
│   │   ├── accounting/        # 记账页面
│   │   │   ├── index.vue      # 记账首页
│   │   │   ├── quick.vue      # 快速记账
│   │   │   ├── manual.vue     # 手动记账
│   │   │   ├── voice.vue      # 语音记账
│   │   │   └── ocr.vue        # OCR记账
│   │   ├── transactions/      # 交易管理页面
│   │   │   ├── index.vue      # 交易列表
│   │   │   ├── detail.vue     # 交易详情
│   │   │   └── edit.vue       # 交易编辑
│   │   ├── accounts/          # 账户管理页面
│   │   ├── categories/        # 分类管理页面
│   │   ├── analysis/          # 财务分析页面
│   │   ├── budget/            # 预算管理页面
│   │   └── settings/          # 设置页面
│   ├── styles/                # 样式文件
│   │   ├── main.css           # 主样式文件
│   │   ├── variables.css      # CSS变量
│   │   ├── components.css     # 组件样式
│   │   └── themes.css         # 主题样式
│   ├── constants/             # 常量定义
│   │   ├── categories.ts      # 预设分类
│   │   ├── accounts.ts        # 预设账户
│   │   └── config.ts          # 配置文件
│   └── locales/               # 国际化
│       ├── zh-CN/             # 中文语言包
│       └── en-US/             # 英文语言包
├── public/                    # 公共资源
├── tests/                     # 测试文件
└── types/                     # 全局类型定义
```

## 4. 核心功能组件规范

### 4.1 快速记账组件
- 组件职责：提供便捷的记账界面，支持收入、支出、转账三种类型
- 核心功能：金额输入、分类选择、账户选择、备注录入、交易时间设置
- 交互要求：实时验证输入完整性，提供清晰的视觉反馈
- 性能要求：组件加载延迟不超过100ms，记账操作响应时间不超过300ms

### 4.2 交易列表组件
- 组件职责：展示交易记录，支持筛选、排序、分页功能
- 核心功能：多条件筛选、批量操作、数据导出、明细查看
- 交互要求：支持键盘快捷键，提供直观的批量操作界面
- 性能要求：大数据量下保持流畅滚动，筛选响应时间不超过200ms

### 4.3 财务分析图表组件
- 组件职责：可视化展示财务数据，提供多种图表类型
- 核心功能：趋势图、饼图、柱状图、预算对比图
- 交互要求：支持图表交互、数据钻取、导出图片
- 性能要求：图表渲染延迟不超过500ms，支持大数据量渲染

## 5. 智能功能规范

### 5.1 语音记账功能
- 功能要求：支持中文语音识别，自动解析金额和分类信息
- 技术实现：基于Web Speech API，集成自然语言处理
- 用户体验：实时语音反馈，支持中断和重新录制
- 准确性要求：金额识别准确率≥95%，分类识别准确率≥80%

### 5.2 OCR图片识别功能
- 功能要求：支持小票、发票等图片识别，自动提取财务信息
- 技术实现：集成第三方OCR服务，支持多种图片格式
- 用户体验：实时预览识别结果，支持手动修正
- 准确性要求：金额识别准确率≥90%，分类识别准确率≥75%

### 5.3 智能分类建议功能
- 功能要求：基于历史记录和机器学习提供分类建议
- 技术实现：本地机器学习模型，支持离线推理
- 用户体验：实时显示建议选项，支持学习用户习惯
- 准确性要求：建议准确率≥85%，支持用户反馈纠正

## 6. 数据管理规范

### 6.1 离线数据存储
- 存储策略：使用IndexedDB存储离线交易数据
- 同步机制：网络恢复时自动同步数据到服务器
- 冲突解决：支持服务端优先和客户端优先两种策略
- 数据一致性：保证离线操作后的数据完整性

### 6.2 数据导出功能
- 支持格式：Excel、CSV、PDF多种导出格式
- 导出内容：交易明细、统计报表、预算报告
- 性能要求：大数据量导出不超过10秒
- 安全要求：敏感数据加密处理，防止泄露

### 6.3 数据备份恢复
- 备份策略：定期自动备份，支持手动备份
- 恢复功能：支持全量恢复和增量恢复
- 数据验证：恢复前验证数据完整性和一致性
- 用户体验：提供备份进度和恢复状态反馈

## 7. 性能优化规范

### 7.1 加载性能
- 首屏加载：页面首屏渲染时间不超过2秒
- 组件加载：核心组件懒加载，减少初始包体积
- 资源优化：图片压缩、字体子集化、代码分割
- 缓存策略：合理使用浏览器缓存和服务端缓存

### 7.2 运行时性能
- 列表渲染：虚拟滚动支持大数据量展示
- 图表渲染：ECharts优化配置，支持大数据量渲染
- 内存管理：及时清理不需要的数据和事件监听
- 计算优化：使用Web Worker处理复杂计算

### 7.3 用户体验优化
- 响应时间：所有用户操作响应时间不超过300ms
- 流畅度：动画和过渡效果保持60fps流畅度
- 无障碍：支持键盘导航和屏幕阅读器
- 移动适配：响应式设计，支持触摸操作

## 8. 安全规范

### 8.1 数据安全
- 敏感数据：用户财务数据加密存储和传输
- 访问控制：基于角色的权限控制机制
- 数据脱敏：日志和备份中的敏感信息脱敏处理
- 审计日志：记录重要操作，便于安全审计

### 8.2 网络安全
- HTTPS：强制使用HTTPS传输所有数据
- 请求验证：API请求签名和时间戳验证
- 限流控制：防止恶意请求和DDoS攻击
- 异常处理：安全的错误信息展示，不暴露系统细节

---

**文档版本**：1.0.0
**最后更新**：2026-01-17
**维护人员**：前端开发团队