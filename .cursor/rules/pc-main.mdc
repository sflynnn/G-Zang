---
alwaysApply: false
filePathPatterns:
  - "apps/pc-main/**"
framework: "Vue 3 + TypeScript + qiankun"
description: "G-Zang PC主应用微前端开发规范"
---

# G-Zang PC主应用 (PC-Main) MDC 规则

## 项目设计哲学

### "智简·归藏" (Smart & Solid)
G-Zang (归藏) 是面向B2B & B2C的SaaS财务管理系统，秉承"智简·归藏"的设计理念：
- **企业端**：强调"Trust/Professionalism" (信任/专业性) - 为企业提供稳定、专业、可信赖的财务管理解决方案
- **个人端**：强调"Simplicity/Fun" (简洁/有趣) - 为个人用户提供简单有趣的记账体验

### 核心隐喻
- **Container (藏)**：卡片式布局，象征财富存储容器
- **Flow (流)**：流畅的页面过渡和清晰的数据可视化

## 1. 应用概述

### 1.1 应用定位
PC主应用是G-Zang财务管理系统的微前端主应用，基于qiankun框架实现。负责统一的用户认证、应用注册、路由分发、状态管理以及子应用的生命周期管理。

### 1.2 核心功能
- **用户认证**：统一的登录认证和权限验证
- **应用管理**：子应用的注册、加载、卸载
- **路由分发**：根据用户权限动态加载对应子应用
- **状态共享**：全局状态管理和子应用间通信
- **统一UI**：顶部导航、侧边栏、用户信息等统一界面
- **错误处理**：子应用加载失败的降级处理

## 2. 技术栈规范

### 2.1 基础技术栈
- **框架**：Vue 3.0+ (Composition API)
- **构建工具**：Vite 4.x
- **微前端**：qiankun 3.x
- **状态管理**：Pinia
- **路由**：Vue Router 4
- **HTTP客户端**：Axios (通过packages/shared)
- **类型系统**：TypeScript 5.x
- **UI组件库**：Naive UI
- **样式框架**：**Tailwind CSS (优先级最高)** - 所有样式均使用Tailwind实现
- **图标库**：Iconify / Lucide-vue
- **图表库**：ECharts (必须使用定义的颜色调色板)
- **字体系统**：
  - UI界面：Inter
  - 中文文本：PingFang SC
  - 数字/货币：DIN Alternate / Roboto Mono (必须使用 `font-mono` 或 `tabular-nums` 类确保表格中垂直对齐)
- **代码规范**：ESLint + Prettier

### 2.2 设计系统规范 (严格实现)

#### 2.2.1 颜色调色板 (Tailwind Config)
在 `tailwind.config.js` 中扩展以下精确的十六进制颜色代码：

```javascript
// tailwind.config.js
module.exports = {
  theme: {
    extend: {
      colors: {
        // 品牌色
        primary: '#0F4C5C',      // 归藏青 - 用于：标题、侧边栏、主文本
        secondary: '#FB8B24',    // 琉璃金 - 用于：CTA、高亮、VIP图标

        // 功能色
        success: '#06D6A0',      // 利润绿
        danger: '#EF476F',       // 支出红 (偏洋红色)
        background: '#F8F9FA',   // 浅灰背景
        surface: '#FFFFFF',      // 卡片背景
      }
    }
  }
}
```

#### 2.2.2 排版规则
- **标题**：`font-weight-600 text-primary`
- **正文**：`font-weight-400 text-slate-700`
- **数字/货币**：**CRITICAL** - 所有财务数字必须使用 `font-mono` 或 `tabular-nums` 类确保表格中垂直对齐

#### 2.2.3 UI组件样式
**卡片 (Container 隐喻)**：
- 背景：`bg-surface` (白色)
- 圆角：`rounded-xl` (移动端) 或 `rounded-2xl` (桌面端)
- 阴影：`shadow-[0_4px_12px_rgba(0,0,0,0.05)]` (柔和浮雕)
- 内边距：`p-6`

**按钮**：
- **主要按钮 (CTA)**：`bg-secondary text-white hover:bg-opacity-90 active:scale-95 transition-all rounded-lg shadow-md`
- **次要按钮**：`border border-primary text-primary hover:bg-primary hover:text-white`

#### 2.2.4 布局模式
**企业场景 (Desktop First)**：
- 氛围：密集、专业、有序
- 结构：固定侧边栏 (`bg-primary` 深色主题)、网格布局仪表板、数据表格
- 表格：条纹行 (`even:bg-slate-50`)、货币列右对齐小数点

**个人场景 (Mobile First)**：
- 氛围：干净、空旷、友好
- 结构：单列垂直滚动、顶部大资产卡片 (渐变背景 `bg-gradient-to-r from-primary to-[#186a7d]`)、底部标签栏
- 导航：底部标签栏，中间Magic Button

### 2.2 微前端特性技术栈
- **应用通信**：qiankun props + 全局状态
- **应用隔离**：JS沙箱 + CSS隔离
- **应用加载**：动态import + 懒加载
- **状态同步**：initGlobalState + 发布订阅
- **应用监控**：性能监控 + 错误边界

## 3. 项目结构规范

### 3.1 目录结构
```
apps/pc-main/
├── src/
│   ├── micro-apps/           # 微前端应用配置
│   │   ├── index.ts         # 子应用注册配置
│   │   ├── apps.ts          # 子应用定义
│   │   └── lifecycle.ts     # 生命周期管理
│   ├── components/          # 组件目录
│   │   ├── layout/          # 布局组件
│   │   │   ├── AppHeader.vue    # 顶部导航栏
│   │   │   ├── AppSidebar.vue   # 侧边栏导航
│   │   │   ├── AppFooter.vue    # 底部信息栏
│   │   │   └── AppBreadcrumb.vue # 面包屑导航
│   │   ├── common/          # 通用组件
│   │   │   ├── UserMenu.vue     # 用户菜单
│   │   │   ├── AppSwitcher.vue  # 应用切换器
│   │   │   └── LoadingSpinner.vue # 加载动画
│   │   └── micro-app/       # 微前端相关组件
│   │       ├── MicroAppContainer.vue # 子应用容器
│   │       ├── AppLoader.vue        # 应用加载器
│   │       └── ErrorBoundary.vue    # 错误边界
│   ├── router/              # 路由配置
│   │   ├── index.ts         # 主路由配置
│   │   ├── routes.ts        # 路由定义
│   │   └── guards.ts        # 路由守卫
│   ├── stores/              # 状态管理
│   │   ├── auth.ts          # 认证状态
│   │   ├── app.ts           # 应用状态
│   │   ├── user.ts          # 用户状态
│   │   └── micro-app.ts     # 微前端状态
│   ├── composables/         # 组合式函数
│   │   ├── useAuth.ts       # 认证hooks
│   │   ├── useMicroApp.ts   # 微前端hooks
│   │   ├── useGlobalState.ts # 全局状态hooks
│   │   └── useAppLoader.ts  # 应用加载hooks
│   ├── types/               # TypeScript类型定义
│   │   ├── micro-app.ts     # 微前端类型
│   │   ├── user.ts          # 用户类型
│   │   └── global-state.ts  # 全局状态类型
│   ├── utils/               # 工具函数
│   │   ├── micro-app.ts     # 微前端工具
│   │   ├── permission.ts    # 权限工具
│   │   └── constants.ts     # 常量定义
│   ├── views/               # 页面组件
│   │   ├── Login.vue        # 登录页面
│   │   ├── Home.vue         # 首页
│   │   ├── MicroApp.vue     # 微前端页面容器
│   │   ├── 403.vue          # 无权限页面
│   │   ├── 404.vue          # 页面不存在
│   │   └── Maintenance.vue  # 维护页面
│   ├── styles/              # 样式文件
│   │   ├── main.css         # 主样式文件
│   │   ├── variables.css    # CSS变量
│   │   ├── layout.css       # 布局样式
│   │   └── micro-app.css    # 微前端样式
│   ├── constants/           # 常量定义
│   │   ├── routes.ts        # 路由常量
│   │   ├── apps.ts          # 应用常量
│   │   └── permissions.ts   # 权限常量
│   └── locales/             # 国际化
│       ├── zh-CN/           # 中文语言包
│       └── en-US/           # 英文语言包
├── public/                  # 公共资源
├── tests/                   # 测试文件
└── types/                   # 全局类型定义
```

## 4. 微前端架构规范

### 4.1 子应用注册配置
基于qiankun框架实现子应用的注册和管理，支持动态注册和权限控制。

### 4.2 子应用定义
定义子应用的基本信息、入口地址、激活路由等配置，支持多环境部署。

### 4.3 生命周期管理
实现子应用的加载、挂载、卸载等生命周期管理，确保资源正确释放。

### 4.4 全局状态管理
基于qiankun的全局状态管理，实现主应用与子应用间的状态共享和通信。

## 5. 布局组件规范

### 5.1 主布局组件
基于Naive UI的Layout组件实现微前端主应用的整体布局结构，包含侧边栏、顶部导航栏、内容区域和底部信息栏。

### 5.2 子应用容器组件
基于qiankun框架实现子应用的加载、渲染和生命周期管理，提供加载状态显示和错误处理机制。

## 6. 路由管理规范

### 6.1 主应用路由配置
基于Vue Router 4实现微前端主应用的路由管理，支持子应用路由的动态注册和权限控制。

### 6.2 路由定义
按业务模块组织路由配置，支持嵌套路由和路由参数传递。

### 6.3 路由守卫
实现路由级别的权限验证和子应用加载前的预处理逻辑。

## 7. 状态管理规范

### 7.1 微前端状态管理
基于Pinia实现微前端架构下的状态管理，支持主应用和子应用间的状态共享。

## 8. 代码生成规范

### 8.1 微前端主应用生成
生成微前端主应用时，请明确指定：
- **子应用配置**：应用名称、入口地址、激活路由
- **权限控制**：用户角色权限、应用访问权限
- **状态共享**：需要共享的全局状态和通信接口
- **UI规范**：导航菜单、用户信息、应用切换

### 8.2 示例指令
明确指定子应用配置、权限控制、状态共享和UI规范要求。

#### 8.2.1 设计系统遵循要求
- **严格使用定义的颜色调色板**：primary (#0F4C5C)、secondary (#FB8B24)、success (#06D6A0)、danger (#EF476F)
- **字体系统**：UI使用Inter，正文使用PingFang SC，数字必须使用等宽字体
- **组件样式**：卡片使用白色背景、柔和阴影、rounded-xl圆角
- **布局模式**：企业端使用固定侧边栏，个人端使用移动优先响应式设计

## 9. 国际化与主题规范

### 9.1 PC主应用国际化配置

#### 9.1.1 主应用语言包结构
按功能模块组织语言包，支持嵌套结构和动态加载。

#### 9.1.2 主应用语言包示例
提供主应用的语言包配置示例，支持多语言切换。

#### 9.1.3 子应用语言包加载
支持子应用语言包的动态加载和语言同步。

### 9.2 PC主应用主题系统

#### 9.2.1 全局主题配置
基于Naive UI的主题系统，支持浅色、深色主题切换。

#### 9.2.2 主题切换逻辑 (Naive UI微前端)
实现主题切换的逻辑，支持用户偏好保存和系统主题跟随。

#### 9.2.3 子应用主题同步 (Naive UI)
确保主应用和子应用的主题保持一致。

### 9.3 主应用布局主题适配

#### 9.3.1 主应用根组件配置
在应用入口配置主题提供者和全局样式。

#### 9.3.2 顶部导航栏主题适配
为顶部导航栏提供主题适配，支持亮色和深色主题。

#### 9.3.3 侧边栏主题适配
为侧边栏提供主题适配，支持折叠状态和主题切换。

---

**文档版本**：1.1.0
**最后更新**：2026-01-18
**维护人员**：前端开发团队