<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gzang.app.mapper.BudgetMapper">

    <!-- 查询用户预算列表 -->
    <select id="selectBudgetsByUserId" resultType="com.gzang.app.entity.Budget">
        SELECT * FROM t_budget
        WHERE user_id = #{userId}
        <if test="companyId != null">
            AND company_id = #{companyId}
        </if>
        <if test="bookId != null">
            AND book_id = #{bookId}
        </if>
        <if test="periodType != null">
            AND period_type = #{periodType}
        </if>
        ORDER BY create_time DESC
    </select>

    <!-- 分页查询预算 -->
    <select id="selectBudgetPage" resultType="com.gzang.app.entity.Budget">
        SELECT * FROM t_budget
        WHERE user_id = #{userId}
        <if test="companyId != null">
            AND company_id = #{companyId}
        </if>
        <if test="bookId != null">
            AND book_id = #{bookId}
        </if>
        <if test="periodType != null">
            AND period_type = #{periodType}
        </if>
        ORDER BY create_time DESC
    </select>

    <!-- 查询启用了预警的预算 -->
    <select id="selectWarningEnabledBudgets" resultType="com.gzang.app.entity.Budget">
        SELECT * FROM t_budget
        WHERE user_id = #{userId}
        <if test="companyId != null">
            AND company_id = #{companyId}
        </if>
        AND warning_enabled = true
        ORDER BY create_time DESC
    </select>

    <!-- 查询所有活跃预算（用于预警检查） -->
    <select id="selectActiveBudgets" resultType="com.gzang.app.entity.Budget">
        SELECT * FROM t_budget
        WHERE warning_enabled = true
        AND NOW() BETWEEN period_start AND period_end
        ORDER BY create_time DESC
    </select>

</mapper>
